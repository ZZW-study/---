本手册包含 2 份源文件：E:\GithubProject\docs\general\计算机基础知识整合指南\06-数据库.md、E:\GithubProject\docs\interview\面试题_数据库.md

# 数据库、缓存与向量存储

### 1. 红黑树是什么

> 本质上就是 **"BST + 一组约束条件 + 插入/删除时通过旋转和变色来维护平衡"**。

红黑树(Red-Black Tree)是一种 **自平衡的二叉搜索树(BST)**。
它通过给每个节点额外加一个"颜色"(红 / 黑)和一组规则,**保证树的高度始终维持在 `O(log n)` 级别**,
从而避免普通 BST 退化成链表(极端情况下查找变 `O(n)`)。

---

### 2. 红黑树的 5 条核心性质(必须背下来)

| 编号 | 性质                                                                      | 含义                         |
| ---- | ------------------------------------------------------------------------- | ---------------------------- |
| ①   | **每个节点要么红色,要么黑色，红黑树的每个节点，都只有一个key**      | 没有第三种颜色               |
| ②   | **根节点是黑色**                                                    | 根不能是红                   |
| ③   | **每个叶子节点(NIL 空节点)是黑色**                                  | NIL 是哨兵,图中常省略        |
| ④   | **红色节点的两个子节点必须都是黑色**(不能连续两个红节点)            | "红不能挨红"                 |
| ⑤   | **从任一节点出发,到其所有后代 NIL 节点的路径上,黑色节点的数量相同** | 这叫"黑高"(black height)一致 |

> 这 5 条合起来就保证了:**最长路径 ≤ 2 × 最短路径**,
> 所以红黑树是"近似平衡"而不是"严格平衡"(不像 AVL 树那样要求左右子树高度差 ≤ 1)。

---

### 3. 红黑树的结构图示

```
                    13(B)            ← 根,黑
                   /     \
                 8(R)    17(R)        ← 红色节点
                /  \    /    \
             1(B) 11(B) 15(B) 25(B)  ← 子节点都是黑
              \
              (NIL)                   ← 叶子全是黑哨兵
```

- 根 13 是黑 ✓
- 红色 8、17 的子节点都是黑 ✓
- 从根到任意 NIL,经过的黑色节点数量都是 2(或 3)✓

对照 5 条性质看:

---

### 4. 关键问题:红黑树的数据存在哪?

**答:所有节点都存数据。**

红黑树是内存里的二叉树,**每个真实节点(不管根、内部、叶子)都同时存"键 + 值"**。
所谓"叶子 NIL"只是哨兵空节点,不存真实数据。

**查找路径举例**:要找 key=15,从根 13 出发,13<15 走右子树到 17,17>15 走左子树到 15,命中。
**沿途每个节点本身就携带数据**。

---

### 5. 为什么要"红"和"黑"?

```
依次插入 1、2、3、4、5、6、7:

- **变色**:把节点从红改黑或反过来。**O(1),极快**。
- **旋转**:调指针方向,左旋或右旋。**最多 3 次**。

- **最短路径**:全是黑节点(没红色)
- **最长路径**:一红一黑交替(不能连续两红)
- 所以 **最长 ≤ 2 × 最短**

> **"平衡"的目的:树越矮,查找越快。** 退化成链 = 树不平衡 = 查找慢。

> **给每个节点染"红"或"黑",并定义一组颜色规则。**
> **插入/删除时,如果违反规则,只用"变色"和"最多 3 次旋转"修复。**
> **修复完,树就保证"大致平衡"——最长路径 ≤ 2 × 最短路径。**

#### 1) 普通 BST 的问题:有序插入会退化成链表

二叉搜索树(BST)在数据**有序插入**时会退化成链表:

1
 \
  2
   \
    3
     \
      4
       \
        5
         \
          6
           \
            7
```

高度 = 7 层,其实就是一条链。**查找 7 要走 7 步**,O(n) 退化成线性查找,完全崩了。

#### 2) 红黑树怎么解决:用"颜色"约束,加上"变色 + 旋转"修复

红黑树的核心思路:

两种操作:

**用极小的代价,换来"树永远不会退化成链"**——这就是红和黑存在的意义。

#### 3) "大致平衡"是怎么算出来的

颜色规则 ④ + ⑤(见 §2)联合推出:

这就是"大致平衡"的数学保证,**不是严格平衡**(AVL 树要求左右子树高度差 ≤ 1,代价是插入要旋转很多次)。

---

### 5.5 一棵红黑树的产生过程(7 节点实例)

#### 为什么新插入的节点默认"染红"?

```
        10(B)
```

```
        10(B)
            \
            20(R)        ← 红,但父 10 是黑,没违规
```

```
        10(B)
            \
            20(R)
                \
                30(R)   ← 父 20 是红,违规!红挨红了
```

```
  修复前 → 修复后(左旋 20,30 上来染黑,20 下去染红):

```
            10(B)
                \
                30(B)
                /
             20(R)
             /
          15(R)              ← 父 20 是红,违规!
```

```
  修复前 → 修复后(右旋 20,15 上来染黑,20 下去染红):

```
            10(B)
                \
                30(B)
                /
             15(B)
                \
              20(R)
                \
               25(R)         ← 父 20 是红,违规!
```

```
  修复前 → 修复后(左旋 20,25 上来染黑,20 下去染红):

```
            10(B)
            /    \
         5(R)    30(B)         ← 5 红,父 10 黑 ✓ 没违规
                 /
              15(B)
                 \
               25(B)
                /
             20(R)
```

```
            10(B)
            /    \
         5(R)    30(B)
         /        /
        1(R)    15(B)         ← 1 红,父 5 红,违规!
                 \
               25(B)
                /
             20(R)
```

```
  修复前 → 修复后(右旋 5,1 上去染黑,5 下去染红):

```
                          10(B)              ← 根,黑
                       /        \
                    1(B)         30(B)        ← 1 染黑,30 黑
                     \          /
                    5(R)      15(B)          ← 5 红,15 黑
                                \
                              25(B)          ← 25 黑
                              /
                          20(R)              ← 20 红
```

- 根 10 是黑 ✅
- 红色节点(5、20)的子都是黑 ✅
- 黑高一致(任何路径的黑节点数都是 1 或 2)✅

- 染红 → 可能违反规则 ④(红挨红)→ 只需局部修复(变色 / 旋转)
- 染黑 → 立刻违反规则 ⑤(黑高变了)→ 影响整棵树,难修

> **目的**:用一组实际数据演示"插入 → 颜色变化 → 旋转"完整过程。
> **新插入的节点默认染红**(为啥?最后讲)。

> **红黑树的"自平衡",本质就是"用颜色把约束变小,用最少代价修复"**。

#### 起点:空树,依次插入 10 → 20 → 30 → 15 → 25 → 5 → 1

---

**步骤 1:插入 10**

空树插入 10,直接当根,**染黑**(规则②)。

---

**步骤 2:插入 20** — 放 10 的右边,染红

---

**步骤 3:插入 30** — 放 20 的右边,染红

🚨 **违规:20 红 + 30 红**。**修复**(叔不存在 = 算叔黑 → 旋转 + 变色):

        10(B)               10(B)
            \                   \
            20(R)  →             30(B)         ← 染黑
                \               /
                30(R)         20(R)            ← 染红
```

✅ 树高 = 3,平衡。

---

**步骤 4:插入 15** — 放 20 的左边,染红

🚨 **违规:20 红 + 15 红**。叔(30)黑 → **右旋 20 + 变色**:

            10(B)               10(B)
                \                   \
                30(B)                30(B)
                /                     /
             20(R)        →        15(B)        ← 染黑
             /                        \
          15(R)                      20(R)        ← 染红
```

✅ 树高 = 4(没变,旋转只调了节点位置,没改变高度)。

---

**步骤 5:插入 25** — 放 30 左 → 放 15 右 → 放 20 右,染红

🚨 **违规:20 红 + 25 红**。叔(15 的右孩子)不存在 → 算叔黑 → **左旋 20 + 变色**:

            10(B)               10(B)
                \                   \
                30(B)                30(B)
                /                     /
             15(B)                  15(B)
                \                      \
              20(R)        →          25(B)    ← 染黑
                \                    /
               25(R)              20(R)       ← 染红
```

✅ 树高 = 5。

---

**步骤 6:插入 5** — 放 10 的左边,染红

✅ **没违规**,啥也不用做。

---

**步骤 7:插入 1** — 放 5 的左边,染红

🚨 **违规:5 红 + 1 红**。叔(30)黑 → **右旋 5 + 变色**:

            10(B)               10(B)
            /    \               /    \
         5(R)    30(B)        1(B)    30(B)  ← 1 染黑
         /        /             \      /
        1(R)    15(B)    →     5(R)  15(B)  ← 5 染红
                 \                \
               25(B)             25(B)
                /                  /
             20(R)              20(R)
```

✅ 修复完。

---

#### ✅ 最终红黑树(7 个节点)

**核对 5 条规则**:

**结果:7 个节点、树高 5,完全没退化成链**(如果是链要 7 层,现在压到 5 层)。

---

**染红 = 把违规控制在局部,容易修。**

---

### 6. 红黑树的应用场景

- Java `TreeMap`、`TreeSet`
- C++ STL `std::map`、`std::set`
- Linux 内核:进程调度(CFS)、epoll 事件管理
- Java 8 `HashMap` 链表转红黑树(桶长度 > 8 时)

---

### 7. B 树是什么

> 一句话:**B 树 = 多叉的、平衡的、每个节点都存数据的搜索树**。

B 树(B-Tree)是一种 **多路平衡搜索树**(一个节点可以有多个孩子)。
它是二叉树的"加宽版"——**为了减少磁盘 I/O 次数而设计**。

---

### 8. B 树的核心性质(以 m 阶 B 树为例)

| 编号 | 性质                                            | 说明                |
| ---- | ----------------------------------------------- | ------------------- |
| ①   | 每个节点**最多有 m 个孩子**               | 也就最多 m-1 个键   |
| ②   | 除根外,每个节点**至少有 ⌈m/2⌉ 个孩子**  | 保证"胖",不瘦成二叉 |
| ③   | 根节点至少 2 个孩子(除非是叶子)                 | 根是特殊节点        |
| ④   | **有 k 个孩子的节点,里面有 k-1 个键**     | 键用来"分叉"        |
| ⑤   | **所有叶子节点在同一层**                  | 这是 B 树平衡的关键 |
| ⑥   | 键在节点内按升序排列,子树范围满足 a < b < c ... | BST 规则的多路版本  |

> 一个常用的具体例子:**5 阶 B 树**(也称 (2,5) 树或 (2,4) 树),
> 每个节点 2~4 个键,3~5 个孩子。

---

### 9. B 树的结构图示

```
                        [ 10 | 20 | 30 ]                    ← 根,3 个键,4 个孩子
                      /     |      |      \
                     /      |      |       \
              [3|5]    [12|15]   [22|25]   [35|40|45]        ← 叶子,全在同一层
              / | \    /  |  \   /  |  \    /  |  \  \
           (数据)(数据)(数据)(数据)(数据)(数据)(数据)(数据) ← 所有叶子都存数据
```

- 根有 3 个键(10、20、30),所以有 4 个孩子
- 叶子都在**同一水平线**上
- 范围划分:左子树 < 10,< 12 且 > 10,< 22 且 > 20,> 30

注意:

以 **5 阶 B 树**为例(每个节点最多 4 个键、5 个孩子):

---

### 10. 关键问题:B 树的数据存在哪?

- 根节点 [10|20|30] **本身就是数据记录**(比如数据库一行)
- 内部节点 [12|15] **也是数据**
- 叶子节点 [3|5] **还是数据**

**答:所有节点都存数据(根、内部节点、叶子都存)。**

这是 B 树和 B+ 树**最大的区别之一**。B 树中:

所以你"命中"任何一个节点都算查找成功,**不用一定查到叶子**。

---

### 11. B 树的高度有多高?为什么说它"矮"?

| 树类型         | 高度                            | 每次查询磁盘 I/O        |
| -------------- | ------------------------------- | ----------------------- |
| 二叉树(红黑树) | log₂(1亿) ≈**27 层**    | 27 次(如果数据在磁盘上) |
| 100 阶 B 树    | log₁₀₀(1亿) ≈**3 层** | 3 次                    |

> 节点"变胖"是有上限的,因为一个节点对应**磁盘的一页(page)**,通常是 4KB 或 16KB,
> 一次 I/O 只能读一页。所以阶数 m 不能无限大。

以 m=100 阶 B 树(一个节点 100 个键)存 1 亿条数据为例:

**这就是 B 树存在的意义**:通过让每个节点"变胖"(多叉),把高度从 27 压到 3。
**磁盘 I/O 每减少一次,查询时间就可能从毫秒级降到微秒级**。

---

### 12. 为什么磁盘存储要用 B 树?

- 一次磁盘 I/O ≈ 几毫秒(机械硬盘)~ 几十微秒(SSD)
- 内存访问 ≈ 几十纳秒
- 两者相差 **10万倍以上**

磁盘读写的**最小单位是"页"(block / page)**:

所以设计磁盘数据结构的核心原则就是:**尽量减少磁盘 I/O 次数**。
B 树通过"一节点 = 一页 = 一次 I/O 装下很多键"来做到这一点。

---

### 13. 选型决策

- 数据在**内存**、要支持**有序 + 快速增删改查** → **红黑树**
- 数据在**磁盘 / 文件系统**、单点查询为主 → B 树
- 数据在**磁盘 / 数据库**、**既要单点查询又要范围查询** → **B+ 树**(99% 选这个)

---

### 14. 常见疑问解答

- 内存中:红黑树"层数多"(如 20 层)其实是好的,因为它意味着每次比较只用 CPU 算,20 纳秒
- 磁盘中:**层数多 = I/O 多 = 灾难**(一次 I/O 是 20 纳秒的 **100 万倍**)

- 红黑树:✓ 好(CPU 计算快)
- B 树 / B+ 树:✗ 坏(要 20 次磁盘 I/O,会死)

**疑问一:为什么"层数高反而好"?**

这是个**反直觉的点**:

所以同样是"20 层高":

**B+ 树的目标就是把高度压到 2~4 层**。
这就是为什么我们要让节点"变胖"——一个节点装 100 个键 vs 装 1 个键,差别是 100 万倍。

## MySQL、InnoDB、索引与 Schema

### 1. MySQL

```
先回忆链表：每个节点存 [数据 | next指针]，next 指向下一个节点。

```
一个 InnoDB 表不止一棵 B+树：

- 聚簇索引（主键索引）：建表时自动创建，1 棵
  叶子节点存：完整行数据

- 每个二级索引：必须手动创建，各 1 棵
  叶子节点存：索引列的值 + 主键值

```
B+树持久存储在磁盘上，每个节点对应一个磁盘页（InnoDB 默认 16KB）。

```
节点结构：

```
为什么 B+ 树适合数据库：

```
一页 16KB 是什么意思：
  InnoDB 读写磁盘的最小单位是 16KB 的一块，这一块叫一个"页"（page）。
  就像看书，你想看一个字，但实际上翻的是整一页。
  InnoDB 默认页大小 = 16KB = 16384 字节，这是固定的。
  B+树的每个节点恰好占一个页。

```sql
SELECT id, name FROM user WHERE name = 'Tom';
```

```
第一步：在二级索引 B+ 树里找 name='Tom'
  叶子节点找到 [Tom, id=1]

```
怎么利用覆盖索引？
  建联合索引时，把查询需要的字段都包含进去：

```
以 user 表为例，有主键 + idx_name 索引：

```
查询优化器怎么选索引？

```text
聚簇索引叶子节点存整行数据，是表数据本身。
二级索引叶子节点只存索引列 + 主键，查完整数据通常要回表。
覆盖索引是查询字段全在二级索引里，不需要回表。
```

注意：child0 指向的子节点里可以有 [20, 40]，比父节点的 50 小；
child2 指向的子节点里可以有 [120, 150]，比父节点的 100 大。
这完全正常——父节点的 key 是"分界线"，不是"上下限"。
子节点的 key 只要属于 child 对应的那个区间就行。

**B+树节点到底存了什么**

B+树的非叶子节点类似，但不是 1 个指针，而是多个指针，中间用 key 隔开：

  [child0 | key1 | child1 | key2 | child2]

  - child0、child1、child2 都是指针，指向子节点（和链表的 next 一样）
  - key1、key2 是分界值，决定该走哪个 child

规则很简单：key 把所有数值切成几段，每段对应一个 child 指针。

以根节点 [50, 100] 为例，它有 3 个 child：
  child0 → 指向一个子节点，那个子树里的所有值 < 50
  child1 → 指向一个子节点，那个子树里的所有值 >= 50 且 < 100
  child2 → 指向一个子节点，那个子树里的所有值 >= 100

所以查找 id=70 时：
  70 >= 50 且 70 < 100 → 走 child1

叶子节点不存 child 指针，而是存真正的数据（或数据指针），
并且叶子之间用双向链表串起来，方便范围查询。

双向链表是什么？
  普通链表节点：[数据 | next指针]        → 只能往后走
  双向链表节点：[prev指针 | 数据 | next指针] → 往前往后都能走

  在 B+树的叶子节点里：
    [prev | 数据1, 数据2, ... | next]
     ↑                          ↑
     指向前一个叶子节点          指向后一个叶子节点

  这样范围查询时（如 WHERE id BETWEEN 100 AND 200），
  找到 100 所在的叶子节点后，沿着 next 指针一路往后扫就行，
  不用每次都回到根节点重新查找。

指针的本质是什么？
  指针就是一个地址（编号），告诉你"数据在哪个位置"。
  就像门牌号：门牌号本身不是房子，但它能定位到房子在哪。

  内存里：指针是内存地址（如 0x7FFF0012）
  磁盘上：指针是页号（如 page 1024），指向磁盘上某个页

  CPU 读取数据时，拿到的就是一整块连续的字节（数据 + 指针混在一起），
  至于哪些字节是数据、哪些是指针，由程序代码决定怎么解读和使用。
```

**2. 一个表有多少棵 B+ 树,或者说有多少个索引,这样走的时候,走索引,会产生选择**

例：users 表有主键、name 索引、email 索引
  → 共 3 棵独立的 B+树

第1棵（主键聚簇索引）：叶子节点存完整行（id, name, email）
第2棵（idx_name）：     叶子节点存 name 值 + id
第3棵（idx_email）：    叶子节点存 email 值 + id
```

**3. B+ 树存在磁盘还是内存**

查询时，用到的节点会被加载到内存的 Buffer Pool 中缓存：
  - 热点节点常驻内存，查询快
  - 未缓存的节点需要读磁盘，查询慢

┌────────────── 磁盘（持久存储）──────────────┐
│ 根节点 page、中间节点 page、叶子节点 page ...│
└─────────────────────────────────────────────┘
          │ 加载
          ▼
┌────────────── 内存 Buffer Pool（缓存）──────┐
│ 根节点（常驻）、最近使用的中间/叶子节点     │
└─────────────────────────────────────────────┘

本质：磁盘存全量，内存存热点。
```

#### 1）B+ 树(InnoDB 的选择)

非叶子节点：[key1 | key2 | ... | child0 | child1 | child2 | ...]
  - 只存 key + 子节点指针
  - 不存任何数据
  - 同样大小的页能放更多的 key

叶子节点：[prev | data1 | data2 | ... | dataN | next]
  - 存真正的数据（或数据指针）
  - 用双向链表串联，方便范围查询(范围查询，就是[a,b]，从a，b俩个点出发，不断压缩查中间即可)

┌─────────────────────────────────────────────┐
│ 非叶子节点（16KB 一页）                      │
│ key1, key2, key3, ... keyN                  │
│ child0, child1, child2, ... childN+1        │
│ → 不存 data，一页能放 500+ 个 key            │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ 叶子节点（16KB 一页）                        │
│ prev指针 | data1, data2, ... dataN | next指针│
│ → 存完整行数据，叶子之间双向链表             │
└─────────────────────────────────────────────┘
```

1. 非叶子节点不存 data → 一页放更多 key → 树更矮
   B 树一页可能放 100 个 key（因为带 data）
   B+ 树一页可能放 500 个 key（只有 key + 指针）
   同样千万数据，B+ 树可能 2 层就够，B 树可能要 3-4 层

2. 叶子节点双向链表 → 范围查询极快
   WHERE id BETWEEN 100 AND 200
   找到 100 的叶子后，沿 next 指针一路扫到 200

3. 查询稳定 → 每次都是从根到叶子，路径长度一致
   不会出现"有时查到非叶子就返回，有时要到叶子"的不一致

InnoDB 默认一个数据页 16KB，一个 B+ 树节点对应一页。
千万级数据，B+ 树高度通常 2-3 层，一次查询 2-3 次磁盘 IO。
```

一张表有多少页：
  页数 = 表数据总大小 / 16KB，取决于数据量。
  假设一行 200 字节，一个叶子页能装 ≈ 80 行：
    100万行 → ≈ 12500 个叶子页
    1000万行 → ≈ 125000 个叶子页
  整棵 B+树的页分布：
    根节点：1 页（常驻内存）
    中间层：几十页
    叶子层：上万~几十万页（占表的 99% 以上）
  查看实际页数：SHOW TABLE STATUS LIKE 'users';
                Data_length / 16384 = 页数

一页里面的数据是怎么存的：
  磁盘上所有数据都是 0 和 1（二进制）。
  一页 16KB = 16384 字节 = 131072 个 bit（0/1）。
  不同行、不同字段怎么隔开？不是用分隔符，而是用固定的"结构"来划分。

  一页的内部结构：
  ┌─────────────────────────────────────────────────────┐
  │ 页头（38字节）                                       │
  │   页号、页类型、记录数、空闲空间位置等元信息         │
  ├─────────────────────────────────────────────────────┤
  │ 用户数据区                                           │
  │   行1 → 行2 → 行3 → ...（每行紧密排列）              │
  │   每行开头有"行头"：字段数、每字段长度、事务ID等     │
  ├─────────────────────────────────────────────────────┤
  │ Slot 目录（页尾，倒着长）                             │
  │   指向每组记录的偏移量，用于页内二分查找              │
  └─────────────────────────────────────────────────────┘

  行数据的内部结构（以 {id=1, name="Tom", age=25} 为例）：
    [行头: 字段数=3, 各字段长度, 事务ID, 删除标记...]
    [字段1: id=1       → 4字节 0x00000001]
    [字段2: name="Tom" → 3字节 0x546F6D]
    [字段3: age=25     → 1字节 0x19]

    偏移量告诉每个字段从第几个字节开始、到第几个字节结束。
    不需要分隔符，靠"偏移量 + 长度"精确定位每个字段。

  类比：
    分隔符方式 → "Tom,25,1"  用逗号隔开，靠逗号定位
    偏移量方式 → [偏移0,长3][偏移3,长1][偏移4,长4]
                  不需要分隔符，靠偏移和长度直接跳到对应位置

  所以一页里 131072 个 0/1，靠页头、行头、偏移量这些结构信息，
  程序知道"从第几个字节到第几个字节是什么数据"。
```

---

#### 2）覆盖索引是什么

如果查询的字段在二级索引里都有，就不需要回表：

需要的字段：id 和 name
二级索引里有的：name 和 id

全部命中，不需要回表，直接返回。
这就叫"覆盖索引"——索引覆盖了查询需要的所有字段。
```

  CREATE INDEX idx_name_age ON user(name, age);

  这样 SELECT id, name, age FROM user WHERE name = 'Tom';
  二级索引叶子节点存 [name, age, id]，全部覆盖，不用回表。
```

---

#### 3）一张表有几棵 B+ 树

第 1 棵：主键聚簇索引
  非叶子节点：主键 key
  叶子节点：完整行数据（id, name, age）

第 2 棵：idx_name 二级索引
  非叶子节点：name key
  叶子节点：name 值 + id

两棵独立的 B+树，各自有根节点、中间节点、叶子节点。
查数据时，MySQL 根据 WHERE 条件决定走哪棵树。
```

  SELECT * FROM user WHERE name = 'Tom';
  → 走 idx_name 二级索引 → 回表拿完整数据

  SELECT * FROM user WHERE id = 5;
  → 直接走主键聚簇索引 → 不需要回表

  SELECT id, name FROM user WHERE name = 'Tom';
  → 走 idx_name 二级索引 → 覆盖索引，不回表
```

一句话：

---

### 3. 为什么 MySQL 主键推荐用自增 ID?

```text
新数据插到最后一个叶子页后面。
```

```text
新数据可能插入中间页。
如果中间页满了，就要页分裂。
```

```text
移动数据
产生更多磁盘 IO
页碎片增加
B+ 树维护成本变高
```

```text
短
有序
稳定
最好自增
```

```text
自增主键能让 B+ 树顺序增长，减少页分裂和磁盘 IO。
```

**合并后的问题：**
B+ 树和主键有什么关系？为什么不推荐 UUID 这种随机主键？

**答案：**
InnoDB 的表数据按主键聚簇索引组织。

如果主键是自增 ID，插入数据基本是顺序追加：

这样性能好，页分裂少。

如果主键是 UUID 或随机字符串，插入位置是随机的：

页分裂会导致：

所以主键建议：

一句话：

---

### 4. B+ 树中的页分裂和页合并是什么?

```
页分裂前：一页装满了 [1, 3, 5, 7, 9]，现在要插入 6

```
页分裂的代价：
  1. 申请新页 → 磁盘分配操作
  2. 移动一半数据 → 磁盘写入
  3. 更新父节点 → 磁盘写入
  4. 更新链表指针 → 磁盘写入

```
顺序插入 vs 随机插入：

```
页合并前：两页各有少量数据

```
删除数据不会立刻释放磁盘空间：

```
页分裂和页合并在什么时候发生：

```text
页分裂是插入时一页满了拆成两页，页合并是删除后两页太空合成一页。
两者都是 InnoDB 自动维护 B+ 树结构的机制，但都会产生磁盘 IO。
```

```
数据少的时候，B+树确实没什么用，一页就能装下所有数据，直接扫一页就完了。

```
一页 = 16384 字节
页头 = 38 字节
叶子页双向链表指针 = prev(8字节) + next(8字节) = 16 字节
可用空间 ≈ 16384 - 38 - 16 = 16330 字节

```
第 1 阶段：插入 id=1 到 id=81

```
第 2 阶段：插入 id=82，一页装不下了 → 触发页分裂

```
第 3 阶段：继续插入 id=83, 84, 85...

```
第 4 阶段：根节点装满了（约 94000 行时）

```
第 5 阶段：继续增长，直到千万、亿级

```
总结：B+树的真实生长过程

**合并后的问题：**
B+ 树插入和删除数据时，底层怎么维护结构？页分裂和页合并是什么意思？

#### 页分裂

一个页（16KB）能装的行数是有限的。当一页写满了，又要插入新数据，
InnoDB 必须把这一页拆成两页，腾出空间。这个过程叫页分裂。

┌─────────────────────────────────┐
│ 叶子页                          │
│ [1, 3, 5, 7, 9]  ← 满了        │
└─────────────────────────────────┘

页分裂过程：
  1. 申请一个新页（从磁盘分配 16KB）
  2. 把原页的一半数据移到新页
  3. 把新数据插入正确的位置
  4. 在父节点中插入一个新 key，指向新页

页分裂后：
┌──────────────────────┐  ┌──────────────────────┐
│ 叶子页 A             │  │ 叶子页 B             │
│ [1, 3, 5]            │  │ [6, 7, 9]            │
└──────────────────────┘  └──────────────────────┘
          ↕ 双向链表连接

父节点也要更新：
  原来：[key: 7] → 指向一个子页
  现在：[key: 5, 7] → 指向两个子页（A 和 B）
```

  一次页分裂可能产生 3-4 次磁盘 IO，很贵。
```

  顺序插入（如自增 id 1,2,3,4,5...）：
    新数据总是插到最后一个叶子页。
    写满一页 → 分裂一次 → 继续往后追加。
    分裂次数少，性能好。

  随机插入（如 UUID）：
    新数据可能插到任意位置的叶子页。
    中间页满了就要分裂，频繁发生。
    分裂次数多，性能差，碎片多。

这就是为什么主键推荐自增 id，不推荐 UUID。
```

---

#### 页合并

删除数据后，如果一页里的数据变得很少（低于 50%），
InnoDB 会检查相邻的页，如果两页加起来能放进一页，就把它们合并。
这个过程叫页合并。

┌──────────────────────┐  ┌──────────────────────┐
│ 叶子页 A             │  │ 叶子页 B             │
│ [1, 3]  （2条）      │  │ [7, 9]  （2条）      │
│ 利用率很低            │  │ 利用率很低            │
└──────────────────────┘  └──────────────────────┘

页合并过程：
  1. 把两页的数据合并到一页
  2. 释放空出来的那一页
  3. 在父节点中删除对应的 key

页合并后：
┌──────────────────────────────────┐
│ 叶子页 A                         │
│ [1, 3, 7, 9]  （4条，合并后）    │
└──────────────────────────────────┘

父节点也要更新：
  原来：[key: 5, 7] → 指向两个子页
  现在：[key: 7] → 只指向一个子页
```

  InnoDB 删除一行时，不会真的把字节擦掉，
  而是把那行标记为"已删除"（删除标记），空间标记为"可复用"。

  下次插入新数据时，如果正好落到这个页，可以复用被删除的空间。

  所以：
    DELETE 100万行 → 磁盘文件大小不变
    INSERT 新数据 → 可能复用之前删除的空间，文件还是不变

  想真正缩小文件，需要：
    OPTIMIZE TABLE user;  → 重建表，释放碎片空间
    或 ALTER TABLE user ENGINE=InnoDB;  → 效果类似
```

  页分裂：插入数据时，目标页满了 → 自动触发
  页合并：删除数据后，相邻页利用率 < 50% → 自动触发

  这两个过程都是 InnoDB 自动完成的，不需要手动操作。
  但频繁的分裂和合并会产生磁盘 IO，影响性能。
  所以主键用自增 id（顺序插入，减少分裂）是很重要的优化。
```

一句话：

#### 数据少时 B+树有什么用

数据量少（1页内）：
  B+树 = 1个根节点 = 1个叶子节点
  查找 = 扫这一页，和没索引差不多
  → B+树的优势体现不出来

数据量多（上万页）：
  B+树 = 1个根 + 几十个中间节点 + 上万个叶子
  查找 = 从根走到叶子，3次IO
  全表扫描 = 扫上万页
  → B+树的优势巨大：3次 vs 上万次

B+树的价值不是在数据少的时候，而是当数据增长到百万、千万行时，查找依然很快。

类比：
  一本书 10 页 → 有没有目录无所谓，翻一遍就行
  一本书 1000 页 → 有目录能省很多时间

InnoDB 不是等数据多了才建树，而是从第一行插入时就维护 B+树。
这样当数据从 100 行增长到 1000 万行时，索引一直在，不需要重建。
树是随着数据增长自动长大的。
```

#### B+树是怎么慢慢形成的

先算清楚一页能装多少数据（真实 16KB）：

假设每行数据 200 字节：
  一个叶子页能装 ≈ 16330 / 200 ≈ 81 行

非叶子节点（只存 key + child 指针）：
  每个 key 8 字节（bigint）+ 每个 child 指针 6 字节 = 14 字节
  一个非叶子页能装 ≈ 16330 / 14 ≈ 1166 个 key
```

按自增 id 逐条插入，看 B+树怎么长大：

  只有 1 个页，既是根节点也是叶子节点。
  所有数据都挤在这一页里。

  ┌──────────────────────────────────────────────┐
  │ 根 = 叶子（同一页）                           │
  │ [id=1, id=2, id=3, ... id=81]               │
  └──────────────────────────────────────────────┘
  树高 = 1 层
  查找：直接在这一页里找，不需要走路
```

  分裂过程：
    1. 申请一个新页（16KB）
    2. 把原页后一半数据（id=42~82）移到新页
    3. 原页保留前一半（id=1~41）
    4. 原来的根"升级"——从叶子变成非叶子节点
    5. 非叶子节点存分界 key=42，指向两个叶子页

  分裂后的结构（树高从 1 变成 2）：

             ┌──────────┐
             │ 根节点    │     ← 非叶子，存 key=42，指向两个子页
             │ [42]     │     ← 能装 1166 个 key，现在只用了 1 个
             └────┬─────┘
            ┌─────┴─────┐
            ▼           ▼
  ┌──────────────┐ ┌──────────────┐
  │ 叶子页 A     │ │ 叶子页 B     │
  │ [1~41]       │ │ [42~82]      │  ← 每页约 81 行
  └──────────────┘ └──────────────┘
       ↕ 双向链表连接

  树高 = 2 层
  查找：从根走到叶子，2 次磁盘 IO
```

  每个叶子页装满 81 行就分裂，产生新的叶子页。
  叶子页越来越多，但根节点能装 1166 个 key，不需要分裂。

  插入到 id=163 时，有 3 个叶子页：
             ┌──────────┐
             │ [42, 123]│     ← 根节点，2 个 key
             └────┬─────┘
       ┌──────────┼──────────┐
       ▼          ▼          ▼
  ┌────────┐ ┌────────┐ ┌────────┐
  │ [1~41] │ │[42~122]│ │[123~163]│  ← 3 个叶子页
  └────────┘ └────────┘ └────────┘

  插入到 id=1000 时，约有 13 个叶子页，根节点存 12 个 key。
  根节点容量 1166，还远远没满，树高一直是 2。
```

  算一下：根节点能装 1166 个 key → 指向 1167 个叶子页
  1167 个叶子页 × 每页 81 行 ≈ 94527 行

  当插入到约 94528 行时，叶子页又满了要分裂，
  但根节点的 1166 个 key 也满了，放不下新 key → 根节点也要分裂

  根节点分裂过程：
    1. 根节点的后一半 key（约 583 个）移到新页
    2. 创建一个新的根节点（更高的层级）
    3. 新根存一个分界 key，指向两个子节点

               ┌──────────┐
               │ 新根节点  │     ← 第 3 层诞生
               │ [分界key] │     ← 只有 1 个 key
               └────┬─────┘
              ┌─────┴─────┐
              ▼           ▼
      ┌───────────┐ ┌───────────┐
      │ 中间节点A  │ │ 中间节点B  │  ← 原来的根变成中间节点
      │ [key×583] │ │ [key×583] │
      └─────┬─────┘ └─────┬─────┘
        ...叶子页...     ...叶子页...

  树高从 2 变成 3
  查找：从根走到中间再到叶子，3 次磁盘 IO
```

  树高 = 3 时能存多少数据？
    新根：1 个 key → 2 个子节点
    中间层：每个节点 1166 个 key → 共 2 × 1166 = 2332 个 key → 2333 个叶子页
    叶子层：2333 × 1166 = 约 272 万个叶子页
    总行数：272 万 × 81 ≈ 2.2 亿行

  所以 3 层 B+树能存约 2 亿行数据。
  超过 2 亿行才会变成 4 层。
  千万级数据，树高 2-3 层就够了。
```

  行数              树高    结构
  1 ~ 81            1 层    根 = 叶子（同一页）
  82 ~ 94527        2 层    1 个根 + 几个~1167 个叶子页
  94528 ~ 2.2 亿    3 层    1 个根 + 中间层 + 叶子层
  2.2 亿以上        4 层    极少见到

  规律：
    树高每 +1，查找就多一次磁盘 IO
    千万级数据只需要 2-3 次 IO
    这就是 B+树的核心价值：数据再多，查找也很快

  树是自动长大的，InnoDB 自动处理分裂和合并，不需要手动维护。
```

---

### 5. Schema 是什么

```sql
CREATE TABLE users (
    id         INT PRIMARY KEY AUTO_INCREMENT,  -- 主键，自增
    name       VARCHAR(50) NOT NULL,            -- 必填，最长50字符
    email      VARCHAR(100) UNIQUE,             -- 唯一约束
    age        INT DEFAULT 0,                   -- 默认值 0
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_name (name)                       -- 索引
);
```

- 有哪些字段（id, name, email, age, created_at）
- 每个字段的类型（INT, VARCHAR, DATETIME）
- 约束（主键、非空、唯一、默认值）
- 索引

```sql
DESC users;               -- 查看字段结构
SHOW CREATE TABLE users;  -- 查看完整建表语句（最完整的 Schema）
```

```json
{
  “type”: “object”,
  “properties”: {
    “name”:  { “type”: “string” },
    “age”:   { “type”: “number”, “minimum”: 0 },
    “email”: { “type”: “string”, “format”: “email” }
  },
  “required”: [“name”, “email”]
}
```

- name 是 string 且必填 → 通过
- age 是 number 且 >= 0 → 通过
- email 必须是合法邮箱格式 → 通过
- 缺少必填字段 → 报错

Schema 的核心意思就一句话：**数据长什么样的规则说明**。

它定义了这份数据必须有哪些字段、每个字段是什么类型、哪些必填、有什么约束”。
不管在什么场景下，Schema 干的都是这一件事。

---

#### 1）表结构 Schema

别人说”这张表的 schema”，通常指表的结构定义：

这段 CREATE TABLE 就是 users 表的 Schema。它定义了：

查看已有表的 Schema：

---

#### 2）JSON Schema

JSON Schema 定义 JSON 数据的格式规范，常用于 API 请求校验：

有了这个 Schema，收到 JSON 请求时可以自动校验：

---

### 6. 数据库优化

什么是全表扫描：MySQL 从第一行到最后一行，逐行检查是否符合条件。
有索引时，MySQL 走 B+树直接定位，不用逐行扫。

  为什么 % 在前面就没法用索引？
    B+树的叶子节点按 name 排好序：Alice, Bob, Jack, Tom, 张三, 张伟...
    LIKE '张%' → 从"张"开始往后扫，利用排序，走索引。
    LIKE '%张%' → "张"可能出现在任何位置，排序没用，只能一个个查。

```sql
-- 没有索引：全表扫描
SELECT * FROM users WHERE email = 'test@example.com';
-- 100万行，最坏比较100万次

```sql
-- 不好：LIKE '%张%' 无法使用索引
SELECT * FROM users WHERE name LIKE '%张%';

```sql
-- 不好：OR 可能导致索引失效
SELECT * FROM users WHERE name = '张三' OR age = 20;

```sql
-- 不好：对索引列做函数运算
SELECT * FROM users WHERE YEAR(created_at) = 2026;

```sql
-- 不好：SELECT * 查所有列
SELECT * FROM users WHERE id = 1;

```sql
-- 不好：LIMIT 偏移量大时很慢
SELECT * FROM users LIMIT 100000, 10;

  怎么用：
    第 1 页：SELECT * FROM users LIMIT 10;              → 返回 id 1~10
    第 2 页：SELECT * FROM users WHERE id > 10 LIMIT 10; → 返回 id 11~20
    第 3 页：SELECT * FROM users WHERE id > 20 LIMIT 10; → 返回 id 21~30
    每次记住上一页最后一条的 id，作为下一页的起点。
```

```sql
EXPLAIN SELECT * FROM users WHERE email = 'test@example.com';
```

```
type 列的值，从慢到快：

```
怎么用 EXPLAIN 排查慢查询：

#### 数据库查询怎么优化？

**问题**：数据库查询慢怎么优化？

**回答**：

**1. 使用索引**：

-- 建索引
CREATE INDEX idx_email ON users(email);
-- 有索引：B+树查找，最多比较约20次
```

**2. 避免全表扫描**：

  LIKE '%张%' 的意思：name 中包含"张"这个字，前面可以有任意字符。
  % 在前面 → MySQL 不知道从哪里开始匹配 → 无法走 B+树索引 → 只能全表扫描。
  100万行数据，每行都要检查一遍。

-- 好：LIKE '张%' 可以使用索引
SELECT * FROM users WHERE name LIKE '张%';

  LIKE '张%' 的意思：name 以"张"开头，后面可以有任意字符。
  % 在后面 → MySQL 知道从"张"开始 → 走 B+树索引 → 直接定位到"张"开头的行。
  从 B+树找到"张"的位置，沿叶子链表往后扫到"张"结束的位置，很快。
```

  这条 SQL 的意思：找 name 是"张三"的，或者 age 是 20 的。
  name 有索引（idx_name），age 也有索引（idx_age）。
  但 OR 连接两个条件时，MySQL 可能无法同时高效使用两个索引，
  退化成全表扫描。

-- 好：用 UNION 拆成两条查询
SELECT * FROM users WHERE name = '张三'
UNION
SELECT * FROM users WHERE age = 20;

  UNION 把一条查询拆成两条：
    第一条：WHERE name = '张三' → 走 idx_name 索引
    第二条：WHERE age = 20     → 走 idx_age 索引
  两条查询各自用各自的索引，最后合并结果，去掉重复行。
  比一条 OR 查询走全表扫描快得多。
```

  YEAR(created_at) 对每一行的 created_at 取年份。
  MySQL 需要先算出每一行的 YEAR()，再比较 → 索引用不上 → 全表扫描。

-- 好：改成范围查询
SELECT * FROM users WHERE created_at >= '2026-01-01'
                      AND created_at < '2027-01-01';

  直接比较 created_at 的值，不套函数 → 走索引 → B+树范围扫描。
```

**3. 只查需要的列**：

-- 好：只查需要的列
SELECT id, name FROM users WHERE id = 1;
```

**4. 分页查询**：

  LIMIT 100000, 10 的意思：跳过前 100000 行，取 10 行。
  MySQL 的做法：从第 1 行开始，逐行扫描，扫过 100000 行后，取第 100001~100010 行。
  偏移量越大，要扫的行越多，越慢。
  LIMIT 1000000, 10 → 要扫 100 万行才能取到 10 行，非常慢。

-- 好：使用上次的最大 ID
SELECT * FROM users WHERE id > 100000 LIMIT 10;

  这条的意思：从 id 大于 100000 的位置开始，取 10 行。
  MySQL 的做法：走主键 B+树，直接定位到 id=100000 的位置，往后取 10 行。
  不管翻到第几页，都是从 B+树直接定位，速度稳定。

**5. 使用 EXPLAIN 分析查询**：

EXPLAIN 是 MySQL 提供的"查看查询计划"工具。
在 SELECT 前面加 EXPLAIN，MySQL 不会真的执行查询，而是告诉你它打算怎么查。

执行后返回一张表，最重要的是 type 列，它告诉你 MySQL 用了什么查找方式：

  ALL（全表扫描）：
    从第一行扫到最后一行，最慢。
    出现原因：没有索引、索引失效、WHERE 条件用不上索引。
    100万行 → 扫 100万行。

  index（索引全扫描）：
    扫描整棵 B+树的叶子节点（但不回表）。
    比 ALL 快一点，但还是要扫很多数据。
    出现原因：查询的列都在索引里，但没有 WHERE 条件限制范围。

  range（范围扫描）：
    走 B+树索引，只扫描一个范围内的叶子节点。
    出现原因：WHERE 用了 >、<、BETWEEN、IN、LIKE '张%' 等范围条件。
    比 index 快很多，只扫部分数据。

  ref（索引查找）：
    走 B+树索引，定位到具体的值。
    出现原因：WHERE 用 = 比较非唯一索引的值。
    例：WHERE name = '张三'（name 有普通索引）

  eq_ref（唯一索引查找）：
    走唯一索引或主键索引，最多返回一行。
    出现原因：JOIN 时用主键或唯一索引关联。
    比 ref 更快，因为结果唯一。

  const（常量查找）：
    通过主键或唯一索引直接找到一行，最快。
    出现原因：WHERE 用 = 比较主键或唯一索引。
    例：WHERE id = 1 → B+树直接定位，1 次 IO。
    MySQL 优化器会把这种查询当作常量处理。
```

  1. 在慢查询前加 EXPLAIN，执行
  2. 看 type 列：如果是 ALL，说明在全表扫描，需要优化
  3. 看 key 列：显示实际用了哪个索引，NULL 表示没用索引
  4. 看 rows 列：MySQL 估计要扫描多少行，数字越大越慢
  5. 根据结果决定：加索引、改 WHERE 条件、或拆分查询

  目标：让 type 尽量是 const、eq_ref、ref，避免 ALL。
```

---

## Redis、缓存与分布式锁

  为什么不用 C 语言自带的字符串？
    C 字符串以 \0 结尾，不能存二进制数据（如图片）。
    SDS 用 len 记录长度，可以存任意二进制数据。
    SDS 可以预分配空间，避免频繁申请内存。

    为什么同时用两个结构？
      skiplist：支持按分数排序和范围查询（ZRANGEBYSCORE）
      dict：支持 O(1) 查找某个 member 的分数（ZSCORE）
      两者配合，兼顾排序和查找。

```
Redis 内部用一张巨大的哈希表存所有 key-value。

```
客户端发送：SET name "hello"

```
最基本的类型，一个 key 对应一个字符串或数字。

```
一个 key 对应一个有序列表，可以从左（头部）或右（尾部）操作。

```
常用操作和底层发生了什么：

```
应用：
  消息队列：LPUSH 发消息，BRPOP 阻塞取消息
  最新消息列表：LPUSH 新消息，LTRIM 只保留最近 100 条
  用户动态：LPUSH user:1:feed "新动态内容"
```

```
一个 key 对应一个小型哈希表，可以存多个 field-value 对。
适合存储对象（如用户信息、商品信息）。

```
常用操作和底层发生了什么：

```
应用：
  存储用户信息：HSET user:1 name "Tom" age 25 email "tom@test.com"
  存储商品信息：HSET product:1001 title "手机" price 2999 stock 500
  购物车：HSET cart:user1 product:1001 2（商品1001买2件）
```

```
一个 key 对应一个无序集合，元素不重复。
支持交集、差集、并集操作。

```
常用操作和底层发生了什么：

```
应用：
  去重：SADD ip:today "192.168.1.1"（统计今天有哪些IP访问）
  共同好友：SINTER user:1:friends user:2:friends
  标签：SADD article:1:tags "Redis" "数据库" "缓存"
```

```
每个元素带一个分数（score），按分数从小到大排序。
既支持按排名访问（第几名），也支持按分数范围查询。

```
常用操作和底层发生了什么：

```
应用：
  排行榜：ZADD game:score 1500 "player1"，ZREVRANGE 取 Top N
  延迟队列：score 存执行时间戳，ZRANGEBYSCORE 取到期的任务
  带权重的消息队列：score 存优先级，ZPOPMIN 取优先级最高的
```

**1. Redis 怎么存数据 — 哈希表**

哈希表的原理（和字典一样）：

  key "name"  → 哈希计算 → 得到一个数字（比如 37）
  → 去第 37 个格子里找 → 找到 value "hello"

  ┌──────────────────────────────────────┐
  │  哈希表（数组）                        │
  │  ┌───┬───┬───┬───┬───┬───┬───┬───┐  │
  │  │ 0 │ 1 │ 2 │...│37 │...│n-1│ n │  │
  │  └───┴───┴───┴───┴─┬─┴───┴───┴───┘  │
  │                     │                 │
  │                     ▼                 │
  │              [name → hello]           │
  │              [age → 25]（同一格有多个，│
  │               用链表串起来）           │
  └──────────────────────────────────────┘

  查找过程：算哈希 → 定位格子 → 在格子里找具体 key
  时间复杂度：O(1)，不管有多少数据，速度几乎一样。
```

---

**2. 执行 SET name "hello" 时发生了什么**

Redis 收到命令后的处理过程：

  第 1 步：解析命令
    从网络数据中提取出三个词：SET、name、hello
    知道这是一个 SET 命令，key 是 "name"，value 是 "hello"

  第 2 步：计算哈希
    对 "name" 算哈希值 → 得到一个数字（比如 37）

  第 3 步：写入哈希表
    在第 37 个格子里存入 [name → hello]
    如果这个 key 已存在，覆盖旧值

  第 4 步：返回 OK
    通过网络发回 "OK" 给客户端

整个过程：解析 → 算哈希 → 写内存 → 返回
没有磁盘操作，全是内存读写，所以极快（微秒级）。
```

**3. Redis 支持哪些数据类型(底层存储详解)**

Redis 不只是简单的 key-value 字符串，支持 5 种基本类型。
每种类型在底层有不同的存储结构，决定了它的读写性能。

---

**5.1 String（字符串）**

底层存储结构：SDS（Simple Dynamic String，简单动态字符串）

  SDS 是 Redis 自己实现的字符串，结构是一块连续内存：

  ┌──────┬──────┬──────┬──────────────────┐
  │ len  │ alloc│flags │ buf（实际内容）    │
  │ 5    │ 10   │ 1    │ hello\0          │
  └──────┴──────┴──────┴──────────────────┘
  │← 头部 →│← 实际数据 →│

  len：当前字符串长度（5）
  alloc：分配的总空间（10，预留了空间给未来扩展）
  buf：真正存内容的地方

  如果 value 是一个小整数（如 0~9999），Redis 不创建 SDS，
  而是直接用一个 int 变量存，省掉 SDS 的内存开销。
```

---

**5.2 List（列表）**

底层存储结构：

  数据量小时（元素少且每个元素小）：ziplist（压缩列表）
  数据量大时：quicklist（快速列表，多个 ziplist 用双向链表串起来）

  ziplist 是什么？
    一块连续内存，所有元素紧凑排列，没有指针。

    ┌────────┬────────┬────────┬────────┬────────┐
    │ zlbytes│ zltail │ zllen  │ entry1 │ entry2 │ ... │ zlend │
    └────────┴────────┴────────┴────────┴────────┘

    每个 entry 的结构：
    ┌─────────────┬───────────┬───────────┐
    │ prevlen     │ encoding  │ data      │
    │ 前一个entry │ 数据类型   │ 实际内容  │
    │ 的长度      │ 和长度    │           │
    └─────────────┴───────────┴───────────┘

    优点：紧凑，省内存，CPU 缓存友好（连续内存）。
    缺点：修改中间的元素时，后面所有元素都要移动。

  quicklist 是什么？
    当列表变大时，Redis 把数据分成多个 ziplist，
    然后用双向链表把这些 ziplist 串起来。

    ┌──────────┐    ┌──────────┐    ┌──────────┐
    │ ziplist1 │ ←→ │ ziplist2 │ ←→ │ ziplist3 │
    │ [a,b,c]  │    │ [d,e,f]  │    │ [g,h,i]  │
    └──────────┘    └──────────┘    └──────────┘

    优点：每个小 ziplist 内部紧凑，链表之间插入删除快。
```

  LPUSH mylist "a" "b"
    → 在列表左边插入 "a" 和 "b"
    → 如果是 ziplist：在开头插入，后面的 entry 全部后移
    → 如果是 quicklist：在第一个 ziplist 的开头插入

  RPUSH mylist "c"
    → 在列表右边插入 "c"
    → 在最后一个 ziplist 的末尾追加

  LPOP mylist
    → 从左边弹出一个元素
    → 从第一个 ziplist 的开头取出，后面的 entry 前移

  LRANGE mylist 0 10
    → 从左到右取 11 个元素
    → 从第一个 ziplist 开始遍历，不够再走链表到下一个 ziplist

  LINDEX mylist 5
    → 取第 6 个元素（从 0 开始计数）
    → 从第一个 ziplist 开始数，跨 ziplist 就沿着链表走
    → 时间复杂度 O(n)，因为要逐个数
```

---

**5.3 Hash（哈希）**

底层存储结构：

  数据量小时：ziplist（压缩列表）
    field1, value1, field2, value2 紧凑排列在一块连续内存里。
    省内存，但查找要遍历。

  数据量大时：dict（哈希表）
    和 Redis 全局哈希表一样的结构。

    ┌──────────────────────────────────────┐
    │  哈希表（数组）                        │
    │  ┌───┬───┬───┬───┬───┐              │
    │  │ 0 │ 1 │ 2 │ 3 │ 4 │              │
    │  └───┴─┬─┴───┴─┬─┴───┘              │
    │        │       │                     │
    │        ▼       ▼                     │
    │  [name→Tom] [age→25]                │
    │        │                             │
    │        ▼                             │
    │  [email→tom@test.com]（哈希冲突，链表）│
    └──────────────────────────────────────┘

  Redis 会根据数据量自动选择用 ziplist 还是 dict：
    默认：field 数量 < 128 且每个 value < 64 字节 → 用 ziplist
    超过阈值 → 自动转换为 dict
```

  HSET user:1 name "Tom"
    → 找到 key "user:1" 的哈希表
    → 如果是 ziplist：在末尾追加 field "name" 和 value "Tom"
    → 如果是 dict：对 "name" 算哈希 → 定位槽位 → 存入

  HGET user:1 name
    → 找到 key "user:1" 的哈希表
    → 如果是 ziplist：遍历找 field "name"，返回后面的 value
    → 如果是 dict：对 "name" 算哈希 → O(1) 直接定位

  HGETALL user:1
    → 返回所有 field-value 对
    → 如果是 ziplist：顺序遍历整个 ziplist
    → 如果是 dict：遍历哈希表所有槽位

  HINCRBY user:1 age 1
    → 找到 field "age" 的 value → 加 1 → 写回
```

---

**5.4 Set（集合）**

底层存储结构：

  全是整数且数量少时：intset（整数集合）
    一块连续内存，存排序好的整数数组。

    ┌────────┬────────┬──────────────────────────┐
    │ encoding│ length │ contents（排序好的整数）   │
    │ int32  │ 3      │ [10, 20, 30]             │
    └────────┴────────┴──────────────────────────┘

    encoding：每个整数占多少字节（16/32/64位）
    如果插入一个更大的整数，Redis 会自动升级 encoding（如 32→64）

  有非整数元素或数量大时：dict（哈希表）
    和 Hash 类型用的 dict 一样，但只存 field，不存 value。
    每个元素就是哈希表里的一个 key。
```

  SADD myset "a" "b" "c"
    → 如果是 intset：在排序数组中插入（后面的元素后移）
    → 如果是 dict：对每个元素算哈希 → 存入哈希表

  SISMEMBER myset "b"
    → 检查 "b" 是否在集合中
    → 如果是 intset：二分查找（因为数组有序）→ O(log n)
    → 如果是 dict：算哈希 → O(1) 直接定位

  SREM myset "b"
    → 从集合中删除 "b"
    → 如果是 intset：找到后，后面的元素前移
    → 如果是 dict：从哈希表中删除

  SINTER set1 set2
    → 求两个集合的交集（共同元素）
    → 遍历较小的集合，对每个元素检查是否在另一个集合中

  SUNION set1 set2    → 并集（所有元素合并）
  SDIFF set1 set2     → 差集（在 set1 但不在 set2 的元素）
```

---

**5.5 Sorted Set（有序集合）**

底层存储结构：

  数据量小时：ziplist
    按分数排序，每个 entry 存 [member, score] 紧凑排列。

  数据量大时：skiplist（跳表）+ dict（哈希表）

    skiplist（跳表）是什么？
      多层链表，每一层都是有序的。

      最底层（第0层）：所有元素，用链表串起来
        [Tom:100] ↔ [Jack:200] ↔ [Alice:300] ↔ [Bob:400]

      第1层：每隔几个元素抽一个出来，建立"快车道"
        [Tom:100] ↔ [Alice:300]

      第2层：更稀疏
        [Tom:100]

      查找过程（找 Alice:300）：
        从第2层开始：Tom(100) < 300 → 往右走 → 到头了 → 下一层
        第1层：Tom(100) < 300 → 往右 → Alice(300) = 300 → 找到！
        不需要从头一个个比较，类似二分查找。

      跳表的结构图：
        Level 2: [Tom] ──────────────────────────→ NULL
        Level 1: [Tom] ──────────→ [Alice] ──────→ NULL
        Level 0: [Tom] ↔ [Jack] ↔ [Alice] ↔ [Bob] ↔ NULL

    dict 存什么？
      每个 member → score 的映射。
      ZSCORE 命令直接查 dict，O(1)。
```

  ZADD leaderboard 100 "Tom" 90 "Jack"
    → 在 skiplist 中按分数插入 Tom(100) 和 Jack(90)
    → 在 dict 中存 [Tom→100] 和 [Jack→90]

  ZSCORE leaderboard "Tom"
    → 直接查 dict：Tom → 100，O(1)

  ZRANGE leaderboard 0 2
    → 按排名从 skiplist 底层从左到右取 3 个元素
    → [Jack, Tom, Alice]（按分数从小到大）

  ZREVRANGE leaderboard 0 2
    → 按排名从右到左取（分数从大到小）
    → [Bob, Alice, Tom]

  ZRANGEBYSCORE leaderboard 80 100
    → 在 skiplist 中找分数在 80~100 之间的所有元素
    → 从底层找到第一个 >= 80 的位置，沿链表往后扫到 > 100 停止

  ZRANK leaderboard "Tom"
    → Tom 排第几名（从 0 开始）
    → 在 skiplist 底层数 Tom 的位置

  ZINCRBY leaderboard 5 "Tom"
    → Tom 的分数 +5
    → 先查 dict 拿到旧分数 → +5 → 更新 dict 和 skiplist 中的分数
```

## SQLite、sqlite-vec、Milvus 与记忆系统

### SQLite 是什么

```
MySQL 的架构（客户端/服务端）：
  你的程序 → TCP连接 → MySQL服务端进程 → 磁盘
  需要单独安装、启动、管理 MySQL 服务

```

```
你写的 SQL：SELECT name FROM users WHERE id = 1;

```
和 MySQL 的内部架构对比：

```
SQLite 的优势：
  1. 零配置：不需要安装、不需要启动服务、不需要配置
  2. 单文件：整个数据库就是一个文件，方便备份和传输
  3. 轻量：整个引擎只有几百 KB，嵌入到手机 App、浏览器、IoT 设备
  4. 可靠：代码经过 100% 分支测试，用在飞机、手机、浏览器里

```
SQLite 用在哪里：
  手机 App：Android 和 iOS 的本地数据库
  浏览器：Chrome、Firefox 的本地存储
  嵌入式设备：IoT、路由器、智能家居
  桌面软件：Electron 应用的本地数据
  测试：单元测试用 SQLite 代替 MySQL
  AI 记忆系统：个人助手的本地记忆存储（如本项目）
```

SQLite 是一个**嵌入式数据库**——不需要安装服务、不需要启动进程，
数据库引擎直接编译到你的程序里，通过函数调用读写磁盘。

整个数据库就是一个 .db 文件，拷贝这个文件就拷贝了整个数据库。

SQLite 的架构（嵌入式）：
  你的程序 → 函数调用 → SQLite引擎（在程序内部） → 磁盘
  不需要服务进程，引擎就是你程序的一部分

#### SQLite 的内部架构（7 层）

一条 SQL 从输入到执行，经过 7 层处理：

第 1 层：Tokenizer（分词器）
  把 SQL 字符串拆成一个个 token（词元）
  "SELECT" "name" "FROM" "users" "WHERE" "id" "=" "1"

第 2 层：Parser（解析器）
  把 token 组成语法树（AST），检查语法是否正确
  如果 SQL 写错了（如 SELCT），在这层报错

第 3 层：Code Generator（代码生成器）
  把语法树翻译成字节码（一种虚拟机指令）
  类似编译器把高级语言翻译成汇编指令

第 4 层：VDBE（虚拟数据库引擎）
  执行字节码的核心引擎
  逐条执行指令：打开表 → 定位行 → 读取字段 → 返回结果
  SQLite 的"CPU"，所有查询都在这里执行

第 5 层：B-Tree
  数据以 B 树形式存储，每张表和每个索引各一棵 B 树
  VDBE 通过 B-Tree 接口读写数据
  SQLite 用的是 B 树（不是 B+ 树），叶子节点也存数据

第 6 层：Pager（页面管理器）
  管理内存页和磁盘页的换入换出
  负责事务（ACID）和 WAL 日志
  读数据时：如果页在内存直接返回，不在就从磁盘加载
  写数据时：先写 WAL 日志，再异步写回数据文件

第 7 层：OS 接口
  调用操作系统的文件读写函数（open, read, write）
  不同操作系统（Windows/Linux/Mac）有不同的实现
```

  MySQL：客户端 → 网络协议 → 解析器 → 优化器 → 执行器 → 存储引擎(InnoDB)
  SQLite：SQL → Tokenizer → Parser → Code Gen → VDBE → B-Tree → Pager → OS

  MySQL 的存储引擎是可插拔的（InnoDB、MyISAM 等）
  SQLite 的存储引擎是内置的（B-Tree + Pager）
  MySQL 有查询优化器（选择最优执行计划），SQLite 也有但更简单
```

---

SQLite 的局限：
  1. 写入并发差：同一时刻只能有一个写操作（读可以并发）
  2. 没有网络访问：只能本机访问，不能远程连接
  3. 没有用户权限管理：谁拿到文件谁就能读写
  4. 不适合高并发 Web 场景：多个请求同时写会排队等待
```

## Milvus

```
向量索引的本质思路：不要和每个向量都比较，先缩小范围。

就像查字典：
  暴力搜索 = 从第 1 页翻到最后一页，逐个看
  有索引   = 先翻到对应字母的分区，再在分区内找

向量索引做的事：
  1. 建索引时：把相似的向量放在一起（聚类、建图）
  2. 查询时：先定位到”大概率包含答案”的区域，只搜这个区域

  结果：搜 1000 个向量而不是 100 万个，快 100 倍。
  代价：可能漏掉一些真正最近的向量（近似搜索，不是精确搜索）。

这就是为什么叫 ANN（Approximate Nearest Neighbor，近似最近邻）：
  不保证找到绝对最近的，但找到的”非常接近”。
  实际应用中，召回率通常 > 95%，几乎和精确搜索一样好。
```

---

## 核心数据结构

### 1. Collection（相当于"表"）

```text
Collection：bearing_fault_documents
字段：
  id          INT64           主键
  content     VARCHAR         原始文本
  device_type VARCHAR         设备类型
  fault_type  VARCHAR         故障类型
  vector      FLOAT_VECTOR    768 维向量
```

### 2. Entity（一行记录）

一个 Entity = 主键 + 标量字段值 + 向量字段值。

### 3. Field（列）

- **标量字段**：INT64、VARCHAR、BOOL、FLOAT、JSON、ARRAY
- **向量字段**：
  - `FLOAT_VECTOR`：float32 数组，768 / 1024 / 1536 维
  - `BINARY_VECTOR`：bit 数组，节省空间但距离度量只能用 Hamming/Jaccard
  - `SPARSE_FLOAT_VECTOR`：稀疏表示，类似 `{12: 0.82, 405: 0.31}`，专门给关键词/BM25 用

### 4. Schema（字段定义和约束）

```text
768 维向量是 768 维空间的一个点
两个点要算距离，必须维度相同
1024 维和 768 维向量在数学上根本不能直接比较
```

**向量字段必须指定维度 dim**。这不是 Milvus 矫情，是**数学上必须的**：

所以 Schema 里写 `dim=768` 后，插入时如果给的是 512 维或 1024 维向量，请求会被**直接拒绝**。这不是"配置项太死"，是"维度不同连距离都算不出来"。

### 5. Partition / Shard / Segment

```text
新数据写入进来
   ↓
进入某个 Shard 的 Growing Segment（不断增长）
   ↓
达到容量阈值（默认 512MB 或类似）
   ↓
被封存（seal），成为 Sealed Segment
   ↓
Data Node 给它建正式索引
   ↓
Query Node 加载它，用于后续检索
```

- **Growing**：可继续写入、可以参与查询（用临时索引）、不能建正式索引
- **Sealed**：不可再写、可以建正式 HNSW 索引、加载到 Query Node 后查询更快

| 概念                | 解决什么问题     | 本质是什么                                           |
| ------------------- | ---------------- | ---------------------------------------------------- |
| **Partition** | 减少查询扫描范围 | 一组 Segment 的逻辑集合，查询时只扫指定 Partition    |
| **Shard**     | 分散写入并发     | 写入路径上的并行度单位，一个 Shard 绑定一个 VChannel |
| **Segment**   | 实际承载数据     | 物理上真实存储一批 Entity 的容器                     |

这三个概念**容易被混在一起说**，但本质完全不同：

**一个 Segment 的生命周期**：

**关键点**：一个 Collection 下可以有多个 Partition，一个 Partition 下有多个 Segment，一个 Segment 容纳若干 Entity。

**Growing vs Sealed**：

### 6. Dynamic Field(动态字段)

## Dynamic Field：像把额外信息塞进一个 JSON 里

```text
Schema：id, vector
插入时额外：{source: "manual.pdf", page: 12}
```

- 显式字段可以建标量索引，过滤快
- Dynamic Field 存的是 JSON-like 结构，标量过滤要走全字段扫描

| 类型 | 适合做什么 | 查询过滤性能 |
| ---- | ---------- | ------------ |

| 显式 Schema 字段 | 高频过滤、排序、分组、权限控制 | 快，可以建索引 |
| ---------------- | ------------------------------ | -------------- |

| Dynamic Field | 临时字段、低频字段、不确定字段 | 慢，可能需要扫描 |
| ------------- | ------------------------------ | ---------------- |

假设你的 Schema 只有：

<pre class="overflow-visible! px-0!" data-start="507" data-end="528"><div class="relative w-full mt-4 mb-1"><div class=""><div class="contents"><div class="relative"><div class="h-full min-h-0 min-w-0"><div class="h-full min-h-0 min-w-0"><div class="border border-token-border-light border-radius-3xl corner-superellipse/1.1 rounded-3xl"><div class="h-full w-full border-radius-3xl bg-token-bg-elevated-secondary corner-superellipse/1.1 overflow-clip rounded-3xl lxnfua_clipPathFallback"><div class="pointer-events-none absolute end-1.5 top-1 z-2 md:end-2 md:top-1"></div><div class="relative"><div class="pe-11 pt-3"><div class="relative z-0 flex max-w-full"><div id="code-block-viewer" dir="ltr" class="q9tKkq_viewer cm-editor z-10 light:cm-light dark:cm-light flex h-full w-full flex-col items-stretch ͼd ͼr"><div class="cm-scroller"><pre class="cm-content q9tKkq_readonly m-0"><code><span>id</span><br/><span>vector</span></code></pre></div></div></div></div></div></div></div></div></div><div class=""><div class=""></div></div></div></div></div></div></pre>

但是启用了 Dynamic Field，插入时可以额外传：

<pre class="overflow-visible! px-0!" data-start="561" data-end="645"><div class="relative w-full mt-4 mb-1"><div class=""><div class="contents"><div class="border border-token-border-light border-radius-3xl corner-superellipse/1.1 rounded-3xl"><div class="relative h-full w-full border-radius-3xl bg-token-bg-elevated-secondary corner-superellipse/1.1 overflow-clip rounded-3xl lxnfua_clipPathFallback"><div class="pointer-events-none absolute inset-x-4 top-12 bottom-4"><div class="pointer-events-none sticky z-40 shrink-0 z-1!"><div class="sticky bg-token-border-light"></div></div></div><div class="relative"><div class="h-full min-h-0 min-w-0"><div class="h-full min-h-0 min-w-0"><div class=""><div class="relative"><div class=""><div class="relative z-0 flex max-w-full"><div id="code-block-viewer" dir="ltr" class="q9tKkq_viewer cm-editor z-10 light:cm-light dark:cm-light flex h-full w-full flex-col items-stretch ͼd ͼr"><div class="cm-scroller"><pre class="cm-content q9tKkq_readonly m-0"><code><span>{</span><br/><span>  "id": </span><span class="ͼj">1</span><span>,</span><br/><span>  "vector": [...],</span><br/><span>  "source": </span><span class="ͼk">"manual.pdf"</span><span>,</span><br/><span>  "page": </span><span class="ͼj">12</span><br/><span>}</span></code></pre></div></div></div></div></div></div></div></div><div class=""><div class=""></div></div></div></div></div></div></div></div></pre>

这些额外字段不会变成真正的独立列，而是被统一塞进一个隐藏的动态字段里，类似：

<pre class="overflow-visible! px-0!" data-start="687" data-end="762"><div class="relative w-full mt-4 mb-1"><div class=""><div class="contents"><div class="border border-token-border-light border-radius-3xl corner-superellipse/1.1 rounded-3xl"><div class="relative h-full w-full border-radius-3xl bg-token-bg-elevated-secondary corner-superellipse/1.1 overflow-clip rounded-3xl lxnfua_clipPathFallback"><div class="pointer-events-none absolute inset-x-4 top-12 bottom-4"><div class="pointer-events-none sticky z-40 shrink-0 z-1!"><div class="sticky bg-token-border-light"></div></div></div><div class="relative"><div class="h-full min-h-0 min-w-0"><div class="h-full min-h-0 min-w-0"><div class=""><div class="relative"><div class=""><div class="relative z-0 flex max-w-full"><div id="code-block-viewer" dir="ltr" class="q9tKkq_viewer cm-editor z-10 light:cm-light dark:cm-light flex h-full w-full flex-col items-stretch ͼd ͼr"><div class="cm-scroller"><pre class="cm-content q9tKkq_readonly m-0"><code><span>{</span><br/><span>  "$meta": {</span><br/><span>    "source": </span><span class="ͼk">"manual.pdf"</span><span>,</span><br/><span>    "page": </span><span class="ͼj">12</span><br/><span>  }</span><br/><span>}</span></code></pre></div></div></div></div></div></div></div></div><div class=""><div class=""></div></div></div></div></div></div></div></div></pre>

这就像 MySQL 里有一列叫 `extra_json`：

<pre class="overflow-visible! px-0!" data-start="795" data-end="849"><div class="relative w-full mt-4 mb-1"><div class=""><div class="contents"><div class="border border-token-border-light border-radius-3xl corner-superellipse/1.1 rounded-3xl"><div class="relative h-full w-full border-radius-3xl bg-token-bg-elevated-secondary corner-superellipse/1.1 overflow-clip rounded-3xl lxnfua_clipPathFallback"><div class="pointer-events-none absolute inset-x-4 top-12 bottom-4"><div class="pointer-events-none sticky z-40 shrink-0 z-1!"><div class="sticky bg-token-border-light"></div></div></div><div class="relative"><div class="h-full min-h-0 min-w-0"><div class="h-full min-h-0 min-w-0"><div class=""><div class="relative"><div class=""><div class="relative z-0 flex max-w-full"><div id="code-block-viewer" dir="ltr" class="q9tKkq_viewer cm-editor z-10 light:cm-light dark:cm-light flex h-full w-full flex-col items-stretch ͼd ͼr"><div class="cm-scroller"><pre class="cm-content q9tKkq_readonly m-0"><code><span>{</span><br/><span>  "source": </span><span class="ͼk">"manual.pdf"</span><span>,</span><br/><span>  "page": </span><span class="ͼj">12</span><br/><span>}</span></code></pre></div></div></div></div></div></div></div></div><div class=""><div class=""></div></div></div></div></div></div></div></div></pre>

你当然也可以查：

<pre class="overflow-visible! px-0!" data-start="861" data-end="895"><div class="relative w-full mt-4 mb-1"><div class=""><div class="contents"><div class="relative"><div class="h-full min-h-0 min-w-0"><div class="h-full min-h-0 min-w-0"><div class="border border-token-border-light border-radius-3xl corner-superellipse/1.1 rounded-3xl"><div class="h-full w-full border-radius-3xl bg-token-bg-elevated-secondary corner-superellipse/1.1 overflow-clip rounded-3xl lxnfua_clipPathFallback"><div class="pointer-events-none absolute end-1.5 top-1 z-2 md:end-2 md:top-1"></div><div class="relative"><div class="pe-11 pt-3"><div class="relative z-0 flex max-w-full"><div id="code-block-viewer" dir="ltr" class="q9tKkq_viewer cm-editor z-10 light:cm-light dark:cm-light flex h-full w-full flex-col items-stretch ͼd ͼr"><div class="cm-scroller"><pre class="cm-content q9tKkq_readonly m-0"><code><span>source == "manual.pdf"</span></code></pre></div></div></div></div></div></div></div></div></div><div class=""><div class=""></div></div></div></div></div></div></pre>

但问题是：它不是正式字段，通常不能像正式字段那样高效建标量索引。

所以数据库要判断每条数据的 `$meta` 里面有没有：

<pre class="overflow-visible! px-0!" data-start="961" data-end="995"><div class="relative w-full mt-4 mb-1"><div class=""><div class="contents"><div class="border border-token-border-light border-radius-3xl corner-superellipse/1.1 rounded-3xl"><div class="relative h-full w-full border-radius-3xl bg-token-bg-elevated-secondary corner-superellipse/1.1 overflow-clip rounded-3xl lxnfua_clipPathFallback"><div class="pointer-events-none absolute inset-x-4 top-12 bottom-4"><div class="pointer-events-none sticky z-40 shrink-0 z-1!"><div class="sticky bg-token-border-light"></div></div></div><div class="relative"><div class="h-full min-h-0 min-w-0"><div class="h-full min-h-0 min-w-0"><div class=""><div class="relative"><div class=""><div class="relative z-0 flex max-w-full"><div id="code-block-viewer" dir="ltr" class="q9tKkq_viewer cm-editor z-10 light:cm-light dark:cm-light flex h-full w-full flex-col items-stretch ͼd ͼr"><div class="cm-scroller"><pre class="cm-content q9tKkq_readonly m-0"><code><span class="ͼk">"source"</span><span>: </span><span class="ͼk">"manual.pdf"</span></code></pre></div></div></div></div></div></div></div></div><div class=""><div class=""></div></div></div></div></div></div></div></div></pre>

这就叫 **全字段扫描** ，也就是要一条一条看，速度会慢很多。

启用后允许插入未预定义字段。

但**生产中需要经常用于过滤的字段应建到 Schema 里**：

## Milvus Distributed

```text
Proxy × N（无状态，可任意扩）
Root Coordinator、Query Coordinator、Data Coordinator（每类一个或几个）
Streaming Node × N
Query Node × N
Data Node × N
共享 etcd（一致）+ 对象存储（MinIO/S3）
```

- 计算节点（Milvus 进程）只负责查询和写入
- 数据放在独立的对象存储（MinIO/S3）里
- 任何计算节点挂掉，新节点拉起后从对象存储加载数据继续工作

| 压力                | 加什么                 |
| ------------------- | ---------------------- |
| 客户端 QPS 高       | 加 Proxy               |
| 实时写入 QPS 高     | 加 Streaming Node      |
| 历史数据搜索 QPS 高 | 加 Query Node          |
| 索引构建慢          | 加 Data Node           |
| 元数据/协调瓶颈     | Coordinator 也能多副本 |

把 Standalone 那个进程里的功能**拆成独立可扩展的组件**：

**和 Standalone 的本质区别**：单个组件可以独立扩。**不是"多跑几个 Standalone"，是把 Standalone 里的功能拆开，按需扩展**。

**Milvus 从设计第一天起就是"存算分离"**：

Standalone 即便只跑在一台机器上，也**故意用三容器**维持这个架构——为的是将来从 Standalone 迁到 Distributed 时，**业务代码、运维脚本都不用改**。

### milvus-standalone

```text
你的 Python/Java 程序
   │
   │ pymilvus 客户端（gRPC 协议）
   ▼
milvus-standalone:19530
   │ 处理：建 Collection、insert、search、query
   │ 内部：Proxy 校验 + Coordinator 调度 + 各种 Node 干活（但都在一个进程里）
```

- 接收客户端请求
- 校验 Schema、字段、向量维度
- 把数据组织成 Segment
- 构建 HNSW 索引
- 执行向量距离计算（L2/COSINE/...）
- 标量过滤、TopK 归并、返回结果

**真正提供向量数据库功能的进程**，对外端口 `19530`。

它负责：

### milvus-etcd

```text
/milvus/collection/bearing_fault/schema     → 字段定义
/milvus/collection/bearing_fault/segments   → 当前所有 segment 状态
/milvus/coord/leader                        → 当前哪个 Coordinator 是主
/milvus/node/query-node-3/heartbeat         → Query Node 3 的心跳
/milvus/checkpoint/insert_log/1001          → 消费到了哪个位置
```

- 不保存主要向量数据（向量在 MinIO 里）
- 不保存大型索引文件
- 不算相似度

**独立的分布式键值存储服务**（和 Milvus 平级，不是 Milvus 的子模块）。

**存什么**（"元数据"——描述 Milvus 自身状态的数据）：

**etcd 解决的是"集群中所有节点对元数据达成一致"**。Milvus 自己如果用本地变量存这些信息，**多节点之间会不一致**。etcd 用 Raft 协议保证多副本强一致。

**etcd 不负责**：

### milvus-minio

```text
milvus-bucket/
├── insert_log/1001/0/100/100200          ← 向量原始数据文件
├── insert_log/1001/0/100/100201
├── index_files/1001/0/100/hnsw_index    ← HNSW 索引文件
├── index_files/1001/0/100/field_stats
├── stats/1001/0/100/                     ← 统计信息
└── ...
```

- 多个 Milvus 节点要共享同一份数据
- 数据可能几百 GB 到几 TB
- 节点故障时新节点要能快速接管——从共享存储拉数据比从别的机器 rsync 快

- 不懂 Collection/向量是什么
- 不构建索引
- 不算相似度
- 它只懂"上传、下载、删除一个对象文件"

**对象存储服务**，提供 S3 兼容的 HTTP API。

**存什么**（"对象文件"——大块二进制）：

**Milvus 为什么不直接用本地文件**：

**MinIO 不负责**：

## **为什么 Milvus 要拆这么多组件？**

| 功能           | 负载特征               | 适合的扩缩方式              |
| -------------- | ---------------------- | --------------------------- |
| 客户端请求接入 | CPU 密集，IO 密集      | 横向加 Proxy                |
| 写入吞吐       | 顺序写，要求低延迟     | 横向加 Streaming Node       |
| 历史数据查询   | 计算密集，内存密集     | 横向加 Query Node           |
| 索引构建       | CPU + 内存密集，批处理 | 横向加 Data Node            |
| 集群调度       | 元数据读写，要求强一致 | Coordinator 通常只有 1 个主 |

Standalone 里所有功能在一个进程里跑得好好的，为什么 Distributed 要拆？

**答案是：每种功能的负载特征不同，混在一起没法独立扩**。**每一类组件都是独立运行的服务进程** 。

**Standalone 把这些挤在一个进程里，扩谁都得扩整个进程**。Distributed 拆开后，**可以精准加瓶颈那一类**。

### 各组件职责

| 组件                     | 关键问题                       | 职责                                                                                                |
| ------------------------ | ------------------------------ | --------------------------------------------------------------------------------------------------- |
| **Proxy**          | 谁来收客户端请求？             | 无状态接入层，做请求校验、路由、结果归并。**和客户端直连的那层**                              |
| **Coordinator**    | 谁来决定数据放哪、任务派给谁？ | 集群大脑。管理元数据、负载均衡、DDL 调度、任务分配。**任一时刻只有 1 个主**（通过 etcd 选举） |
| **Streaming Node** | 刚写入的实时数据谁来管？       | 基于 WAL 处理写入、管 Growing Segment、对增量数据提供查询                                           |
| **Query Node**     | 历史数据谁负责查？             | 加载 Sealed Segment + 索引到内存，对历史数据执行向量搜索                                            |
| **Data Node**      | 后台脏活累活谁干？             | Compaction、索引构建、批量导入等离线任务                                                            |

### 插入数据流程（最完整版）

```text
1. 客户端发 insert 请求
   ↓
2. Proxy 校验（Collection 存在？字段合法？维度匹配？）
   ↓
3. Proxy 按 shard 路由规则，把请求发给某个 Streaming Node
   （为什么按 shard？因为一个 Collection 的数据被分到多个 Shard
    写到不同 Shard 才能并行化）
   ↓
4. Streaming Node 立刻把操作写进 WAL
   （关键：WAL 写入成功后才向 Proxy 返回成功
    这保证崩溃后能重放）
   ↓
5. 数据进入对应 Shard 的 Growing Segment
   ↓
6. Growing Segment 立即可查（Milvus 给它建临时小索引）
   ↓
7. 后台异步：Growing 达到容量阈值 → seal → 上传 MinIO → Data Node 建正式 HNSW
   ↓
8. Query Node 加载 Sealed + 索引 → 走 HNSW 路径服务后续查询
```

### 搜索数据流程

```text
1. 客户端发 search 请求
   ↓
2. 负载均衡转到某个 Proxy
   ↓
3. Proxy 解析请求：一致性级别、TopK、过滤条件、查询向量
   ↓
4. Proxy 决定要查哪些 Shard
   （如果业务上能确定只涉及某几个 Shard，可以只查那几个）
   ↓
5. 对每个相关 Shard：
   - Streaming Node 查 Growing Segment
   - Query Node 查 Sealed Segment
   每个节点返回该节点上的局部 TopK
   ↓
6. Proxy 合并所有局部 TopK，按分数重排
   ↓
7. 返回全局 TopK 给客户端
```

- **Growing Segment** 在 Streaming Node 上（最新写入的、还在变的）
- **Sealed Segment** 在 Query Node 上（已封存的、有正式索引的）

**为什么查询要分两路**：

数据可能同时在这两类节点上，**搜全才不漏结果**。

### 时间戳和一致性级别（一致性怎么生效的）

```text
insert A  → t=100
insert B  → t=101
search    → 在 t=100 这个时间点"应该"看到什么？
```

- **Strong**：等到 t=当前最新 都能被搜到才返回
- **Bounded**：可以容忍最多 N 秒的延迟（默认几秒）
- **Session**：同会话内自己的写立刻能读到
- **Eventually**：不管几秒，能查到就行

每个 insert 操作都会获得一个**单调递增的时间戳 TSO**（Timestamp Oracle，由 Coordinator 分配）。

一致性级别决定 search 看到哪个时间点的数据：

**这种"时间戳+一致性级别"机制让 Milvus 不需要"全局读锁"就能提供不同强度的一致性**。

**节点结构**

### 1. HNSW：当前默认推荐的图索引

```text
节点关系示意（每个节点保留 3 个最近邻）：

```text
1. 从某个入口节点出发（通常是某个固定起点）
2. 看当前节点的邻居里有没有比当前更接近 q 的
3. 有：跳到那个更近的节点
4. 没有：当前节点就是这一跳的局部最优
5. 重复，直到找不到更近的邻居
```

```text
Layer 2：稀疏图
A ----------- H ----------- M
 \                         /
  ----------- K ----------

```text
1. 从最高层入口节点出发
2. 贪心走到这一层的局部最优
3. 下降到下一层，从该局部最优开始继续贪心
4. 重复直到第 0 层
5. 在第 0 层收集 ef 个最近邻作为候选，返回 TopK
```

```text
高层节点少、跨度大 → 用来"快速定位大致区域"
低层节点密、跨度小 → 用来"在区域内精细搜索"
```

```
HNSW 的核心是一个多层图结构，类似 Redis 的跳表，但不是链表而是图。

```
向量逐个插入，每个新向量决定自己出现在哪些层：

```
查询向量 Q = [0.13, -0.34, 0.79, ...]，找最近的 5 个：

```
“小世界”的意思：图中任意两个节点，通过很少的几步就能到达。

```
两个关键参数：

```
优点：
  1. 查询极快：百万级数据，查询通常 < 1ms
  2. 召回率高：efSearch 足够大时，召回率 > 99%
  3. 支持动态插入：新向量可以直接加入图中

HNSW = Hierarchical Navigable Small World，**分层可导航小世界图**。

它基于一个朴素思想：**"和查询向量接近的向量，大概率和接近它的向量也接近"**。也就是说，向量之间存在局部聚类性，可以构建一张"邻居图"。

**类比**：它和跳表（Skip List）非常像。跳表是给链表加多层索引加速查找，HNSW 是给近邻图加多层加速搜索。

#### 1.1 NSW 图先讲清楚

**NSW（Navigable Small World）** 是 HNSW 的底层。先理解 NSW：

**数据结构**：每个向量是一个节点，**每个节点和它的若干个近邻连边**。

v_1  ── v_5
│      │
v_3  ── v_8 ── v_2
       │
       v_6
```

**搜索过程**（贪心）：

**问题**：单层 NSW 会陷入局部最优。从入口节点出发，如果方向一开始就不对，可能越走越远。

#### 1.2 HNSW 的分层结构

HNSW 给 NSW 加了**多层**：HNSW 的每一层都是一张图 graph；多层叠起来，形成一个分层图结构。

Layer 1：较稠密图
A ---- C ---- H ---- K ---- M
|      |      |      |      |
B ---- D ---- G ---- J ---- L

Layer 0：最稠密图，包含所有向量
A - B - C - D - E - F - G - H - I - J - K - L - M ...
并且每个节点还会连接多个近邻，不是一条线
```

**层数**：每个节点有一个随机最大层数 `level`，按指数衰减概率生成（很像跳表）。Milvus 中通过 `M` 和 `efConstruction` 等参数影响。

**搜索过程**（自顶向下）：

**为什么这样设计**：

这和跳表完全是一个思路：**用稀疏层做粗筛，用稠密层做精排**。

  每个向量是一个节点，每个节点可能出现在多层中。
  层级越高，节点越稀疏；层级越低，节点越密集。

  
```

**建图过程**

4. 插入完成
  第 1 步：随机决定层级
    用一个概率分布决定新节点的最高层级。
    越高层概率越低（类似抛硬币，每往上一层概率减半）。
    大多数节点只在第 0 层，少数在第 1 层，极少数在最高层。

    例：插入 10000 个向量
      第 0 层：10000 个节点（全部）
      第 1 层：约 2000 个节点
      第 2 层：约 400 个节点
      第 3 层：约 80 个节点

  3. 从第 L 层往下，对每一层：
   a. 贪心搜索找到 efConstruction 个最近邻
   b. 在 v_new 和这些邻居之间连边
   c. 如果某个邻居的边数超过 M_max，就裁剪最远的边

    例：插入向量 V，它出现在第 0~2 层
      第 2 层：找到最近的 2 个节点，建立连接
      第 1 层：找到最近的 5 个节点，建立连接
      第 0 层：找到最近的 20 个节点，建立连接

  最终形成的图结构：
    Level 2:  [A] ─── [F]                    （稀疏，远距离跳跃）
    Level 1:  [A] ── [D] ── [E] ── [F]      （较稀疏）
    Level 0:  [A]-[B]-[C]-[D]-[E]-[F]-[G]   （密集，所有节点都在）

    每个节点和同层的若干邻居相连。
    高层节点少但跨度大，低层节点多但跨度小。
```

**查询过程**

  第 1 步：从最高层的入口节点开始
    假设最高层是第 3 层，只有 1 个节点 A。
    计算 Q 和 A 的距离。

  第 2 步：在当前层贪心搜索
    从 A 出发，看 A 的邻居，找比 A 更接近 Q 的节点。
    如果找到更近的，就跳过去，继续看新节点的邻居。
    直到当前层没有比当前位置更近的邻居了。

    Level 3: A → 没有更近的 → 下降到 Level 2

  第 3 步：下降到下一层，继续贪心搜索
    从上一层的落脚点出发，在当前层继续找最近邻居。
    当前层节点更多，搜索更精细。

    Level 2: A → F（F 比 A 更接近 Q）→ 没有更近的 → 下降到 Level 1

  第 4 步：继续下降，直到第 0 层
    Level 1: F → E（更近）→ D（更近）→ 没有更近的 → 下降到 Level 0
    Level 0: D → C（更近）→ B（更近）→ 没有更近的 → 停止

  第 5 步：在第 0 层的搜索结果中，取最近的 5 个返回

  整个过程：从高层到低层，每层都是”贪心跳跃”，
  高层跳得远（快速定位大致区域），低层跳得近（精确定位）。
  类比：先在地图上找到城市 → 再找到街区 → 再找到具体门牌号。
```

**为什么叫”小世界图”**

  虽然有 100 万个节点，但从任意节点出发，
  通常只需要跳 10~20 步就能到达任意其他节点。

  这是因为高层的”长距离连接”让搜索能快速跨越大范围。
  类似社交网络：你和世界上任何人之间，通常只隔 6 个人（六度分隔）。
```

**HNSW 的参数**

  M：每个节点最多连接多少个邻居
    M = 16 → 每个节点最多 16 个邻居
    M 越大 → 图越密 → 搜索越准 → 但内存占用越大、建索引越慢

  efConstruction：建索引时的搜索宽度
    efConstruction = 200 → 建索引时每次搜索最多考察 200 个候选节点
    越大 → 建索引越慢 → 但图的质量越好（邻居选得更准）

  efSearch：查询时的搜索宽度
    efSearch = 64 → 查询时每次搜索最多考察 64 个候选节点
    越大 → 查询越慢 → 但结果越准确
    efSearch 必须 >= k（要返回的结果数）
```

**HNSW 的优缺点**

缺点：
  1. 内存占用大：每个向量 + 每层的邻居指针都要存内存
    100 万 × 768 维 × 4 字节 = 约 3GB（向量本身）
    加上邻居指针，总内存约 4~6GB
  2. 建索引慢：每个新节点都要搜索最近邻居
  3. 不适合磁盘：内存不够时性能急剧下降
```

### DiskANN：HNSW 装不下时的磁盘方案

```text
内存中：
  ├── 图结构（邻居关系）
  └── 压缩向量（PQ 编码）
磁盘上：
  └── 原始向量 + 全精度邻居信息
```

```text
1. 在内存中的压缩图上贪心搜索
2. 当需要更精确距离时，从磁盘按需加载原始向量
3. 合并精排，返回 TopK
```

```text
[
  {12: 0.82},
  {405: 0.31},
  {9200: 1.25}
]
```

**核心思想**：HNSW 性能好但必须全在内存。**DiskANN 把向量主体放磁盘，内存里只保留图结构 + 压缩向量**。

**数据结构**：

**查询过程**：


### SPARSE_INVERTED_INDEX：稀疏向量的倒排索引

**先明白什么是稀疏向量**：

稀疏向量绝大多数维度都是 0，只有少量维度非零。典型例子是关键词词频表示：

它**维度可能很高**（几万到几十万），但**只有几十个维度有值**。密集向量那种"每个维度都算距离"在稀疏向量上毫无意义。

#### 3.1 倒排索引的核心结构

SPARSE_INVERTED_INDEX 本质就是**倒排索引**。Milvus 中 BM25 全文检索就用这个索引。


意思是：

**稀疏向量里，大部分维度都是 0，只有少数维度有值。倒排索引只记录这些“非零维度”出现在哪些文档 / 向量里。**

---

## 1. 先看稀疏向量是什么

```text
[0.12, 0.56, 0.33, 0.91, 0.28, ...]
```

```text
维度 3: 0.8
维度 10: 0.5
维度 102: 0.9
```

```text
[0, 0, 0, 0.8, 0, 0, 0, 0, 0, 0, 0.5, ..., 0.9]
```

普通稠密向量可能是这样：

每个维度基本都有值。

但稀疏向量是这样：

完整写出来其实是：

大部分都是 `0`，所以只存非零维度。

---

## 2. “每个非零维度对应一个 posting list”是什么意思？

```text
doc1 = {3: 0.8, 10: 0.5}
doc2 = {3: 0.6, 20: 0.9}
doc3 = {10: 0.7, 20: 0.4}
```

```text
维度 3  -> doc1: 0.8, doc2: 0.6
维度 10 -> doc1: 0.5, doc3: 0.7
维度 20 -> doc2: 0.9, doc3: 0.4
```

假设有 3 篇文档，被 BM25 转成稀疏向量：

这里的 `3、10、20` 可以理解成词 ID，或者稀疏向量的维度编号。

那么倒排索引会反过来存：

这个列表就叫  **posting list** 。

---

## 3. posting list 是什么？

```text
维度 3 -> doc1: 0.8, doc2: 0.6
```

```text
doc1 在维度 3 上有值，权重是 0.8
doc2 在维度 3 上有值，权重是 0.6
doc3 在维度 3 上没有值，所以不记录
```

> 每个非零维度对应一个 posting list，记录哪些向量在这个维度上有值

> 对每一个出现过的词 / 维度，建立一个名单，名单里记录哪些文档包含这个词，以及这个词在该文档中的权重是多少。

**posting list = 某个维度出现在哪些文档 / 向量里的记录列表。**

比如：

意思是：

所以你那句话：

翻译成大白话就是：

---

## 4. 放到 BM25 里怎么理解？

```text
doc1：苹果 发布 新手机
doc2：苹果 股价 上涨
doc3：英伟达 发布 芯片
```

```text
苹果 -> doc1, doc2
发布 -> doc1, doc3
新手机 -> doc1
股价 -> doc2
上涨 -> doc2
英伟达 -> doc3
芯片 -> doc3
```

```text
苹果 发布
```

```text
苹果 -> doc1, doc2
发布 -> doc1, doc3
```

```text
doc1 同时包含 苹果 和 发布，分数高
doc2 只包含 苹果，分数低一些
doc3 只包含 发布，分数低一些
```

BM25 全文检索里，每个词可以看成一个维度。

比如有三篇新闻：

可以建立倒排索引：

当你搜索：

系统不用扫描所有文档，只需要查：

然后把相关文档拿出来算分：

这就是倒排索引加速检索的核心。

---

## 5. 一句话总结

```text
原始存法：每个向量有哪些非零维度
倒排存法：每个非零维度对应哪些向量
```

- 精确关键词命中（型号、编号、专有名词）
- 适合需要"原词命中"的检索
- 与密集向量检索互补，做混合搜索

- 不擅长语义理解（同义词、近义词召回不到）
- 维度越高 posting list 越长
- 单独使用召回质量有限，必须配合密集向量

**SPARSE_INVERTED_INDEX 的本质就是：把“文档有哪些词”反过来变成“每个词出现在哪些文档里”。**

在 Milvus 稀疏向量里就是：

这样查询时只查 query 中出现的那些维度对应的 posting list，不需要遍历全部向量。

#### 3.4 优缺点

**优点**：

**缺点**：

---

### 4. 三个索引的真实关系（一句话总结）

```text
HNSW：默认，密集向量的首选，内存换速度
DiskANN：HNSW 装不下时的退路，磁盘换内存
SPARSE_INVERTED_INDEX：另一类问题，关键词检索，和 HNSW 互补
```

```text
用户问题
   ├── HNSW 密集向量 → 找语义接近的
   └── SPARSE_INVERTED_INDEX 关键词 → 找精确命中的
                ↓
             重排序融合
                ↓
             返回最终 TopK
```

**典型混合搜索流程**：


## 十、距离度量

**核心问题：什么叫"两个向量相似"？**

数学上有 3 种主流定义，对应 Milvus 3 种度量方式。

### 1. L2（欧氏距离 / Euclidean Distance）

```text
向量 A = [a1, a2, ..., aD]
向量 B = [b1, b2, ..., bD]
L2(A, B) = sqrt( (a1-b1)² + (a2-b2)² + ... + (aD-bD)² )

**最直观**——空间里两点的直线距离。

768 维时：768 次减法 + 768 次乘法 + 1 次开方
```

**几何意义**：把向量想成 D 维空间里的一个点，L2 就是这个空间里两点的直线距离。

**越接近 → L2 越小 → 越相似**。

**适用**：当向量的**绝对位置和大小**都有意义时（比如某些图像特征）。

### 2. IP（内积 / Inner Product）

```text
IP(A, B) = a1·b1 + a2·b2 + ... + aD·bD
         = ||A|| × ||B|| × cos(θ)

- 如果两个向量方向一致（θ=0），IP 等于它们长度的乘积
- 如果方向相反（θ=180°），IP 是负数
- 如果方向正交（θ=90°），IP=0

**最常被搞混的一个**。

其中 θ 是两个向量的夹角
||A|| 是向量 A 的长度（模）
```

**关键洞察**：IP 实际上是**"两个向量的长度乘积"乘以"它们方向上的余弦"**。

**IP 越大越相似**——但"相似"在 IP 里既包含"方向一致"也包含"长度都大"。

**最常用 IP 模拟 COSINE**（见下）。

### 3. COSINE（余弦相似度 / Cosine Similarity）

```text
COSINE(A, B) = (A · B) / (||A|| × ||B||)
             = cos(θ)                ← 几何上就是夹角的余弦
```

- COSINE=1：完全同向
- COSINE=0：正交
- COSINE=-1：完全反向

**只看方向，不看长度**。

**值域**：`[-1, 1]`。

**适用**：当你想表达"两个东西语义相似不相似"，而**不在乎它们的量级**时。比如文本 embedding 经常用，因为 "我喜欢猫" 和 "我真的非常喜欢猫" 这两个向量的长度可能不一样，但方向（语义）应该接近。



## 十一、过滤搜索

```python
results = client.search(
    collection_name="bearing_fault",
    data=[query_vector],
    filter='device_type == "bearing" and speed > 1000',
    limit=10
)
```

**核心问题：能不能在向量搜索的同时加业务条件？**

比如查"最像的图片，但只查 device_type=轴承"。

### 两种执行方式

```text
先按 filter 条件筛出符合条件的实体
   ↓
只在这些实体里做 ANN 搜索
   ↓
返回 TopK
```

```text
全库 1000 万条向量
   ↓ filter 筛掉 99% 噪声
剩 10 万条候选
   ↓
只对 10 万条做向量搜索
```

```text
正常做 ANN 搜索
   ↓
逐步返回候选
   ↓
对每个候选检查 filter
   ↓
直到收集够 K 个满足条件的
```

```text
ANN 搜出 100 个候选
   ↓
按 filter 检查，过滤掉 80 个
   ↓
再搜 100 个，再过滤
   ↓
直到攒够 TopK
```

**标准过滤（Standard Filtering）—— 前置过滤**：

**适合**：filter 选得很准，**能大幅缩小候选范围**的场景。

**迭代过滤（Iterative Filtering）—— 后置过滤**：

**适合**：filter 太复杂（比如复杂正则、JSON 字段），前置扫一遍代价极高。

### 怎么选

```text
filter 选得准、能缩到原数据 1% 以下 → 标准过滤
filter 复杂、选择性差、前置代价高 → 迭代过滤
```

**别死背"Milvus 都是前过滤"**——实际有这两种实现。



### 查询慢怎么排查（按从易到难）

```python
# 查看 Collection 实际用什么索引
desc = client.describe_collection("bearing_fault")
for f in desc["fields"]:
    if "params" in f:
        print(f["name"], f["params"])
# 如果输出 index_type=FLAT，那确实在暴力搜
```

```text
TopK 太大（比如 10000）→ 结果集合并开销大
ef 太大（HNSW）→ 候选集扫描多
nprobe 太大（IVF）→ 簇扫得多
返回字段太多 → 序列化开销
```

```python
filter='content LIKE "%轴承%"'  # 字符串模糊匹配，扫全字段
```

```python
# 看 segment 数量
stats = client.get_collection_stats("bearing_fault")
print(stats)  # 看 row_count, segment_count
```

```text
Query Node OOM？→ 减少单节点加载的 segment
CPU 100%？→ HNSW 是 CPU 密集（距离计算）
MinIO 慢？→ 网络或磁盘 IO 瓶颈
WAL 堆积？→ Streaming Node 跟不上
```

**第一层：是不是还在暴力搜？**

FLAT 在百万级以上数据基本不可用。**没建索引是性能最常见的原因**。

**第二层：查询参数是不是太重？**

**第三层：过滤条件是否过重？**

这种 filter 没有合适的标量索引，**每条候选都要现场算**，极慢。

**第四层：Segment 状态是不是有问题？**

如果 `segment_count` 远大于预期（比如 1 亿数据分了几千个 segment），说明**缺少 compaction**。每个 segment 都要单独搜一遍然后合并，segment 越多越慢。

**第五层：系统资源**


---


---

# 数据库

---

## 1. 什么是 PostgreSQL？

**我的回答：**

在我看来，PostgreSQL 是一个功能非常强大的开源关系型数据库管理系统，它以稳定性、可扩展性和标准兼容性著称，在我的项目经验中，它经常是我们处理复杂业务的首选。

**核心特点**

我认为开源免费是它最大的优势，采用 BSD 许可证，无商业限制，企业可以自由使用和修改。

ACID 兼容方面，我的理解是它完整支持事务的原子性、一致性、隔离性、持久性，非常适合金融等对数据一致性要求高的场景。

扩展性强，在我的项目中，我们经常使用自定义类型、函数、索引，可以根据业务需求扩展数据库能力。

多数据类型支持，除了传统的关系型数据，还支持 JSON/JSONB、数组、几何类型、自定义类型，可以处理半结构化数据。

MVCC 多版本并发控制，读写不阻塞，高并发场景下性能优异，我在实际项目中深切体会到这一点。

全文检索内置，不需要额外集成搜索引擎，就能支持全文检索功能。

**与 MySQL 对比**

我认为 PostgreSQL 的复杂查询能力更强，支持窗口函数、递归查询（CTE）、复杂连接，适合分析型场景。

JSON 支持更好，JSONB 是二进制存储，支持索引，查询性能远超 MySQL 的 JSON 类型。

扩展性更强，支持扩展插件，如 PostGIS 支持地理信息，pg_trgm 支持模糊搜索。

适用场景不同，我的理解是 PostgreSQL 适合复杂业务、分析型场景；MySQL 适合简单业务、读多写少场景。

**典型应用场景**

复杂业务系统，需要复杂查询、事务支持。

JSON 数据存储，需要灵活的半结构化数据存储。

地理信息系统，使用 PostGIS 扩展。

全文检索，使用内置的全文检索功能。

---

## 2. MySQL 的整体架构是怎样的？

```
┌─────────────────────────────────────────────────────────┐
│                      客户端连接                          │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│  连接层：连接管理、认证、线程池                           │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│  服务层：SQL接口 → 解析器 → 优化器 → 执行器              │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│  存储引擎层：InnoDB / MyISAM / Memory                    │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│  文件系统层：数据文件、日志文件、配置文件                 │
└─────────────────────────────────────────────────────────┘
```

```
客户端发送 SQL → 连接层接收 → 服务层解析优化 → 执行器调用存储引擎 → 存储引擎读写数据 → 返回结果
```

**我的回答：**

我的理解是，MySQL 采用分层架构，从上到下分为四层：连接层、服务层、存储引擎层、文件系统层。

**连接层**

我认为这一层主要负责连接管理、认证、线程管理。客户端连接到达后，连接层进行身份认证，分配线程处理请求。在我的项目中，我们使用连接池复用连接，减少连接建立的开销。

关键参数包括最大连接数（max_connections）、连接超时时间（wait_timeout）、交互超时时间（interactive_timeout）。

**服务层**

这是核心层，包含 SQL 接口、解析器、优化器、缓存、执行器。

我的理解是，SQL 接口接收 SQL 语句，解析器进行语法分析和语义分析，优化器生成执行计划，执行器调用存储引擎执行。

查询缓存在 MySQL 8.0 已移除，我认为原因是缓存失效频繁，收益不大。

**存储引擎层**

MySQL 的特色是插件式存储引擎，不同引擎有不同特点。

在我的项目经验中，InnoDB 是默认引擎，支持事务、行锁、外键，适合 OLTP 业务。

MyISAM 不支持事务，表锁，适合读多写少场景。

Memory 是内存存储，适合临时表、缓存。

**文件系统层**

数据存储层，包括数据文件、日志文件、配置文件。

InnoDB 的数据文件包括系统表空间（ibdata1）、独立表空间（.ibd）、重做日志（ib_logfile）。

**查询执行流程**

我认为可以用下面的流程来理解：

---

## 3. MySQL 包含哪些锁？各类锁的作用分别是什么？

```
┌─────────────────────────────────────────────────────────────┐
│                        MySQL 锁体系                          │
├─────────────────────────────────────────────────────────────┤
│  按粒度分类：                                                │
│    ├─ 全局锁（锁定整个数据库实例）                            │
│    ├─ 表级锁（锁定整张表）                                    │
│    └─ 行级锁（锁定单行记录，InnoDB 特有）                     │
├─────────────────────────────────────────────────────────────┤
│  按类型分类：                                                │
│    ├─ 共享锁（S 锁）：允许其他事务读，阻塞写                  │
│    └─ 排他锁（X 锁）：阻塞其他所有读写                        │
├─────────────────────────────────────────────────────────────┤
│  InnoDB 行锁类型：                                           │
│    ├─ 记录锁（Record Lock）：锁定单行                        │
│    ├─ 间隙锁（Gap Lock）：锁定记录之间的间隙                  │
│    └─ 临键锁（Next-Key Lock）：记录锁 + 间隙锁               │
└─────────────────────────────────────────────────────────────┘
```

| 锁类型 | 共享锁(S) | 排他锁(X) |
|-------|----------|----------|
| 共享锁(S) | 兼容 | 互斥 |
| 排他锁(X) | 互斥 | 互斥 |

**我的回答：**

我的理解是，MySQL 锁机制分为多个维度：按粒度分为全局锁、表级锁、行级锁；按类型分为共享锁、排他锁；按模式分为意向锁、记录锁、间隙锁、临键锁。

**全局锁**

全局锁锁定整个数据库，只读不可写。在我的项目中，使用场景主要是全库逻辑备份，保证备份期间数据一致性。

通过 FLUSH TABLES WITH READ LOCK 加锁，UNLOCK TABLES 释放。

**表级锁**

我认为表级锁锁定整张表，粒度大，并发度低。

表锁分为共享读锁和独占写锁。读锁允许其他读，阻塞写；写锁阻塞所有读写。

元数据锁（MDL）是自动加的，DDL 操作会阻塞 DML，防止表结构变更时数据不一致。

意向锁是表级锁，表示事务意图在行上加锁。意向共享锁（IS）表示意图加共享锁，意向排他锁（IX）表示意图加排他锁。我的理解是，意向锁的作用是快速判断表中是否有行被锁定，避免逐行检查。

**行级锁**

行级锁是 InnoDB 特有的，粒度小，并发度高。

记录锁（Record Lock）锁定单行记录，防止其他事务修改或删除。

间隙锁（Gap Lock）锁定记录之间的间隙，防止其他事务插入，解决幻读问题。

临键锁（Next-Key Lock）是记录锁加间隙锁，锁定记录及其前后的间隙，是 InnoDB 默认的行锁模式。

**锁兼容性**

我认为锁的兼容性可以用下表来理解：

意向锁之间兼容，意向锁和表级排他锁互斥。

---

## 4. Redis 核心特性？

| 数据类型 | 适用场景 | 我的实践经验 |
|---------|---------|-------------|
| String | 简单键值、计数器 | 缓存用户信息、分布式计数 |
| Hash | 对象存储 | 存储商品详情 |
| List | 消息队列 | 简单消息推送 |
| Set | 去重集合 | 用户标签、共同好友 |
| ZSet | 排行榜 | 热门榜单、延迟队列 |

**我的回答：**

在我看来，Redis 是一个高性能的内存键值数据库，核心特性包括：

**内存存储**

数据存储在内存中，读写速度极快，QPS 可达 10 万以上。我认为这是 Redis 最大的优势，也是最大的限制——内存容量有限，不能存储海量数据。

**单线程模型**

Redis 采用单线程处理请求，避免多线程的锁竞争和上下文切换。我认为单线程之所以快，是因为内存操作是纳秒级，CPU 不是瓶颈，单线程避免了锁开销。Redis 6.0 引入多线程处理网络 IO，但命令执行仍是单线程。

**丰富数据类型**

支持 String、Hash、List、Set、ZSet、Stream、Geo 等数据类型。我的理解是，不同类型适合不同场景：

**持久化**

支持 RDB 快照和 AOF 日志两种持久化方式，可以保证数据不丢失。RDB 是定时快照，恢复快但可能丢失数据；AOF 是追加日志，数据安全但文件大。

**高可用**

支持主从复制、哨兵模式、Cluster 集群，可以实现高可用和水平扩展。

**其他特性**

事务支持（MULTI/EXEC）、发布订阅（Pub/Sub）、Lua 脚本、过期策略、内存淘汰策略。

---

## 5. Redis 支持哪些数据类型？对应的底层数据结构是什么？

```
ZSet 底层数据结构示意：

**我的回答：**

**String**

最基础的类型，可以存储字符串、整数、浮点数、二进制数据。

我的理解是，底层是 SDS（Simple Dynamic String），动态字符串。相比 C 语言字符串，SDS 记录了长度，获取长度是 O(1)；二进制安全，可以存储任意数据；自动扩容，避免缓冲区溢出。

**Hash**

键值对集合，适合存储对象。

底层是 ziplist（压缩列表）或 hashtable（哈希表）。我的理解是，元素少时用 ziplist，节省内存；元素多时用 hashtable，查询效率高。

**List**

有序列表，支持从两端插入和弹出。

底层是 quicklist（快速列表），是 ziplist 组成的双向链表。我认为这结合了链表的灵活和 ziplist 的内存紧凑。

**Set**

无序集合，元素唯一。

底层是 intset（整数集合）或 hashtable。我的理解是，元素都是整数且数量少时用 intset，否则用 hashtable。

**ZSet**

有序集合，元素唯一且有分数排序。

底层是 ziplist 或 skiplist（跳表）加 dict（字典）。我的理解是，跳表支持 O(logN) 的范围查询，字典支持 O(1) 的成员查询。

跳表（支持范围查询 O(logN)）:
    ┌───┐     ┌───┐     ┌───┐     ┌───┐
    │ 1 │────→│ 3 │────→│ 5 │────→│ 7 │
    └───┘     └───┘     └───┘     └───┘
      │         │         │         │
    ┌───┐     ┌───┐     ┌───┐
    │ 1 │────→│ 3 │────→│ 5 │
    └───┘     └───┘     └───┘

字典（支持成员查询 O(1)）:
    ┌─────────────┐
    │ member → score │
    ├─────────────┤
    │ "user1" → 100 │
    │ "user2" → 200 │
    └─────────────┘
```

**Stream**

消息队列类型，支持消费者组、消息确认。

底层是 radix tree（基数树）加 listpack（紧凑列表）。

**Geo**

地理坐标类型，支持距离计算、范围查询。

底层是 ZSet，将经纬度编码为分数存储。

---

## 6. Redis 为什么采用单线程架构？

```
IO 多路复用工作模型：

**我的回答：**

我的理解是，Redis 采用单线程是基于以下考虑：

**避免锁竞争**

多线程需要加锁保护共享资源，锁竞争会带来性能损耗。我认为 Redis 的操作大多是简单的内存读写，加锁的开销可能比操作本身还大。单线程避免了锁竞争，简化了实现。

**避免上下文切换**

线程切换需要保存和恢复寄存器、栈等上下文，频繁切换消耗 CPU。我的理解是，Redis 的请求处理很快，多线程的切换开销可能超过并行带来的收益。

**内存操作为主**

Redis 的主要操作是内存读写，CPU 不是瓶颈。我认为单线程处理内存操作已经足够快，多线程的收益有限。

**IO 多路复用**

Redis 使用 epoll/kqueue 实现 IO 多路复用，单线程可以处理大量并发连接。我的理解是，网络 IO 不是瓶颈，不需要多线程处理。

┌─────────────────────────────────────────────┐
│              单线程事件循环                   │
│                                             │
│   ┌─────────┐                              │
│   │ epoll   │ ← 监听多个 socket 连接        │
│   │ 等待    │                              │
│   └────┬────┘                              │
│        ↓                                   │
│   ┌─────────┐                              │
│   │ 分发    │ → 可读事件 / 可写事件         │
│   │ 事件    │                              │
│   └────┬────┘                              │
│        ↓                                   │
│   ┌─────────┐                              │
│   │ 执行    │ → 处理命令（内存操作，极快）  │
│   │ 回调    │                              │
│   └─────────┘                              │
└─────────────────────────────────────────────┘
```

**Redis 6.0 的多线程**

Redis 6.0 引入多线程处理网络 IO，用于解压和压缩数据。但命令执行仍是单线程。我认为这是因为在高吞吐场景下，网络 IO 成为瓶颈，多线程可以提升网络处理能力。

---

## 7. 为什么 Redis 的读写速度这么快？

**我的回答：**

我认为 Redis 高性能的原因是多方面的：

**纯内存操作**

内存读写是纳秒级，磁盘读写是毫秒级，差了几个数量级。我的理解是，Redis 数据存储在内存中，这是高性能的根本原因。

**单线程模型**

单线程避免了锁竞争和上下文切换。在我看来，虽然多线程看起来应该更快，但对于 Redis 这种内存密集型操作，单线程反而更高效。

**IO 多路复用**

Redis 使用 epoll/kqueue 实现 IO 多路复用，单线程可以处理大量并发连接。我的理解是，epoll 的高效在于只处理就绪的连接，不需要遍历所有连接。

**高效数据结构**

SDS、跳表、压缩列表等数据结构都是为性能优化的。比如我认为跳表支持 O(logN) 的范围查询，比平衡树实现简单且性能相当。

**协议简单**

Redis 使用 RESP 协议，解析简单高效。相比 HTTP 等复杂协议，RESP 的解析开销很小。

**无磁盘 IO**

常规操作不涉及磁盘 IO，只有持久化时才写磁盘。异步持久化不阻塞主线程。

---

## 8. Redis 持久化？

```
混合持久化文件结构：

**我的回答：**

我的理解是，Redis 提供两种持久化机制：RDB 和 AOF。

**RDB（快照）**

RDB 是将内存数据快照保存到磁盘。触发方式可以是手动（SAVE/BGSAVE）或自动（配置规则）。

SAVE 会阻塞主线程，我认为生产环境不推荐。BGSAVE 会 fork 子进程处理，不阻塞主线程。

我的理解是，RDB 的优点是文件紧凑、恢复快、对性能影响小。缺点是定时快照，可能丢失数据；fork 大数据集时可能阻塞。

**AOF（日志）**

AOF 是将每个写命令追加到日志文件。有三种同步策略：always（每次写入都同步）、everysec（每秒同步）、no（由操作系统决定）。

我认为 AOF 的优点是数据更安全（最多丢失 1 秒数据）、可读性好。缺点是文件大、恢复慢、写入性能略低。

AOF 重写可以压缩文件大小，去除冗余命令。

**混合持久化**

Redis 4.0 引入混合持久化，AOF 重写时先写入 RDB 格式数据，再追加 AOF 增量数据。我认为这结合了 RDB 恢复快和 AOF 数据安全的优点。

┌─────────────────────────────────────────────┐
│              混合 AOF 文件                   │
├─────────────────────────────────────────────┤
│  ┌───────────────────────────────────────┐  │
│  │  RDB 格式（基础数据快照）              │  │
│  │  - 全量数据                            │  │
│  │  - 加载速度快                          │  │
│  └───────────────────────────────────────┘  │
│  ┌───────────────────────────────────────┐  │
│  │  AOF 格式（增量命令）                  │  │
│  │  - 最新变更                            │  │
│  │  - 数据更完整                          │  │
│  └───────────────────────────────────────┘  │
└─────────────────────────────────────────────┘
```

---

## 9. Redis 的持久化机制有哪些（RDB/AOF/混合持久化）？各自的优缺点是什么？

| 场景 | 推荐方案 |
|-----|---------|
| 允许分钟级丢失 | RDB |
| 不允许数据丢失 | AOF |
| 兼顾性能和安全 | 混合持久化 |
| 纯缓存场景 | 可不持久化 |

**我的回答：**

**RDB**

我认为 RDB 的优点是：文件紧凑压缩，适合备份；恢复速度快；对性能影响小（fork 后子进程处理）。

缺点是：定时快照，可能丢失几分钟数据；fork 大数据集时可能阻塞；不适合实时持久化。

在我的项目中，适用场景是：允许分钟级数据丢失；数据量大，需要快速恢复；备份场景。

**AOF**

我认为 AOF 的优点是：数据更安全，最多丢失 1 秒数据；可读性好，便于分析；支持重写压缩。

缺点是：文件比 RDB 大；恢复速度慢；写入性能略低。

适用场景是：不允许数据丢失；数据量不大；需要审计日志。

**混合持久化**

我认为混合持久化的优点是结合 RDB 恢复快和 AOF 数据安全。

缺点是：配置复杂；文件格式不兼容旧版本。

适用场景是：兼顾性能和安全；Redis 4.0 以上版本。

**选择建议**

我认为可以这样选择：

---

## 10. Redis 作为缓存时，缓存穿透、缓存击穿、缓存雪崩分别是什么？对应的解决方案有哪些？

```
缓存问题对比图：

**我的回答：**

我认为这三个问题是 Redis 缓存使用中最经典的问题，让我逐一分析：

┌──────────────────────────────────────────────────────────────┐
│                     正常请求流程                              │
│                                                              │
│   客户端请求 → 缓存命中？ → 是 → 返回数据                     │
│                  ↓ 否                                        │
│             查询数据库 → 写入缓存 → 返回数据                  │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│  缓存穿透：key 不存在，每次都打到数据库                        │
│                                                              │
│   请求不存在的 key → 缓存未命中 → 数据库查询 → 返回空         │
│         ↑                              ↓                     │
│         └────────── 恶意请求循环 ────────┘                   │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│  缓存击穿：热点 key 过期瞬间，大量请求打穿                     │
│                                                              │
│   热点 key 过期 → 大量并发请求 → 全部打到数据库 → 数据库压力   │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│  缓存雪崩：大量缓存同时过期或 Redis 宕机                       │
│                                                              │
│   缓存批量过期 → 所有请求 → 数据库 → 数据库崩溃               │
└──────────────────────────────────────────────────────────────┘
```

**缓存穿透**

我的理解是，缓存穿透是指查询不存在的数据，缓存和数据库都没有，每次请求都穿透到数据库。

在我的项目中，遇到过恶意请求查询不存在的 key，大量请求打到数据库的情况。

解决方案：

缓存空值：查询结果为空时，也缓存一个空值（设置较短过期时间），下次请求直接返回空。

布隆过滤器：在查询缓存前，先用布隆过滤器判断 key 是否可能存在。我的理解是，布隆过滤器能判断"一定不存在"，过滤掉不存在的 key。

**缓存击穿**

我的理解是，缓存击穿是指热点数据过期瞬间，大量请求同时穿透到数据库。

场景是某个热点 key 过期，瞬间大量请求查询这个 key，全部打到数据库。

解决方案：

互斥锁：我认为可以让第一个请求获取锁后查询数据库并缓存，其他请求等待或返回旧数据。

逻辑过期：不设置 TTL，数据中包含过期时间。后台异步更新，请求始终能获取数据（可能是旧数据）。

**缓存雪崩**

我的理解是，缓存雪崩是指大量缓存同时过期，或 Redis 宕机，所有请求打到数据库。

在我的项目经验中，遇到过系统启动时预热了大量缓存，设置相同过期时间，同时过期后数据库崩溃的情况。

解决方案：

随机过期时间：我认为可以在基础过期时间上加随机偏移，避免同时过期。

多级缓存：本地缓存 + Redis 缓存，Redis 失效时本地缓存还能扛住。

熔断降级：数据库压力过大时熔断，返回降级响应。

---

## 11. 如何保证缓存与数据库的数据一致性？为什么选择"更新数据库后删除缓存"而非"更新缓存"？

```
问题场景时序图：

```
正确场景时序图：

使用 Binlog 监听（如 Canal）自动同步缓存变更。数据库变更后，自动删除或更新缓存，我认为这能保证最终一致性。

| 策略 | 问题描述 |
|-----|---------|
| 先更新缓存，再更新数据库 | 缓存成功、数据库失败，数据不一致 |
| 先更新数据库，再更新缓存 | 并发时可能覆盖新值 |
| 先删除缓存，再更新数据库 | 并发读时可能读到旧数据写入缓存 |
| 先更新数据库，再删除缓存 | 极端情况可能不一致，概率最低 |

**我的回答：**

**常见策略对比**

我认为这个问题需要从几种策略的优缺点来分析：

**为什么选择"更新数据库后删除缓存"**

让我分析"先删除缓存，再更新数据库"的问题：

线程 A：删除缓存 ──────────────────→ 更新数据库（新值）
              ↓
线程 B：      读缓存未命中 → 查数据库（旧值）→ 写入缓存（旧值）

结果：缓存是旧值，数据库是新值 → 数据不一致！
```

再看"先更新数据库，再删除缓存"：

线程 A：更新数据库（新值）→ 删除缓存
              ↓
线程 B：      读缓存命中（旧值）→ 返回旧数据

线程 A 完成删除缓存后：
线程 B：读缓存未命中 → 查数据库（新值）→ 写入缓存

结果：短暂不一致后恢复正常
```

**延迟双删**

为了进一步降低不一致概率，我的项目中采用延迟双删策略：先删除缓存 → 更新数据库 → 延迟后再删除缓存。我的理解是，延迟时间要大于数据库主从同步时间。

**最终一致性保障**

---

## 12. Redis 分布式锁？

**我的回答：**

我的理解是，分布式锁需要满足四个条件：互斥性（同一时刻只有一个客户端持有锁）、防死锁（锁必须有超时时间）、可重入（同一线程可多次获取）、高可用（锁服务稳定可靠）。

**基础实现**

在我的项目中，使用 SET key value NX EX seconds 命令实现。NX 表示 key 不存在才设置，EX 表示设置过期时间。我认为这个命令是原子的，可以保证互斥性和防死锁。

释放锁时，需要判断 value 是否是自己设置的，避免误删其他客户端的锁。我的理解是，使用 Lua 脚本保证原子性：先判断 value，再删除 key。

**Redisson 实现**

Redisson 是 Redis 的 Java 客户端，提供了完善的分布式锁实现。

可重入：我认为这是一个重要特性，同一个线程可以多次获取同一把锁，需要记录获取次数。

看门狗机制：锁默认 30 秒过期，后台线程每 10 秒检查，如果锁还被持有则续期。在我的项目经验中，这避免了业务执行时间超过锁过期时间导致锁失效的问题。

公平锁：按请求顺序获取锁，避免饥饿。

读写锁：支持读读共享、读写互斥、写写互斥。

**Redlock 算法**

我的理解是，Redlock 在多个 Redis 节点上获取锁，只有大多数节点获取成功才算成功。这解决了单节点故障问题。

---

## 13. 如何基于 Redis 实现分布式锁？（基于 set nx/ex vs Redisson 实现的区别与优势）

| 特性 | SET NX/EX | Redisson |
|------|-----------|----------|
| 基本锁 | 支持 | 支持 |
| 可重入 | 不支持 | 支持 |
| 看门狗 | 不支持 | 支持 |
| 等待队列 | 不支持 | 支持 |
| 公平锁 | 不支持 | 支持 |
| 实现复杂度 | 简单 | 复杂 |

**我的回答：**

**SET NX/EX 实现**

在我的项目中，使用 SET key value NX EX seconds 命令实现。NX 保证互斥性，EX 保证防死锁。

我认为这种方式的优点是实现简单，不需要额外依赖。

缺点是不支持可重入，不支持等待队列，无看门狗机制，需要自己处理各种边界情况。

**Redisson 实现**

Redisson 提供了完善的分布式锁实现，开箱即用。

我认为它的优点是支持可重入、看门狗自动续期、公平锁、读写锁、信号量等高级功能。

缺点是依赖 Redisson 库，实现复杂。

**对比**

我认为可以用下面的表格来对比：

**选择建议**

我的建议是：

简单场景、快速验证：SET NX/EX。

生产环境、需要高可靠：Redisson。

需要可重入或看门狗：Redisson。

---

## 14. Redis 如何实现高可用？（主从复制、哨兵模式、Redis Cluster）

```
哨兵模式架构图：

```
Redis Cluster 架构示意：

**我的回答：**

**主从复制**

我的理解是，主从复制的架构是一个 Master 负责读写，多个 Slave 负责读。Master 数据异步复制到 Slave。

我认为它的优点是实现简单，读写分离提升读性能。

缺点是故障需要手动切换，Master 宕机后需要手动提升 Slave 为 Master。

**哨兵模式**

架构是主从复制 + Sentinel 集群。Sentinel 负责监控 Master，自动故障转移。

┌─────────────────────────────────────────────────────────┐
│                     Sentinel 集群                        │
│         ┌─────┐    ┌─────┐    ┌─────┐                  │
│         │ S1  │    │ S2  │    │ S3  │                  │
│         └──┬──┘    └──┬──┘    └──┬──┘                  │
│            └──────────┼──────────┘                      │
│                       ↓ 监控 + 故障转移                  │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│                   Redis 主从集群                         │
│                                                         │
│         ┌─────┐      复制      ┌─────┐                 │
│         │Master│ ───────────→ │Slave1│                 │
│         │(读写)│              │(只读)│                 │
│         └─────┘      复制      └─────┘                 │
│              └────────────────→┌─────┐                 │
│                              │Slave2│                 │
│                              │(只读)│                 │
│                              └─────┘                 │
└─────────────────────────────────────────────────────────┘
```

工作流程：Sentinel 监控 Master → 发现 Master 下线 → 选举领头 Sentinel → 选择新 Master → 通知其他 Slave 复制新 Master → 通知客户端新 Master 地址。

我认为优点是自动故障转移，无需人工干预。

缺点是数据可能丢失（异步复制），单点写入瓶颈，故障切换有延迟。

**Redis Cluster**

架构是多主多从，数据分片。16384 个槽位分布在不同 Master 上，每个 Master 负责一部分槽位。

┌─────────────────────────────────────────────────────────────┐
│                    16384 个槽位分布                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                 │
│  │ Master A │  │ Master B │  │ Master C │                 │
│  │ 槽 0-5460│  │槽 5461-10922│ │槽 10923-16383│            │
│  │    ↓     │  │    ↓     │  │    ↓     │                 │
│  │ Slave A' │  │ Slave B' │  │ Slave C' │                 │
│  └──────────┘  └──────────┘  └──────────┘                 │
│                                                             │
│  节点间通过 Gossip 协议通信，自动故障转移                    │
└─────────────────────────────────────────────────────────────┘
```

我认为优点是数据分片，支持水平扩展；自动故障转移；写入能力可扩展。

缺点是客户端需要支持集群协议；跨槽位操作受限；运维复杂。

**选择建议**

我的建议是：

数据量小、写入 QPS 低：哨兵模式。

数据量大、写入 QPS 高：Redis Cluster。

简单场景、可接受手动切换：主从复制。

---

## 15. 哨兵模式存在哪些问题？

```
脑裂问题示意：

**我的回答：**

**数据丢失风险**

我的理解是，哨兵模式采用异步复制，Master 接收写入命令后立即返回成功，再异步复制到 Slave。如果 Master 宕机时部分数据还未复制到 Slave，这部分数据就丢失了。

在我的项目中，我们使用配置 min-replicas-to-write 和 min-replicas-max-lag 来解决，要求至少 N 个 Slave 且延迟小于 M 秒时才接受写入。

**脑裂问题**

网络分区可能导致出现两个 Master。Sentinel 集群与原 Master 网络分区，选举出新 Master；但部分客户端仍连接原 Master，导致两个 Master 同时接受写入。

正常状态：                    脑裂后：
┌──────────────┐            ┌──────────────┐
│   Sentinel   │            │   Sentinel   │
│    集群      │            │    集群      │
└──────┬───────┘            └──────┬───────┘
       ↓                           ↓
┌──────────────┐            ┌──────────────┐
│    Master    │            │ 新 Master    │← Sentinel 选举
│   (原主)     │            │  (原Slave)   │
└──────────────┘            └──────────────┘
       ↑                    ┌──────────────┐
       │ 网络分区            │ 原 Master    │← 部分客户端仍连接
客户端请求                   │  (孤立)      │
                             └──────────────┘
```

我认为解决方法是设置合理的超时时间，配置 min-replicas-to-write 限制写入条件。

**单点写入瓶颈**

只有一个 Master 可写，写入 QPS 受限于单节点。无法通过增加节点提升写入能力。

我认为如果写入是瓶颈，应该考虑 Redis Cluster。

**故障切换延迟**

故障检测需要主观下线 → 客观下线 → 选举 → 切换，整个过程可能超过 1 分钟。期间服务不可用。

解决方法是调整 down-after-milliseconds 参数，但我的理解是，太短可能误判。

---

## 16. Redis 如何实现高可扩展性？有限内存的 Redis 如何支持海量数据存储？

```
数据分层存储策略：

**我的回答：**

**水平扩展方案**

**Redis Cluster 分片**

我的理解是，Redis Cluster 将数据分片到多个节点，每个节点负责一部分槽位。写入和读取都可以水平扩展。

**客户端分片**

客户端根据 key 计算应该访问哪个节点。在我的项目中，我们使用一致性哈希算法，节点变化时只影响部分 key。

**代理分片**

客户端通过代理（如 Twemproxy、Codis）访问 Redis 集群。我认为代理负责路由请求，对客户端透明。

**海量数据存储策略**

**数据分层**

┌─────────────────────────────────────────────────────────┐
│  热数据（访问频繁）→ Redis 内存存储                      │
│  - 实时访问要求高                                       │
│  - 数据量相对较小                                       │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│  温数据（访问一般）→ SSD 存储（如 RocksDB）              │
│  - 访问延迟可接受                                       │
│  - 容量比内存大                                         │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│  冷数据（很少访问）→ 磁盘数据库                          │
│  - 归档存储                                             │
│  - 成本最低                                             │
└─────────────────────────────────────────────────────────┘
```

**数据压缩**

对大 value 进行压缩存储，减少内存占用。我的理解是，读取时解压，用 CPU 换内存。

**数据过期策略**

设置合理的过期时间，定期清理过期数据。使用 LRU 淘汰策略，内存不足时淘汰最少使用的数据。

**容量规划**

我的建议是，单节点内存建议不超过 10GB（避免 fork 阻塞）。集群规模 = 总数据量 / 单节点内存。考虑主从复制，实际节点数翻倍。

---

## 17. Redis 的集群模式有哪些？整体架构是怎样的？

```
Redis Cluster 详细架构：

| 模式 | 特点 | 适用场景 |
|-----|------|---------|
| 主从复制 | 1 主 N 从，简单但无自动故障转移 | 简单场景 |
| 哨兵模式 | 主从 + Sentinel，自动故障转移 | 中小规模 |
| Redis Cluster | 多主多从，数据分片，原生集群 | 大规模 |
| Codis | 代理模式，透明分片，需额外组件 | 兼容性好 |
| Twemproxy | 代理模式，静态分片 | 简单分片 |

| 维度 | 哨兵模式 | Redis Cluster |
|------|---------|---------------|
| 数据分片 | 不支持 | 支持 |
| 水平扩展 | 不支持 | 支持 |
| 故障转移 | 支持 | 支持 |
| 客户端 | 简单 | 需要 Smart Client |
| 运维复杂度 | 中 | 高 |
| 适用规模 | 中小 | 大规模 |

**我的回答：**

**集群模式分类**

我的理解是，Redis 集群模式主要有以下几种：

**Redis Cluster 架构**

我认为 Redis Cluster 采用去中心化架构，没有中心节点。

┌─────────────────────────────────────────────────────────────────┐
│                         客户端请求                               │
│                    CRC16(key) % 16384                           │
└─────────────────────────────────────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│                      Smart Client                               │
│                 （缓存槽位映射，直接访问正确节点）               │
└─────────────────────────────────────────────────────────────────┘
          ↓                ↓                ↓
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│   Master A   │ │   Master B   │ │   Master C   │
│  槽 0-5460   │ │槽 5461-10922 │ │槽 10923-16383│
│   ┌────┐     │ │   ┌────┐     │ │   ┌────┐     │
│   │Slave│    │ │   │Slave│    │ │   │Slave│    │
│   └────┘     │ │   └────┘     │ │   └────┘     │
└──────────────┘ └──────────────┘ └──────────────┘
        ↑               ↑               ↑
        └───────────────┴───────────────┘
                    Gossip 协议通信
              （状态同步、故障检测、配置传播）
└─────────────────────────────────────────────────────────────────┘
```

数据分片：16384 个槽位分布在不同 Master 上。每个 key 根据公式 CRC16(key) % 16384 计算所属槽位。

节点通信：使用 Gossip 协议，节点间定期交换状态信息，包括槽位分配、节点状态等。

故障转移：Master 下线后，其 Slave 自动提升为 Master。我的理解是，需要半数以上 Master 同意。

客户端访问：客户端可以连接任意节点，节点返回 MOVED 重定向到正确节点。Smart Client 会缓存槽位映射，直接访问正确节点。

**集群 vs 哨兵**

我认为可以用下表来对比：

---
