# 计算机底层原理与代码执行机制

### 一、二进制：计算机硬件状态的数字映射

- 数字与文本：普通整数可直接转换为二进制；英文字母、中文、标点、运算符、代码中的函数名、变量名、关键字，本质都是字符，会通过ASCII、UTF-8等标准化字符编码规则映射为固定数字，再进一步转换为二进制，最终以二进制形式存储。
- 图片：屏幕图像由像素点矩阵构成，每个像素的颜色通过RGB三通道（红、绿、蓝）表示，每个通道用0~255的整数代表颜色强度，每个像素对应一组整数，这些整数最终全部转换为二进制，一张图片的本质就是像素对应的二进制序列集合。
- 视频：视频的本质是按固定时间间隔连续播放的静态图片（帧），30帧/秒即1秒内连续播放30张静态图片，60帧/秒即1秒内播放60张。每一帧图片均可转换为二进制数据，再搭配音频二进制数据、时间戳、压缩规则等内容，最终全部以二进制形式存储。
- 音频：声音通过等时间间隔采样转换为数字序列，每隔固定极短时间记录一次声音波形的振幅强弱，得到一组连续整数，该序列可完整还原声音波形，最终也全部转换为二进制形式存储。

计算机全程使用二进制，核心根源是其底层完全由海量半导体电子电路构成，这是整个计算机体系的物理起点，而非人为设定的抽象规则。

CPU本质上是无数个基础门电路的集合（门电路的组合又可以实现加法器），这些门电路由半导体三极管（晶体管）搭建而成；电子电路在工程上最容易稳定区分、抗干扰能力最强的，只有两种互斥的稳定物理状态——通电与不通电。为了实现逻辑抽象与数学运算，工程上将这两种物理状态分别映射为数字1和0，无数个晶体管就是无数个0和1，就可以代表世界万物，根据二进制指令，就可以改变0和1，也就是实施计算，由此构建了二进制数字体系。因此二进制不是计算机的可选规则，而是硬件物理特性决定的、最易实现、最稳定、最可靠的数字体系。

基于这个底层物理特性，计算机内部的所有信息，最终都可以用0和1组成的二进制序列表示。核心逻辑是：任何信息都可以通过标准化规则映射为整数数字，而所有整数都可以无损转换为二进制格式，该规则覆盖计算机可处理的所有内容：

此处有不可打破的底层核心规则：**同一串二进制序列，其物理含义完全由CPU执行的解码规则决定，按照不同规则解码，会呈现完全不同的形态**。同一串二进制，CPU执行文本编码解码指令时，它就是一段文字；CPU执行图片格式解码指令时，它就是一张图片；CPU执行音频格式解码指令时，它就是一段声音；**仅当CPU按照自身指令集规范对该二进制序列进行解码时，它才是CPU可直接执行的机器指令**；除此之外的所有二进制数据，CPU均无法直接识别和执行。

### 二、CPU：计算机体系唯一的物理执行与计算核心（执行机器指令，控制电路，完成对二进制数据计算）

CPU是计算机体系中**唯一的物理执行主体**，所有算术运算、逻辑运算、硬件控制、程序流程执行，都必须通过CPU执行二进制机器指令完成，不存在任何其他可执行指令的主体，这是计算机体系最核心的底层物理规则。

关于CPU的底层运行，有以下不可动摇的硬件级核心规则：

1. **指令执行的唯一性**：CPU只能直接识别、执行**完全符合自身指令集规范的二进制机器指令**，除此之外的任何内容，哪怕是二进制格式的文本、代码、图片，CPU都完全无法理解和直接运行。
2. **执行流程的硬件固定性**：CPU执行程序的物理本质，是以其时钟频率对应的极高速度，无限循环执行**取指→译码→执行→写回**的固定硬件流程，该流程由CPU硬件电路强制实现，无任何软件可修改：
   - 取指：CPU从程序计数器（PC/IP寄存器）指向的内存地址，取出待执行的二进制机器指令；
   - 译码：CPU内部译码电路对二进制指令进行解析，确定指令的操作类型、要操作的寄存器/内存地址与数据；
   - 执行：CPU通过内部门电路，完成指令对应的算术/逻辑计算，或生成硬件控制电信号(发给要操作的设备，比如内存、磁盘，然后由这些设备内部的控制器，比如说内存控制器（就是小芯片），去完成对内存的改变)；
   - 写回：改变完成后，控制器向cpu发结果信息，CPU将执行得到的二进制结果，写回指定的寄存器、CPU缓存或内存地址，同时自动更新PC寄存器的值，使其指向下一条待执行的指令地址。
     执行完一条指令后，CPU立刻自动进入下一次循环，程序就是通过该循环的持续运行得以实现。
3. **执行流的绝对控制性**：CPU的执行流100%由程序计数器（PC/IP寄存器）决定，CPU只会执行PC寄存器指向的内存地址中的指令。想让CPU执行任何代码，唯一的方式是修改PC寄存器的值，使其指向该代码的内存起始地址，无任何例外。
4. **特权级的硬件隔离性**：CPU硬件强制实现了特权级隔离（x86架构为ring0~ring3，arm架构为EL0~EL3），其中最高特权级（内核态）可执行所有CPU指令、访问所有硬件资源；最低特权级（用户态）仅能执行非特权指令、访问受限的内存空间。特权级的切换，只能由CPU执行特定硬件指令、或CPU接收到硬件中断时自动触发，用户态软件无法自主完成特权级切换。
5. **存储层级的硬件特性**：CPU内部有严格的速度与容量层级，寄存器是CPU内部速度最快、容量最小的临时存储空间，用于存放当前正在处理的数据、PC寄存器值、指令执行的中间结果；CPU缓存位于CPU与内存之间，用于减少CPU等待内存数据的耗时。它们的速度排序为：**寄存器＞CPU缓存＞内存＞磁盘**，容量排序则完全相反。

### 三、磁盘与内存：CPU的两级存储硬件载体

内存和磁盘的本质，都是存放二进制数据的硬件空间，二者的定位、硬件特性、访问逻辑完全不同，是CPU执行指令过程中核心的存储载体。

#### 内存

内存是CPU可直接寻址访问的高速临时存储硬件，是CPU与磁盘之间的唯一数据桥梁，专门用于存放CPU当前正在执行的机器指令、待处理的实时数据。

此处有不可打破的底层核心规则：**所有存入内存的内容，最终都只能是纯二进制格式，不存在"非二进制的其他代码"**。无论是操作系统内核程序、应用程序的可执行指令，还是高级语言源代码，只要进入内存等待CPU处理，就必须是纯二进制形态，无任何例外。

内存的核心硬件特性：读写速度极快，与CPU总线直接对接，可被CPU直接寻址访问；但断电后内部所有二进制数据会完全丢失，无法长期保存。

#### 磁盘

磁盘（包括机械硬盘HDD、固态硬盘SSD、U盘等）是计算机的持久化存储硬件，用于长期保存所有二进制数据。开发者编写的代码文件、安装的应用程序、操作系统本体、图片、视频、文档，最终都以纯二进制形式存放在磁盘中。

此处有不可打破的底层硬件规则：**CPU无法直接读写磁盘内的任何数据和代码，磁盘内的所有二进制内容，必须先通过总线传输到内存中，CPU才能读取、处理和执行**。磁盘本身无法执行通用机器指令，其内置的磁盘控制器仅能识别并执行CPU通过总线下发的、特定格式的硬件读写控制电信号。

从CPU硬件执行视角，一次完整的磁盘读写操作，全底层严谨流程如下：

1. **参数准备与系统调用触发**：CPU逐行执行用户态机器指令，按照操作系统约定的ABI规范，将磁盘读写类型、目标物理地址、数据长度、目标内存地址等参数写入CPU指定通用寄存器，随后执行CPU指令集内置的**系统调用硬件指令（x86_64为 `syscall`，arm为 `svc`）**。
2. **硬件自动特权级切换**：CPU执行 `syscall`指令时，硬件自动完成以下动作，无需任何软件参与：将CPU特权级从用户态切换至内核态（最高特权级），自动保存当前程序上下文（PC寄存器值、程序状态寄存器值），并将PC寄存器的值自动修改为操作系统启动时提前写入CPU特定寄存器的内核系统调用入口地址。
3. **内核态指令执行与命令下发**：PC寄存器已指向内核系统调用入口，CPU开始执行操作系统内核与磁盘驱动预编译的二进制机器指令，完成参数校验、权限检查、操作合法性校验，随后生成对应磁盘操作的机器指令序列并执行；执行过程中，CPU通过系统总线，将二进制读写命令、操作地址、控制参数以电信号形式，下发给磁盘控制器。
4. **磁盘硬件执行操作**：磁盘控制器接收到总线传输的电信号命令后，执行对应的硬件读写操作：读操作时，从磁盘对应物理地址读取二进制数据，通过系统总线回传至内存的指定地址；写操作时，从内存指定地址读取待写入的二进制数据，写入磁盘对应物理地址，完成数据持久化存储。
5. **硬件中断触发与CPU响应**：磁盘操作完成后，磁盘控制器通过系统总线向CPU的中断控制器发送硬件中断信号，同时携带对应中断号。CPU执行完当前正在执行的完整机器指令后，自动检测到待处理中断，硬件自动完成以下动作：将当前程序上下文全部压入内核栈保存，切换至内核态，从CPU内部的中断描述符表寄存器（IDTR）中读取操作系统提前注册的中断向量表地址，根据中断号找到对应中断处理程序的入口地址，自动将PC寄存器的值修改为该入口地址。
6. **中断处理与流程收尾**：CPU开始执行内核中断处理程序的二进制机器指令，完成操作结果确认、状态更新、数据校验等收尾工作；中断处理完成后，CPU执行中断返回硬件指令，硬件自动恢复之前保存的程序上下文，切回原特权级，PC寄存器跳回被中断的指令地址，CPU继续执行原流程的机器指令。

### 四、操作系统：CPU硬件管控的指令集合

操作系统（Windows、Linux、macOS、Android等）的本体，是一套提前编译完成、体量庞大的二进制机器指令集合，其所有功能的底层，都是符合对应CPU指令集规范的机器指令。**操作系统本身无任何主动执行能力，所有硬件管控、系统调度功能，都必须由CPU执行其对应的机器指令才能实现**，没有CPU的指令执行，操作系统只是磁盘/内存中静态的二进制数据，无任何管控能力。

操作系统的核心定位，是计算机体系中**唯一持有CPU最高特权级、可执行所有硬件控制指令的指令集合**。日常使用的浏览器、解释器、游戏、办公软件等所有用户态应用，对应的机器指令均运行在CPU的最低特权级，无权限执行硬件控制的特权指令，必须通过CPU执行系统调用指令，触发特权级切换后，由CPU执行操作系统内核的机器指令，代为完成硬件操作，无任何例外。

### 完全不需要切换内核态的内存修改（最常见场景）

* 代码里修改局部变量、全局变量、静态变量；
* 读写 `malloc/new`申请的堆内存；
* 读写进程栈上的数据。
* CPU 直接执行用户态的内存写入指令，把数据写入虚拟地址；CPU 内置的 MMU 硬件，通过内核提前建好的页表，自动把虚拟地址翻译成物理内存地址，直接访问内存芯片完成写入。整个过程完全由 CPU 硬件执行，没有操作系统参与，没有上下文切换，开销极低 —— 如果每次改个变量都要进内核，CPU 性能会被上下文切换彻底耗尽，这也是操作系统设计的核心优化逻辑。

只要是修改**当前进程用户空间内、已完成合法映射、有读写权限**的**内存**，全程都在用户态完成，不需要任何系统调用，也不会切换内核态。

这是程序最频繁的操作，比如：

针对程序加载的核心场景，全底层严谨表述如下：
应用程序的本体，是符合CPU指令集规范的二进制机器指令集合，平时以二进制可执行文件的形式持久化保存在磁盘中。所谓的"程序启动加载"，本质是：CPU执行当前界面程序的用户态机器指令，执行到系统调用硬件指令，触发特权级切换至内核态；CPU随即执行操作系统内核预编译的机器指令，完成程序启动的权限校验、进程虚拟地址空间分配、运行环境初始化，同时执行磁盘驱动对应的机器指令，通过总线向磁盘控制器下发读命令，将磁盘中应用程序的二进制机器指令与数据，读取并加载到内存的指定地址；加载完成后，CPU修改PC寄存器的值，使其指向应用程序入口指令的内存地址，CPU随即开始执行应用程序的机器指令，程序正式启动运行。

包括内存分配、硬件调度、进程管理、文件系统操作在内的所有操作系统核心功能，底层100%通过**CPU执行操作系统内核预编译的二进制机器指令**实现，无任何例外。

### 五、编译器与解释器：高级语言到机器指令的翻译指令集合

开发者手写的C、C++、Java、Python、JavaScript等高级语言代码，是面向人类可读的语法格式，并非CPU可直接识别执行的二进制机器指令，必须经过翻译转换为符合CPU指令集规范的机器指令，才能被CPU执行。

必须明确的底层规则：**编译器与解释器本身，都是提前编译完成的二进制机器指令集合，无任何主动执行能力；所谓的"翻译动作"，物理执行主体是CPU，本质是CPU执行编译器/解释器的机器指令，完成高级语言代码到机器指令的转换**。

二者的底层运行逻辑，有明确的硬件执行层面区分：

#### 编译器：提前全量翻译，一次编译多次执行

C、C++、Go等编译型语言对应的编译器，其本体是二进制机器指令集合。所谓的"编译过程"，本质是：CPU执行编译器的二进制机器指令，将开发者编写的完整高级语言源代码，一次性全量解析、优化、翻译为符合目标CPU指令集规范的二进制机器码文件（Windows的.exe可执行文件、Linux的ELF可执行文件）；翻译完成后，该机器码文件以二进制形式持久化保存在磁盘中，后续运行时，只需将该文件加载到内存，CPU即可直接执行其中的机器指令，运行全程无需编译器再次参与。

#### 解释器：边执行边翻译，逐行解码逐行运行

Python、JavaScript等解释型语言对应的解释器，其本体是二进制机器指令集合。解释器不会提前完成全量代码的翻译，所谓的"解释执行过程"，本质是：CPU先执行内存中解释器的二进制机器指令，逐行读取开发者编写的高级语言源代码，实时解析、翻译为一句一句符合CPU指令集规范的二进制机器指令，写入内存指定地址；随后CPU修改PC寄存器的值，跳转到翻译完成的机器指令地址，逐行执行这些指令，得到二进制计算结果后，再交回给解释器的指令流程进行后续处理。

最终，若需要将计算结果展示给用户，本质是：CPU 执行解释器的用户态机器指令，执行到系统调用硬件指令，触发特权级切换至内核态；CPU 随即执行操作系统内核、显卡驱动的二进制机器指令，完成显示权限校验、原始二进制数据整理，通过 MMIO 内存映射机制，将渲染指令与数据打包写入 GPU 的命令队列，并通过写入门铃寄存器的硬件操作，经由片上 / PCIe 总线向 GPU 发起任务通知；GPU收到指令后，独立完成二进制计算结果到像素画面的渲染转换，生成完整帧存入显存 ，再通过自身内置的硬件显示控制器，将帧数据转换为屏幕可识别的标准视频电信号，直接输出给屏幕自带的专用驱动芯片；最终由屏幕驱动芯片解析视频时序信号，将二进制像素数据转换为驱动电平，逐点驱动屏幕像素点亮，输出为人类可读的文字、图像、数字等可视化内容，完成从 CPU 二进制运算到用户视觉呈现的完整结果输出闭环。

### 六、高级语言代码运行的完整底层全流程

以运行一个开发者手写的高级语言代码文件为例，将上述所有底层逻辑串联为完整的、无断点的、完全符合硬件物理事实的全流程，严格遵循所有底层核心规则：

1. 开发者手写的高级语言代码文件，通过字符编码规则转换为二进制数据，以纯二进制文本的形式，持久化保存在磁盘中，断电后数据不丢失。
2. 开发者触发代码运行操作，CPU逐行执行当前界面程序的用户态二进制机器指令，完成运行请求的参数准备，随后执行系统调用硬件指令；CPU硬件自动完成用户态→内核态的特权级切换，PC寄存器自动跳转到操作系统内核的系统调用入口地址。
3. CPU开始执行操作系统内核预编译的二进制机器指令，完成运行请求的权限校验、运行环境初始化、内存区域划分，随后执行磁盘驱动对应的机器指令，通过系统总线向磁盘控制器下发读命令，将磁盘中对应的解释器/编译器可执行文件、开发者手写的代码文件，读取并完整加载到内存的指定区域。
4. CPU修改PC寄存器的值，使其指向解释器/编译器的入口指令地址，开始执行解释器/编译器的二进制机器指令，完成翻译工作：
   - 若为编译型语言：CPU执行编译器的机器指令，一次性将内存中完整的高级语言代码，全量解析、优化、翻译为符合当前CPU指令集规范的二进制机器码，写入内存的用户程序区域；
   - 若为解释型语言：CPU执行解释器的机器指令，逐行读取内存中的高级语言代码，实时解析、翻译为对应的二进制机器指令，逐行写入内存的用户程序区域。
5. CPU修改PC寄存器的值，使其指向内存用户程序区域中翻译完成的机器指令入口地址，以时钟频率对应的极高速度，无限循环「取指→译码→执行→写回」的固定硬件流程，完成代码对应的算术运算、逻辑运算、流程控制等操作，将计算得到的二进制结果，写回内存的指定区域。
6. 若代码运行过程中需要修改并持久化保存数据：CPU执行用户程序的机器指令，执行到系统调用硬件指令，触发特权级切换至内核态；CPU随即执行操作系统内核的机器指令，生成对应磁盘写入操作的指令序列并执行，通过系统总线向磁盘控制器下发二进制写命令，将内存中修改后的二进制数据，传输至磁盘控制器并写入磁盘的对应物理地址，完成数据的持久化修改。
7. 若代码运行完成后需要向用户展示结果：CPU执行用户程序的机器指令，执行到系统调用硬件指令，触发特权级切换至内核态；CPU随即执行操作系统内核、显示驱动与界面程序的二进制机器指令，将内存中的二进制计算结果，转换为屏幕可识别的视频电信号，最终输出为人类可读的文字、图像、数字等可视化内容，完成最终的结果输出。

### 12. 32 位和 64 位系统是什么意思？

我们的程序和数据都是存储在内存，存储的区域是线性的。
在计算机数据存储中，存储数据的基本单位是字节（byte），1 字节等于 8 位（8 bit）

**32 位和 64 位，主要指 CPU 架构、操作系统和程序使用的位宽体系。**

它主要影响两方面：

主要是*通用寄存器* ，用来存放需要进行运算的数据，比如需要进行加和运算的两个数据，它一次能多少位数的二进制数，32位，就只能最大放入32位的数，有俩个超过32位的数，就不能放入寄存器，也就被cpu利用不了，也就计算不了，结果超过32位也不行。但是实际上cpu会把超过位次的多次分批放入，或者放入多个寄存器，来进行计算，所以也是可以计算超过32位的数据的。

## 一、CPU 读取内存时如何配合

假设地址 `0x1000` 中保存的数据是 `25`。

### 第一步：地址总线指定位置

CPU 把地址：

<pre class="overflow-visible! px-0!" data-start="182" data-end="200"><div class="relative w-full mt-4 mb-1"><div class=""><div class="contents"><div class="relative"><div class="h-full min-h-0 min-w-0"><div class="h-full min-h-0 min-w-0"><div class="border border-token-border-light border-radius-3xl corner-superellipse/1.1 rounded-3xl"><div class="h-full w-full border-radius-3xl bg-(--code-block-surface) corner-superellipse/1.1 overflow-clip rounded-3xl [--code-block-surface:var(--bg-elevated-secondary)] dark:[--code-block-surface:var(--composer-surface-primary)] lxnfua_clipPathFallback"><div class="pointer-events-none absolute end-1.5 top-1 z-2 md:end-2 md:top-1"></div><div class="relative"><div class="pe-11 pt-3"><div class="relative z-0 flex max-w-full"><div id="code-block-viewer" dir="ltr" class="q9tKkq_viewer cm-editor z-10 light:cm-light dark:cm-light flex h-full w-full flex-col items-stretch ͼd ͼr"><div class="cm-scroller"><pre class="cm-content q9tKkq_readonly m-0"><code><span>0x1000</span></code></pre></div></div></div></div></div></div></div></div></div><div class=""><div class=""></div></div></div></div></div></div></pre>

放到地址总线上。

内存控制器看到这个地址后，判断这个地址对应哪一个内存单元。

此时地址总线表达的是：

<pre class="overflow-visible! px-0!" data-start="256" data-end="279"><div class="relative w-full mt-4 mb-1"><div class=""><div class="contents"><div class="relative"><div class="h-full min-h-0 min-w-0"><div class="h-full min-h-0 min-w-0"><div class="border border-token-border-light border-radius-3xl corner-superellipse/1.1 rounded-3xl"><div class="h-full w-full border-radius-3xl bg-(--code-block-surface) corner-superellipse/1.1 overflow-clip rounded-3xl [--code-block-surface:var(--bg-elevated-secondary)] dark:[--code-block-surface:var(--composer-surface-primary)] lxnfua_clipPathFallback"><div class="pointer-events-none absolute end-1.5 top-1 z-2 md:end-2 md:top-1"></div><div class="relative"><div class="pe-11 pt-3"><div class="relative z-0 flex max-w-full"><div id="code-block-viewer" dir="ltr" class="q9tKkq_viewer cm-editor z-10 light:cm-light dark:cm-light flex h-full w-full flex-col items-stretch ͼd ͼr"><div class="cm-scroller"><pre class="cm-content q9tKkq_readonly m-0"><code><span>我要访问 0x1000</span></code></pre></div></div></div></div></div></div></div></div></div><div class=""><div class=""></div></div></div></div></div></div></pre>

### 第二步：控制总线说明操作类型

CPU 在控制总线上发出“读”信号，例如：

<pre class="overflow-visible! px-0!" data-start="324" data-end="368"><div class="relative w-full mt-4 mb-1"><div class=""><div class="contents"><div class="relative"><div class="h-full min-h-0 min-w-0"><div class="h-full min-h-0 min-w-0"><div class="border border-token-border-light border-radius-3xl corner-superellipse/1.1 rounded-3xl"><div class="h-full w-full border-radius-3xl bg-(--code-block-surface) corner-superellipse/1.1 overflow-clip rounded-3xl [--code-block-surface:var(--bg-elevated-secondary)] dark:[--code-block-surface:var(--composer-surface-primary)] lxnfua_clipPathFallback"><div class="pointer-events-none absolute end-1.5 top-1 z-2 md:end-2 md:top-1"></div><div class="relative"><div class="pe-11 pt-3"><div class="relative z-0 flex max-w-full"><div id="code-block-viewer" dir="ltr" class="q9tKkq_viewer cm-editor z-10 light:cm-light dark:cm-light flex h-full w-full flex-col items-stretch ͼd ͼr"><div class="cm-scroller"><pre class="cm-content q9tKkq_readonly m-0"><code><span>Memory Read = 1
Memory Write = 0</span></code></pre></div></div></div></div></div></div></div></div></div><div class=""><div class=""></div></div></div></div></div></div></pre>

它告诉内存：

<pre class="overflow-visible! px-0!" data-start="378" data-end="404"><div class="relative w-full mt-4 mb-1"><div class=""><div class="contents"><div class="relative"><div class="h-full min-h-0 min-w-0"><div class="h-full min-h-0 min-w-0"><div class="border border-token-border-light border-radius-3xl corner-superellipse/1.1 rounded-3xl"><div class="h-full w-full border-radius-3xl bg-(--code-block-surface) corner-superellipse/1.1 overflow-clip rounded-3xl [--code-block-surface:var(--bg-elevated-secondary)] dark:[--code-block-surface:var(--composer-surface-primary)] lxnfua_clipPathFallback"><div class="pointer-events-none absolute end-1.5 top-1 z-2 md:end-2 md:top-1"></div><div class="relative"><div class="pe-11 pt-3"><div class="relative z-0 flex max-w-full"><div id="code-block-viewer" dir="ltr" class="q9tKkq_viewer cm-editor z-10 light:cm-light dark:cm-light flex h-full w-full flex-col items-stretch ͼd ͼr"><div class="cm-scroller"><pre class="cm-content q9tKkq_readonly m-0"><code><span>我要读取这个地址，不是修改它</span></code></pre></div></div></div></div></div></div></div></div></div><div class=""><div class=""></div></div></div></div></div></div></pre>

### 第三步：内存通过数据总线返回数据

内存找到 `0x1000` 中保存的数据 `25`，然后把 `25` 放到数据总线上。

CPU 再从数据总线上接收这个数据。

整个过程可以表示为：

<pre class="overflow-visible! px-0!" data-start="505" data-end="585"><div class="relative w-full mt-4 mb-1"><div class=""><div class="contents"><div class="relative"><div class="h-full min-h-0 min-w-0"><div class="h-full min-h-0 min-w-0"><div class="border border-token-border-light border-radius-3xl corner-superellipse/1.1 rounded-3xl"><div class="h-full w-full border-radius-3xl bg-(--code-block-surface) corner-superellipse/1.1 overflow-clip rounded-3xl [--code-block-surface:var(--bg-elevated-secondary)] dark:[--code-block-surface:var(--composer-surface-primary)] lxnfua_clipPathFallback"><div class="pointer-events-none absolute end-1.5 top-1 z-2 md:end-2 md:top-1"></div><div class="relative"><div class="pe-11 pt-3"><div class="relative z-0 flex max-w-full"><div id="code-block-viewer" dir="ltr" class="q9tKkq_viewer cm-editor z-10 light:cm-light dark:cm-light flex h-full w-full flex-col items-stretch ͼd ͼr"><div class="cm-scroller"><pre class="cm-content q9tKkq_readonly m-0"><code><span>CPU ──地址总线──> 内存：地址 0x1000
CPU ──控制总线──> 内存：读
CPU <──数据总线── 内存：数据 25</span></code></pre></div></div></div></div></div></div></div></div></div><div class=""><div class=""></div></div></div></div></div></div></pre>

注意，读取时数据流向通常是：

<pre class="overflow-visible! px-0!" data-start="603" data-end="623"><div class="relative w-full mt-4 mb-1"><div class=""><div class="contents"><div class="relative"><div class="h-full min-h-0 min-w-0"><div class="h-full min-h-0 min-w-0"><div class="border border-token-border-light border-radius-3xl corner-superellipse/1.1 rounded-3xl"><div class="h-full w-full border-radius-3xl bg-(--code-block-surface) corner-superellipse/1.1 overflow-clip rounded-3xl [--code-block-surface:var(--bg-elevated-secondary)] dark:[--code-block-surface:var(--composer-surface-primary)] lxnfua_clipPathFallback"><div class="pointer-events-none absolute end-1.5 top-1 z-2 md:end-2 md:top-1"></div><div class="relative"><div class="pe-11 pt-3"><div class="relative z-0 flex max-w-full"><div id="code-block-viewer" dir="ltr" class="q9tKkq_viewer cm-editor z-10 light:cm-light dark:cm-light flex h-full w-full flex-col items-stretch ͼd ͼr"><div class="cm-scroller"><pre class="cm-content q9tKkq_readonly m-0"><code><span>内存 → CPU</span></code></pre></div></div></div></div></div></div></div></div></div><div class=""><div class=""></div></div></div></div></div></div></pre>

---

## 二、CPU 写入内存时如何配合

假设 CPU 要把数字 `80` 写入地址 `0x1000`。

### 第一步：地址总线给出地址

<pre class="overflow-visible! px-0!" data-start="701" data-end="724"><div class="relative w-full mt-4 mb-1"><div class=""><div class="contents"><div class="relative"><div class="h-full min-h-0 min-w-0"><div class="h-full min-h-0 min-w-0"><div class="border border-token-border-light border-radius-3xl corner-superellipse/1.1 rounded-3xl"><div class="h-full w-full border-radius-3xl bg-(--code-block-surface) corner-superellipse/1.1 overflow-clip rounded-3xl [--code-block-surface:var(--bg-elevated-secondary)] dark:[--code-block-surface:var(--composer-surface-primary)] lxnfua_clipPathFallback"><div class="pointer-events-none absolute end-1.5 top-1 z-2 md:end-2 md:top-1"></div><div class="relative"><div class="pe-11 pt-3"><div class="relative z-0 flex max-w-full"><div id="code-block-viewer" dir="ltr" class="q9tKkq_viewer cm-editor z-10 light:cm-light dark:cm-light flex h-full w-full flex-col items-stretch ͼd ͼr"><div class="cm-scroller"><pre class="cm-content q9tKkq_readonly m-0"><code><span>地址总线：0x1000</span></code></pre></div></div></div></div></div></div></div></div></div><div class=""><div class=""></div></div></div></div></div></div></pre>

表示要操作地址 `0x1000`。

### 第二步：数据总线放入数据

<pre class="overflow-visible! px-0!" data-start="763" data-end="782"><div class="relative w-full mt-4 mb-1"><div class=""><div class="contents"><div class="relative"><div class="h-full min-h-0 min-w-0"><div class="h-full min-h-0 min-w-0"><div class="border border-token-border-light border-radius-3xl corner-superellipse/1.1 rounded-3xl"><div class="h-full w-full border-radius-3xl bg-(--code-block-surface) corner-superellipse/1.1 overflow-clip rounded-3xl [--code-block-surface:var(--bg-elevated-secondary)] dark:[--code-block-surface:var(--composer-surface-primary)] lxnfua_clipPathFallback"><div class="pointer-events-none absolute end-1.5 top-1 z-2 md:end-2 md:top-1"></div><div class="relative"><div class="pe-11 pt-3"><div class="relative z-0 flex max-w-full"><div id="code-block-viewer" dir="ltr" class="q9tKkq_viewer cm-editor z-10 light:cm-light dark:cm-light flex h-full w-full flex-col items-stretch ͼd ͼr"><div class="cm-scroller"><pre class="cm-content q9tKkq_readonly m-0"><code><span>数据总线：80</span></code></pre></div></div></div></div></div></div></div></div></div><div class=""><div class=""></div></div></div></div></div></div></pre>

表示准备写入的数据是 `80`。

### 第三步：控制总线发出写命令

<pre class="overflow-visible! px-0!" data-start="821" data-end="839"><div class="relative w-full mt-4 mb-1"><div class=""><div class="contents"><div class="relative"><div class="h-full min-h-0 min-w-0"><div class="h-full min-h-0 min-w-0"><div class="border border-token-border-light border-radius-3xl corner-superellipse/1.1 rounded-3xl"><div class="h-full w-full border-radius-3xl bg-(--code-block-surface) corner-superellipse/1.1 overflow-clip rounded-3xl [--code-block-surface:var(--bg-elevated-secondary)] dark:[--code-block-surface:var(--composer-surface-primary)] lxnfua_clipPathFallback"><div class="pointer-events-none absolute end-1.5 top-1 z-2 md:end-2 md:top-1"></div><div class="relative"><div class="pe-11 pt-3"><div class="relative z-0 flex max-w-full"><div id="code-block-viewer" dir="ltr" class="q9tKkq_viewer cm-editor z-10 light:cm-light dark:cm-light flex h-full w-full flex-col items-stretch ͼd ͼr"><div class="cm-scroller"><pre class="cm-content q9tKkq_readonly m-0"><code><span>控制总线：写</span></code></pre></div></div></div></div></div></div></div></div></div><div class=""><div class=""></div></div></div></div></div></div></pre>

内存收到写信号后，将数据总线上的 `80` 保存到地址 `0x1000`。

过程可以表示为：

<pre class="overflow-visible! px-0!" data-start="890" data-end="970"><div class="relative w-full mt-4 mb-1"><div class=""><div class="contents"><div class="relative"><div class="h-full min-h-0 min-w-0"><div class="h-full min-h-0 min-w-0"><div class="border border-token-border-light border-radius-3xl corner-superellipse/1.1 rounded-3xl"><div class="h-full w-full border-radius-3xl bg-(--code-block-surface) corner-superellipse/1.1 overflow-clip rounded-3xl [--code-block-surface:var(--bg-elevated-secondary)] dark:[--code-block-surface:var(--composer-surface-primary)] lxnfua_clipPathFallback"><div class="pointer-events-none absolute end-1.5 top-1 z-2 md:end-2 md:top-1"></div><div class="relative"><div class="pe-11 pt-3"><div class="relative z-0 flex max-w-full"><div id="code-block-viewer" dir="ltr" class="q9tKkq_viewer cm-editor z-10 light:cm-light dark:cm-light flex h-full w-full flex-col items-stretch ͼd ͼr"><div class="cm-scroller"><pre class="cm-content q9tKkq_readonly m-0"><code><span>CPU ──地址总线──> 内存：地址 0x1000
CPU ──数据总线──> 内存：数据 80
CPU ──控制总线──> 内存：写</span></code></pre></div></div></div></div></div></div></div></div></div><div class=""><div class=""></div></div></div></div></div></div></pre>

虽然解释时分成三步，但在实际硬件中，这些信号经常会有时间重叠，只要满足规定的时序即可。

那 CPU 执行程序的过程如下：
• 第一步，CPU 读取「程序计数器」的值，这个值是指令的内存地址，然后 CPU 的「控制单元」操作「地址总线」指定需要访问的内存地址，接着通知内存设备准备数据，数据准备好后通过「数据总线」将指令数据传给 CPU，CPU 收到内存传来的数据后，将这个指令数据存入到「指令寄存器」。
• 第二步，「程序计数器」的值自增，表示指向下一条指令。这个自增的大小，由 CPU 的位宽决定，比如 32 位的 CPU，指令是 4 个字节，需要 4 个内存地址存放，因此「程序计数器」的值会自增 4；
• 第三步，CPU 分析「指令寄存器」中的指令，确定指令的类型和参数，如果是计算类型的指令，就把指令交给「逻辑运算单元」运算；如果是存储类型的指令，则交由「控制单元」执行；

## . 程序计数器本质上是什么

程序计数器是 CPU 内部的一个特殊寄存器，里面保存的是：

> 下一条准备执行的指令所在的内存地址。

例如，内存中有以下指令：

<pre class="overflow-visible! px-0!" data-start="240" data-end="299"><div class="relative w-full mt-4 mb-1"><div class=""><div class="contents"><div class="relative"><div class="h-full min-h-0 min-w-0"><div class="h-full min-h-0 min-w-0"><div class="border border-token-border-light border-radius-3xl corner-superellipse/1.1 rounded-3xl"><div class="h-full w-full border-radius-3xl bg-(--code-block-surface) corner-superellipse/1.1 overflow-clip rounded-3xl [--code-block-surface:var(--bg-elevated-secondary)] dark:[--code-block-surface:var(--composer-surface-primary)] lxnfua_clipPathFallback"><div class="pointer-events-none absolute end-1.5 top-1 z-2 md:end-2 md:top-1"></div><div class="relative"><div class="pe-11 pt-3"><div class="relative z-0 flex max-w-full"><div id="code-block-viewer" dir="ltr" class="q9tKkq_viewer cm-editor z-10 light:cm-light dark:cm-light flex h-full w-full flex-col items-stretch ͼd ͼr"><div class="cm-scroller"><pre class="cm-content q9tKkq_readonly m-0"><code><span>地址 0x1000：第1条指令
地址 0x1004：第2条指令
地址 0x1008：第3条指令</span></code></pre></div></div></div></div></div></div></div></div></div><div class=""><div class=""></div></div></div></div></div></div></pre>

此时程序计数器可能保存：

<pre class="overflow-visible! px-0!" data-start="315" data-end="338"><div class="relative w-full mt-4 mb-1"><div class=""><div class="contents"><div class="relative"><div class="h-full min-h-0 min-w-0"><div class="h-full min-h-0 min-w-0"><div class="border border-token-border-light border-radius-3xl corner-superellipse/1.1 rounded-3xl"><div class="h-full w-full border-radius-3xl bg-(--code-block-surface) corner-superellipse/1.1 overflow-clip rounded-3xl [--code-block-surface:var(--bg-elevated-secondary)] dark:[--code-block-surface:var(--composer-surface-primary)] lxnfua_clipPathFallback"><div class="pointer-events-none absolute end-1.5 top-1 z-2 md:end-2 md:top-1"></div><div class="relative"><div class="pe-11 pt-3"><div class="relative z-0 flex max-w-full"><div id="code-block-viewer" dir="ltr" class="q9tKkq_viewer cm-editor z-10 light:cm-light dark:cm-light flex h-full w-full flex-col items-stretch ͼd ͼr"><div class="cm-scroller"><pre class="cm-content q9tKkq_readonly m-0"><code><span>PC = 0x1000</span></code></pre></div></div></div></div></div></div></div></div></div><div class=""><div class=""></div></div></div></div></div></div></pre>

CPU 根据 PC 中的地址读取第 1 条指令。读取完成后，PC 更新为：

<pre class="overflow-visible! px-0!" data-start="379" data-end="402"><div class="relative w-full mt-4 mb-1"><div class=""><div class="contents"><div class="relative"><div class="h-full min-h-0 min-w-0"><div class="h-full min-h-0 min-w-0"><div class="border border-token-border-light border-radius-3xl corner-superellipse/1.1 rounded-3xl"><div class="h-full w-full border-radius-3xl bg-(--code-block-surface) corner-superellipse/1.1 overflow-clip rounded-3xl [--code-block-surface:var(--bg-elevated-secondary)] dark:[--code-block-surface:var(--composer-surface-primary)] lxnfua_clipPathFallback"><div class="pointer-events-none absolute end-1.5 top-1 z-2 md:end-2 md:top-1"></div><div class="relative"><div class="pe-11 pt-3"><div class="relative z-0 flex max-w-full"><div id="code-block-viewer" dir="ltr" class="q9tKkq_viewer cm-editor z-10 light:cm-light dark:cm-light flex h-full w-full flex-col items-stretch ͼd ͼr"><div class="cm-scroller"><pre class="cm-content q9tKkq_readonly m-0"><code><span>PC = 0x1004</span></code></pre></div></div></div></div></div></div></div></div></div><div class=""><div class=""></div></div></div></div></div></div></pre>

下一次就去读取地址 `0x1004` 中的指令。

所以，PC 可以理解为一个“书签”，记录程序当前读到哪里。

1. 当前指令取出后，不再依赖 PC

假设：

PC = 0x1000

地址 0x1000 中存放一条指令：

ADD R1, R2

CPU 取指令时：

PC中的0x1000
       ↓
从内存读取ADD指令
       ↓
指令保存到指令寄存器IR

此时：

IR = ADD R1, R2

指令已经进入 CPU 内部，后续译码和执行都根据 IR 中的内容进行，不需要 PC 继续保存 0x1000。

所以 PC 可以更新为下一条指令地址：

PC = 0x1004

这时 CPU 内部同时存在：

IR：当前正在执行的指令 ADD R1, R2
PC：下一条指令的地址 0x1004

二者职责不同。

# 时钟周期（简要解析）

1. **定义** ：数字电路 / CPU 中，时钟信号完成一次完整高低电平振荡所用的时间，是系统最基础的 **最小时间单位** 。

有寄存器仍然需要缓存，是因为二者解决的问题不同：

> **寄存器负责保存 CPU 当前正在计算的少量数据；缓存负责保存 CPU 很可能马上还会用到的大量指令和数据。**

它们都比内存快，但容量、用途和管理方式不同。

## 1. 寄存器为什么不能直接代替缓存

寄存器位于 CPU 核心内部，直接连接运算单元。例如执行：

```c
c = a + b;
```

CPU 可能把它转换为类似过程：

```text
寄存器 R1 ← a
寄存器 R2 ← b
寄存器 R3 ← R1 + R2
```

真正进行加法时，操作数通常必须已经在寄存器中。

但是寄存器数量非常少。例如一颗 CPU 可能只有几十个通用寄存器。即使每个寄存器是 64 位，能直接保存的数据总量也只有几百字节。

而一个程序可能包含：

* 几十 KB 甚至几 MB 的机器指令；
* 大量数组、对象和变量；
* 操作系统和其他程序的数据。

不可能把整个程序都放进寄存器。

寄存器不能无限增加，主要有几个原因：

1. 寄存器必须离运算单元非常近；
2. 每条指令都要快速选择和访问寄存器；
3. 寄存器数量越多，寻址、布线和控制越复杂；
4. 寄存器过多会增加面积、功耗，并降低访问速度。

因此，寄存器适合保存“此刻正在算的内容”，而不适合承担大容量存储。

---

## 2. 为什么不能每次都直接从内存读取

CPU 的运算速度远高于主内存的访问速度。

假设 CPU 执行一条加法只需要很短时间，但读取内存中的数据可能需要等待几十乃至数百个时钟周期。若每条指令都直接访问内存，CPU 大部分时间都会处于等待状态：

```text
CPU：我要数据
内存：正在找
CPU：继续等待
内存：数据来了
CPU：完成一次计算
```

因此，在寄存器和内存之间增加一层较快的存储：

```text
寄存器
   ↓
CPU Cache
   ↓
主内存
```

缓存相当于把近期可能使用的数据，提前放在 CPU 附近。

---

## 3. 寄存器、缓存和内存分别存什么

可以用下面的关系理解：

| 部件      | 主要存放内容                           | 谁管理               | 特点               |
| --------- | -------------------------------------- | -------------------- | ------------------ |
| 寄存器    | 当前指令正在使用的数据、地址和计算结果 | 指令和编译器直接管理 | 最快、最少         |
| CPU Cache | 最近使用或预计即将使用的指令和数据副本 | 主要由硬件自动管理   | 较快、容量中等     |
| 主内存    | 正在运行的程序、程序数据、操作系统数据 | 操作系统和程序管理   | 容量大、速度较慢   |
| 硬盘/SSD  | 文件、程序、长期数据                   | 文件系统管理         | 容量最大、速度更慢 |

例如程序：

```c
int sum = 0;

for (int i = 0; i < 1000; i++) {
    sum += array[i];
}
```

执行时可能出现：

* `i` 和 `sum` 当前值主要放在寄存器中；
* `array` 当前附近的一小段数据放在缓存中；
* 完整的 `array` 存放在主内存中；
* 程序没有运行时，文件存放在 SSD 中。

缓存存的不是一种特殊数据，而主要是：

> 主内存中部分内容的临时副本。

其中既可能是程序指令，也可能是普通数据。

---

## 4. CPU 如何使用缓存读取数据

假设 CPU 需要读取内存地址 `0x1000` 的数据。

CPU 不会一开始就直接去主内存，而是按照层级查询：

```text
先找 L1
  ↓ 没找到
再找 L2
  ↓ 没找到
再找 L3
  ↓ 没找到
最后访问主内存
```

如果在某一级找到，叫作：

> 缓存命中，Cache Hit。

如果没有找到，叫作：

> 缓存未命中，Cache Miss。

例如：

```text
CPU需要地址0x1000的数据
        ↓
L1没有
        ↓
L2找到了
        ↓
数据送入寄存器
        ↓
CPU开始计算
```

数据从 L2 送给 CPU 的同时，通常还会复制一份到 L1，因为 CPU 很可能很快再次使用它。

---

## 5. 为什么需要 L1、L2、L3 三级缓存

因为存储器存在一个基本矛盾：

> 容量越大，通常越难做到极低延迟；速度越快，通常越昂贵、越占芯片面积。

无法制造一个既像寄存器一样快、又像内存一样大的存储器，所以采用分层结构。

大致关系如下：

```text
            速度更快，容量更小
                    ↑
                寄存器
                    ↑
                  L1
                    ↑
                  L2
                    ↑
                  L3
                    ↑
                 主内存
                    ↑
                 SSD
            容量更大，速度更慢
```

三级缓存是在速度、容量、面积和功耗之间进行折中。

---

## 6. L1 缓存存什么

L1 是最接近 CPU 执行单元的缓存，容量最小，访问最快。

L1 通常会分成两部分：

### L1 指令缓存

简称 L1I，存放即将执行的机器指令。

例如：

```text
ADD R1, R2
LOAD R3, [R4]
JUMP 0x2000
```

CPU 取指令时，优先从 L1 指令缓存获取。

### L1 数据缓存

简称 L1D，存放当前程序近期使用的数据。

例如：

* 数组中的元素；
* 函数局部变量；
* 对象字段；
* 栈中的数据。

L1 分成指令缓存和数据缓存，可以让 CPU 同时：

* 读取下一条指令；
* 读取当前指令所需的数据。

例如：

```text
L1I → 提供 ADD 指令
L1D → 提供需要相加的数据
```

二者可以并行工作。

---

## 7. L2 缓存存什么

L2 通常比 L1 大，但访问速度稍慢。

它同样保存指令和数据的副本，一般不再区分成独立的指令缓存和数据缓存。

可以理解为：

> L2 是 L1 的后备仓库。

当 L1 中没有找到数据时，CPU 会去 L2 查找。

例如：

```text
L1D中只有当前常用的少量数组数据
L2中保存更大范围的数组数据和程序指令
```

在现代多核 CPU 中，L2 通常是每个核心独立拥有，也有例外。

---

## 8. L3 缓存存什么

L3 比 L2 更大，但速度进一步降低。它通常由多个 CPU 核心共享。

例如四核 CPU 可以简化为：

```text
核心1：寄存器 + L1 + L2
核心2：寄存器 + L1 + L2
核心3：寄存器 + L1 + L2
核心4：寄存器 + L1 + L2
              ↓
          共享 L3
              ↓
            内存
```

L3 主要有两个作用：

第一，减少访问主内存的次数。一个核心的 L1、L2 没有数据时，可能在 L3 找到。

第二，帮助不同 CPU 核心共享数据。例如核心 1 刚刚使用过某段数据，核心 2 随后也需要使用，可能可以通过共享缓存和缓存一致性机制获得，而不必重新访问主内存。

但具体的 L3 组织方式因 CPU 架构不同而不同。

---

## 9. 为什么不用一个很大的 L1

因为缓存越大，查找和访问通常越慢。

假设设计一个几十 MB 的 L1 缓存，它需要：

* 更长的内部连线；
* 更复杂的地址比较电路；
* 更大的芯片面积；
* 更高功耗；
* 更长的访问时间。

这样它就无法继续充当“极快的一级缓存”。

因此采用：

```text
很小但极快的 L1
较大但稍慢的 L2
更大但更慢的 L3
```

大多数访问在 L1 命中，少数去 L2，更少去 L3，只有极少部分真正访问内存。

这样获得的平均访问时间，比直接使用一个巨大但较慢的缓存更合适。

---

## 10. 缓存为什么有效

缓存有效主要依赖程序访问数据的两个规律。

### 时间局部性

一个数据刚刚被使用，很可能很快再次被使用。

例如循环中的变量：

```c
for (int i = 0; i < 1000; i++) {
    sum += array[i];
}
```

变量 `sum` 和 `i` 会被反复使用，因此适合留在寄存器或缓存中。

### 空间局部性

程序访问一个地址后，很可能继续访问附近地址。

例如数组：

```c
array[0]
array[1]
array[2]
array[3]
```

这些元素在内存中通常连续存放。

因此 CPU 读取 `array[0]` 时，不只读取这一个元素，而是把附近一整块数据一起放进缓存，这一块通常称为：

> 缓存行，Cache Line。

现代 CPU 常见缓存行大小是 64 字节，但具体取决于架构。

假设每个 `int` 占 4 字节，一条 64 字节缓存行可以装下：

[
64 \div 4=16
]

个整数。

读取 `array[0]` 时，可能同时把 `array[0]` 到 `array[15]` 放进缓存。随后读取后面的元素时，就不需要再次访问内存。

---

## 11. 数据最终如何进入寄存器

CPU 不能直接拿缓存里的数据做普通 ALU 运算。通常需要先通过加载指令把数据送入寄存器。

例如：

```text
LOAD R1, [地址A]
LOAD R2, [地址B]
ADD  R3, R1, R2
```

执行第一条加载指令时：

```text
查 L1
 ↓
命中：直接把数据送入 R1
```

若 L1 未命中：

```text
L1没有
 ↓
查询L2
 ↓
查询L3
 ↓
查询主内存
 ↓
数据逐级进入缓存
 ↓
最终进入R1
```

因此，运算路径是：

```text
主内存中的数据
       ↓
     Cache
       ↓
     寄存器
       ↓
      ALU
       ↓
     寄存器
```

---

## 12. 一个完整例子

假设执行：

```c
c = a + b;
```

变量位于内存中：

```text
地址 0x2000：a = 10
地址 0x2004：b = 20
地址 0x2008：c
```

CPU 执行过程可以简化为：

```text
第一步：取出“读取a”的指令
指令优先从L1指令缓存获取

第二步：读取a
先查L1数据缓存
如果命中，a直接进入R1
如果未命中，继续查L2、L3和内存

第三步：读取b
b进入R2

第四步：执行加法
R3 = R1 + R2 = 30

第五步：写入c
先写入缓存，随后根据缓存策略更新内存
```

在这个过程中：

* 寄存器保存当前的 `10`、`20` 和 `30`；
* 缓存保存 `a`、`b`、`c` 附近的一整块内存数据；
* 主内存保存程序完整的数据区域。

---

## 13. 写数据时缓存如何工作

CPU 计算出结果后，往往不是立即等待主内存写完。

常见方式是先把结果写入缓存：

```text
CPU计算结果
    ↓
写入L1缓存
    ↓
CPU继续执行后续指令
    ↓
缓存稍后再更新主内存
```

这称为写回缓存的一种基本思想。

这样 CPU 不必因为内存写入较慢而一直等待。

不过缓存的写入策略有多种，例如：

* 写回，Write Back；
* 直写，Write Through；
* 写分配和不写分配。

具体策略取决于 CPU 架构。

---

## 14. 缓存中保存的是“原始数据”还是“副本”

一般来说，缓存中保存的是主内存内容的副本。

例如：

```text
主内存地址0x2000：10
L3缓存中的副本：10
L2缓存中的副本：10
L1缓存中的副本：10
寄存器R1：10
```

同一个数据可能同时出现在多个层级。

如果 CPU 修改了数据，硬件必须记录哪一份是最新的，并在适当时机更新其他层级。这也是缓存控制和多核缓存一致性比较复杂的原因。

---

## 15. 一个更直观的类比

可以把 CPU 看成正在工作的人员：

* **寄存器**：手里正在拿着的几张纸；
* **L1**：桌面上最常用的材料；
* **L2**：桌边抽屉；
* **L3**：几个人共享的文件柜；
* **主内存**：办公室档案室；
* **SSD**：远处的大型仓库。

计算时必须把数据拿到手里，也就是寄存器中。

但手里不可能拿下整个档案室，所以把常用材料放在桌面和抽屉中。三级缓存的目的，就是尽量减少去档案室取材料的次数。

## 核心结论

寄存器和缓存并不重复：

> **寄存器是 CPU 当前运算的直接工作区；缓存是内存与寄存器之间的高速缓冲区。**

三级缓存的设计目的是同时兼顾速度与容量：

```text
寄存器：当前正在计算的数据
L1：马上要用的数据和指令
L2：近期可能用的数据和指令
L3：多个核心可能使用的更大范围数据
内存：程序运行所需的完整数据
```

越靠近 CPU，速度越快、容量越小；越远离 CPU，容量越大、速度越慢。

## 第一，CPU 能表示多长的内存地址

```text
32 位系统：地址/指针通常用 32 bit 表示
64 位系统：地址/指针通常用 64 bit 表示
```

```c
int x = arr[10];
```

```text
1. 取出 arr 的起始地址
2. 计算 arr[10] 的地址
3. 把这个地址交给内存系统
4. 从该地址读取数据
```

```text
32 位地址最多能表示 2^32 个地址
```

```text
2^32 字节 ≈ 4GB
```

```text
2^64 个地址
```

这里的“地址有多长”，意思是：

**程序中的内存地址，也就是指针，通常用多少个二进制位来表示。**

比如：

CPU 访问内存时，并不是直接“凭空去读地址”，而是要先在 CPU 内部保存、计算和传递地址。

例如程序中有：

CPU 大概要做：

这些地址需要放在 CPU 的寄存器中进行计算。

如果是 32 位地址：

如果一个地址对应 1 字节，那么：

所以 32 位系统通常最多只能直接管理约 4GB 的地址空间。

64 位地址理论上能表示：

这个范围非常大，远远超过普通电脑的需求。不过现实中的 64 位 CPU 不一定真的使用完整 64 位地址，通常只使用其中一部分，但也已经远大于 4GB。

---

## 第二，CPU 一次更自然地处理多少数据

```text
32 位 CPU：通用寄存器通常是 32 位，更自然地处理 32 位整数数据
64 位 CPU：通用寄存器通常是 64 位，更自然地处理 64 位整数数据
```

```text
32 位 / 64 位主要指 CPU 通用寄存器和地址/指针的位宽，
不是所有硬件通路的唯一宽度。
```

32 位和 64 位还会影响 CPU 通用寄存器的宽度。

可以理解为：

例如，32 位 CPU 处理一个 64 位整数时，可能需要拆成多次完成；而 64 位 CPU 可以更自然地一次处理 64 位整数。

不过要注意：

**这不是说 CPU 所有操作都只能处理 32 位或 64 位。**

现代 CPU 还有缓存、总线、SIMD 向量指令等，一次搬运或计算的数据可能超过 64 位。

所以更准确地说：

---

## 对应到指针大小

```text
32 位程序：指针一般占 4 字节
64 位程序：指针一般占 8 字节
```

```text
4 字节 = 32 bit
8 字节 = 64 bit
```

```text
32 位程序：指针一般是 4 字节
64 位程序：指针一般是 8 字节
```

指针本质上存的是内存地址，所以地址位数越长，指针占用空间通常越大。

因为：

但是要注意：

**64 位 CPU 上运行 32 位程序时，指针仍然可能是 4 字节。**

所以更严谨地说：

## 总结

**32 位和 64 位系统，本质上是 CPU、操作系统和程序使用的数据宽度、地址宽度不同。**

32 位系统中，地址/指针通常是 32 位，最多能表示约 4GB 地址空间；64 位系统中，地址/指针通常是 64 位，可以表示更大的地址空间。

同时，64 位 CPU 的通用寄存器通常更宽，处理 64 位整数数据更自然，也更适合大内存和大规模数据处理。

### 内存里到底存的是什么？是不是全是 0 和 1？

```text
bit：一个 0 或 1
```

```text
1 Byte = 8 bit
```

```text
00000011
```

```text
100
101
102
103
```

**问题合并：**
内存里到底存的是什么？数字、节点、指针、数据块到底是什么？是不是全是 0 和 1？

**答案：**
**是的，内存底层存的全是 0 和 1。**

最小单位是：

**但 CPU 通常不会单独读一个 bit，而是按字节读写：**

比如数字 `3`，在内存里可能是：

如果是一个 `int`，通常占 4 个字节，也就是 32 个 bit。

所谓“连续数据块”，就是一段地址连续的字节。
**比如一个 `int` 占 4 字节，如果从地址 100 开始存，就可能占：**

**这 4 个连续字节合起来表示一个整数。**

可以把“CPU 选择线程”理解为：

> **Linux 调度器从当前 CPU 上所有处于可运行状态的 `task_struct` 中，选出一个作为 `next`，然后把 CPU 的执行现场切换到这个任务。**

因此，它的本质不是“把线程本身拿出来运行”，而是：

[
\boxed{\text{选择 task_struct}+\text{切换寄存器等执行上下文}}
]

切换完成后，CPU 才开始继续执行该线程对应的机器指令。

---

# 一、什么叫“选择线程”

假设一台计算机只有一个 CPU 核心，现在有三个线程：

```text
线程 A：浏览器渲染
线程 B：音乐播放
线程 C：文件解压
```

一个 CPU 核心在某个时刻通常只能执行一个线程的指令。例如当前正在执行线程 A：

```text
CPU 当前执行：线程 A
等待运行：线程 B、线程 C
```

过一段时间，Linux 内核可能决定：

```text
暂停线程 A
改为运行线程 B
```

这就是“选择线程”：

> 从多个已经具备运行条件的线程中，决定下一段时间让哪一个线程占用 CPU。

“选择”的直接结果，可以简化为一个指针：

```c
struct task_struct *next;
```

例如：

```text
next = 线程B对应的task_struct
```

Linux 内核的 `__schedule()` 会查看当前 CPU 的本地运行队列，找到最适合下一步运行的任务。内核源码也明确说明，`__schedule()` 会锁定本地 CPU 的运行队列，并从本地运行队列数据结构中寻找下一个最合适的任务。([GitHub][1])

---

# 二、`task_struct` 和线程是什么关系

Linux 内核调度时，不直接处理“Java 线程”“浏览器线程”这些抽象概念，而是处理：

```c
struct task_struct
```

每个可以被 Linux 调度的线程，通常都有一个对应的 `task_struct`。

其中记录或关联了该任务的很多信息，例如：

```text
任务状态
优先级
调度策略
允许运行的 CPU
虚拟运行时间
内核栈
地址空间
打开的文件
信号信息
保存的 CPU 上下文
```

因此，“选择线程”在内核层面更准确地说是：

> 选择一个代表该线程的 `task_struct`。

Linux 源码中的 `include/linux/sched.h` 定义了 `task_struct`，并将其作为调度、唤醒等核心接口处理的任务对象。([GitHub][2])

---

# 三、什么是“可运行任务”

并不是系统中的所有任务都会进入候选范围。

例如：

```text
线程 A：正在使用 CPU               可运行
线程 B：等待使用 CPU               可运行
线程 C：等待磁盘读取完成            不可运行
线程 D：等待用户输入                不可运行
线程 E：已经退出                    不可运行
```

调度器主要从“当前已经可以运行”的任务中选择。

Linux 中 `TASK_RUNNING` 这个状态实际上包含两种情况：

```text
正在 CPU 上运行
已经准备好，正在等待 CPU
```

因此，“运行队列”中的任务不是说它们都已经在 CPU 上运行，而是表示：

> 这些任务现在具备运行条件，只是在等待获得 CPU 时间。

---

# 四、每个 CPU 为什么都有自己的运行队列

假设有四个 CPU 核心：

```text
CPU 0
CPU 1
CPU 2
CPU 3
```

Linux 通常为每个逻辑 CPU 维护一个自己的运行队列：

```text
CPU 0 → rq0
CPU 1 → rq1
CPU 2 → rq2
CPU 3 → rq3
```

每个 `rq` 记录这个 CPU 当前运行的任务以及分配给该 CPU 的可运行任务。

例如：

```text
CPU 0 的 rq：
当前运行：A
等待运行：B、C

CPU 1 的 rq：
当前运行：D
等待运行：E

CPU 2 的 rq：
当前运行：F
等待运行：无

CPU 3 的 rq：
当前运行：idle
等待运行：无
```

如果 CPU 3 空闲，而 CPU 0 排队任务很多，负载均衡机制可能把 CPU 0 上的某个任务迁移到 CPU 3。

这里要区分两个问题：

1. **线程应该放在哪个 CPU 上**
2. **这个 CPU 下一步运行哪个线程**

第一个涉及 CPU 选择、亲和性和负载均衡；第二个才是从该 CPU 的运行队列中挑选 `next`。

---

# 五、一个 `rq` 为什么还包含多种子运行队列

你引用的简化结构是：

```text
每个 CPU 的 rq
├── dl_rq
├── rt_rq
└── cfs_rq
```

这是因为不同类型的任务，选择规则不同。

## 1. `dl_rq`

用于 Deadline 调度任务。

这类任务通常根据运行预算、周期和截止时间等参数进行调度，核心目标是满足时间约束。

## 2. `rt_rq`

用于实时任务，主要对应：

```text
SCHED_FIFO
SCHED_RR
```

实时任务主要按照实时优先级选择。高优先级实时任务可以抢占低优先级实时任务和普通任务。

## 3. `cfs_rq`

用于普通公平调度任务，例如常见的：

```text
SCHED_NORMAL
SCHED_BATCH
SCHED_IDLE
```

这里的 `cfs_rq` 名称仍然保留，但当前 Linux 公平调度的选择逻辑已经逐步从经典 CFS 过渡到 EEVDF。([Linux内核文档][3])

当前内核源码中的 `struct rq` 和相关调度结构仍包含公平、实时和 Deadline 等调度队列；公平运行队列中仍可看到 `tasks_timeline` 红黑树等数据结构。([GitHub][4])

需要注意：

> “每个 `rq` 只包含三个队列”是入门级简化。

现代 Linux 还可能包含停止任务、空闲任务以及 `sched_ext` 等调度类；启用任务组调度后，公平调度队列还可能形成分层结构。

---

# 六、调度器先选择调度类别，再选择具体任务

可以把选择过程简化成两层。

## 第一层：选择任务类别

例如当前 CPU 上有：

```text
普通任务 A
普通任务 B
实时任务 C
Deadline任务 D
```

简化优先顺序可以理解为：

```text
Deadline任务
    ↓
实时任务
    ↓
普通公平任务
    ↓
空闲任务
```

调度器优先检查更高调度等级中是否存在可以运行的任务。

例如：

```text
Deadline队列有任务吗？
有 → 从Deadline任务中选

没有
↓
实时队列有任务吗？
有 → 从实时任务中选

没有
↓
从普通公平任务中选

普通任务也没有
↓
运行每个CPU自己的idle任务
```

实际内核还有特殊的停止调度类等，所以这只是面向普通应用任务的简化顺序。

## 第二层：在相同调度类别中选择

不同类别使用不同算法。

```text
Deadline任务：根据Deadline规则选择
实时任务：优先级优先，同级再按照FIFO或时间片选择
普通任务：根据公平调度算法选择
```

所以不是把全部任务都放进一棵树，然后直接找一个最小值。

---

# 七、“CFS 选择红黑树最左侧任务”还准确吗

这句话描述的是经典 CFS 的核心思想：

```text
普通任务
    ↓
按 vruntime 排入红黑树
    ↓
选择 vruntime 较小的左侧任务
```

`vruntime` 可以粗略理解为：

> 一个经过任务权重修正的“该任务已经使用了多少公平 CPU 时间”的指标。

例如三个普通任务：

| 任务 | vruntime |
| ---- | -------: |
| A    |      120 |
| B    |       80 |
| C    |      200 |

经典 CFS 会倾向选择 B，因为：

```text
B使用的公平CPU时间较少
```

红黑树中可理解为：

```text
最左边                              最右边
B：80        A：120                 C：200
```

因此 B 更可能被选中。

Linux 官方的经典 CFS 文档确实描述了按照 `vruntime` 组织时间排序红黑树，并选择左侧任务的机制。([Linux内核文档][5])

但是，从 Linux 6.6 开始，公平调度逐步过渡到 **EEVDF**。当前公平调度不能再简单概括为“谁的 `vruntime` 最小，就运行谁”。

EEVDF 大致先判断任务是否“应该获得 CPU 时间”，再计算其虚拟截止时间：

```text
第一步：判断任务是否 eligible
第二步：在 eligible 的任务中
第三步：选择虚拟截止时间最早的任务
```

即：

[
\boxed{\text{选择有资格任务中虚拟截止时间最早者}}
]

官方文档说明，EEVDF 根据任务的虚拟运行情况计算 lag，并从 lag 不小于零的候选任务中选择虚拟截止时间最早者。([Linux内核文档][3])

所以你引用的内容应当改成：

> 经典 CFS 使用按 `vruntime` 排序的红黑树，并倾向选择左侧任务；当前 Linux 公平调度已经逐步转向 EEVDF，选择时还要考虑任务的 eligibility 和 virtual deadline，不能只比较 `vruntime`。

---

# 八、什么时候需要“重新选择线程”

调度器不会每执行一条机器指令就重新选一次任务。

一般在以下情况产生调度需求：

## 1. 当前线程主动阻塞

例如线程执行：

```c
read(fd, buffer, size);
```

但磁盘数据还没准备好。

该线程暂时无法继续运行：

```text
线程A：等待磁盘
```

它会退出可运行状态，CPU 必须选择其他任务。

## 2. 当前线程等待锁

例如：

```text
线程A需要互斥锁
但锁在线程B手中
```

线程 A 可能进入睡眠，调度器选择其他任务。

## 3. 更高优先级任务被唤醒

例如普通任务 A 正在运行，这时高优先级实时任务 B 被唤醒：

```text
普通任务A正在运行
实时任务B突然可运行
```

调度器可能要求 A 被抢占，让 B 先运行。

## 4. 当前任务运行了一段时间

调度器会记录任务消耗的 CPU 时间。公平调度算法认为其他任务更应该获得 CPU 时，会设置重新调度标志。

## 5. 当前任务主动让出 CPU

线程可以通过相应机制主动让出 CPU，让其他可运行任务获得执行机会。

## 6. 当前任务退出

任务结束后，CPU 必须寻找新的任务。

---

# 九、真正的选择过程

假设 CPU 0 当前运行任务 A，运行队列中还有 B 和 C：

```text
CPU 0：

rq->curr = A

可运行候选：
B
C
```

当产生调度需求时，简化过程如下：

```text
1. 进入内核调度器
2. 获取CPU 0的运行队列rq
3. 记录当前任务prev = A
4. 更新A的运行时间和状态
5. 从运行队列选择next
6. 如果next仍然是A，继续运行A
7. 如果next是B，执行上下文切换
```

对应的核心逻辑可以抽象为：

```c
prev = rq->curr;
next = pick_next_task(rq);

if (prev != next) {
    rq->curr = next;
    context_switch(prev, next);
}
```

Linux 内核的实际 `__schedule()` 正是围绕“查看本地运行队列、选择最合适任务、必要时进行上下文切换”展开的。([GitHub][1])

---

# 十、选择出 B 后，CPU 怎么开始执行 B

这是最关键的一步。

每个线程运行到一半被暂停时，都需要保存自己的“执行现场”，主要包括：

```text
程序计数器 PC
栈指针 SP
部分通用寄存器
内核栈状态
地址空间相关信息
浮点和向量状态
其他体系结构相关状态
```

假设线程 A 被暂停时：

```text
A的PC = 0x401080
A的SP = 0x7FFF1000
A的寄存器R1 = 20
A的寄存器R2 = 30
```

这些状态被保存后，内核载入线程 B 上次保存的状态：

```text
B的PC = 0x502040
B的SP = 0x7FFE2000
B的寄存器R1 = 100
B的寄存器R2 = 200
```

然后 CPU 的实际寄存器变成 B 的状态：

```text
CPU PC ← B的PC
CPU SP ← B的SP
CPU寄存器 ← B保存的寄存器
```

此后 CPU 从：

```text
PC = 0x502040
```

继续取机器指令。

从线程 B 看，它感觉自己只是：

```text
执行到一半
暂时停了一下
然后继续执行
```

它通常不知道中间 CPU 已经运行了其他线程。

---

# 十一、选择线程的本质是不是“执行计算机指令”

需要分成两部分回答。

## 1. 调度器本身也在执行计算机指令

Linux 调度器是一段内核程序。

当调度器运行时，CPU 确实在执行调度器的机器指令，例如：

```text
读取运行队列
比较优先级
计算虚拟截止时间
更新任务状态
选择next指针
保存寄存器
恢复寄存器
```

因此从硬件层面看：

> 调度算法本身当然也是由 CPU 执行的一系列机器指令。

## 2. 但“选择线程”不等于“执行该线程的指令”

选择线程只是决定：

```text
下一步使用哪套执行上下文
```

完整过程是：

```text
调度器选择任务B
        ↓
保存任务A的执行现场
        ↓
恢复任务B的执行现场
        ↓
CPU的PC切换为任务B的PC
        ↓
CPU开始执行任务B的机器指令
```

所以应当表述为：

[
\boxed{
\text{调度选择}
\neq
\text{执行线程指令}
}
]

而是：

[
\boxed{
\text{调度选择}
\rightarrow
\text{上下文切换}
\rightarrow
\text{执行所选线程的指令}
}
]

---

# 十二、“CPU 选择线程”其实是一种简化表达

严格来说，不是 CPU 硬件自己认识：

```text
浏览器线程
音乐线程
task_struct
优先级
vruntime
```

CPU 硬件主要认识：

```text
机器指令
寄存器
内存地址
中断
特权级
```

真正理解 `task_struct`、运行队列和调度策略的是：

```text
Linux内核调度器
```

但调度器代码又必须由 CPU 执行，所以日常会简化地说：

> CPU 选择了某个线程。

更准确的表达是：

> CPU 进入 Linux 内核并执行调度器代码，调度器从可运行任务中选出下一个 `task_struct`，随后内核执行上下文切换，使 CPU 开始执行该任务的指令。

---

# 十三、用一个完整例子串起来

现在 CPU 0 上有三个任务：

```text
A：普通任务，正在运行
B：普通任务，等待运行
C：实时任务，正在等待网络数据
```

初始状态：

```text
rq->curr = A

普通运行队列：
A、B

实时运行队列：
空
```

此时 C 等待的网络数据到达，中断处理程序唤醒 C：

```text
实时运行队列：
C
```

由于 C 是实时任务，优先级高于普通任务 A，内核设置重新调度标志。

CPU 在合适的调度点进入调度器：

```text
prev = A
```

调度器检查运行队列：

```text
Deadline队列：空
实时队列：有C
普通队列：有A、B
```

于是：

```text
next = C
```

内核随后：

```text
保存A的PC、SP及寄存器
rq->curr = C
载入C的PC、SP及寄存器
```

假设 C 上次停在：

```text
PC = 0x600100
```

切换完成后：

```text
CPU从0x600100继续执行C的指令
```

之后 C 处理完网络数据，再次睡眠：

```text
C退出可运行状态
```

调度器重新选择普通任务。公平调度算法可能在 A 和 B 中选择更应该获得 CPU 时间的一个，例如 B。

---

# 核心结论

“CPU 选择线程”包含三个层次：

```text
第一层：哪些任务已经具备运行条件
第二层：调度器从运行队列选择哪一个task_struct
第三层：切换CPU寄存器，使CPU执行该任务的指令
```

其本质可以写成：

[
\boxed{
\text{从可运行任务中选择next}
+
\text{把CPU执行上下文切换到next}
}
]

CPU 真正执行的始终是机器指令。线程调度决定的是：

> **接下来 CPU 使用哪个线程保存的 PC、栈和寄存器状态，从而继续执行哪一段机器指令。**


计算机内部最终只能存储二进制位：

[
0 \quad \text{和} \quad 1
]

负数和小数本身并不是特殊的电信号，而是人为规定一组二进制编码，并约定如何解释这些编码。

---

# 一、负数如何存储

现代计算机中的有符号整数，通常使用**补码**存储。

## 1. 为什么不能直接存负号

在十进制中可以写：

[
-5
]

但内存中没有专门的“负号字符”，只有若干个二进制位，例如 8 位：

```text
00000101
```

因此必须规定：

* 哪些编码表示正数；
* 哪些编码表示负数；
* CPU 应该怎样进行加减法。

最常用的规则就是补码。

---

## 2. 8 位整数的范围

一个 8 位二进制数有：

[
2^8=256
]

种编码。

若作为无符号整数解释，范围是：

[
0\sim255
]

若作为有符号补码整数解释，范围是：

[
-128\sim127
]

这 256 种编码被分配为：

```text
00000000 ～ 01111111：0 ～ 127
10000000 ～ 11111111：-128 ～ -1
```

最高位为 0 时通常表示非负数，最高位为 1 时通常表示负数。

但不能简单理解为“最高位只是负号”，因为剩余位也不是普通绝对值，而是补码编码。

---

# 二、负数的补码怎样得到

以 8 位的 (-5) 为例。

先写出 (+5)：

```text
00000101
```

然后按位取反：

```text
11111010
```

再加 1：

```text
11111011
```

所以：

[
-5
]

在 8 位补码中表示为：

```text
11111011
```

可以记成：

[
\text{负数补码}=\text{对应正数按位取反}+1
]

---

## 3. 怎样把补码还原成负数

看到：

```text
11111011
```

最高位为 1，说明按照有符号补码解释时是负数。

按位取反：

```text
00000100
```

再加 1：

```text
00000101
```

得到 5，所以原数是：

[
-5
]

---

# 三、为什么要使用补码

补码最大的优点是：

> 加法器不需要专门区分正数加法和负数减法。

例如计算：

[
5+(-3)
]

8 位表示如下：

[
5=00000101
]

[
-3=11111101
]

相加：

```text
  00000101
+ 11111101
-----------
1 00000010
```

结果产生了第 9 位进位。由于这里只使用 8 位，最高进位被舍弃：

```text
00000010
```

结果就是：

[
2
]

因此 CPU 可以用同一个二进制加法器完成：

* 正数加法；
* 负数加法；
* 减法。

例如：

[
5-3
]

可以转化成：

[
5+(-3)
]

---

# 四、为什么 8 位范围是 (-128\sim127)

8 位补码中：

```text
01111111 = 127
```

而：

```text
10000000 = -128
```

补码的最高位具有负权值。

8 位补码可以理解为：

[
-b_7\times2^7+
b_6\times2^6+
b_5\times2^5+\cdots+
b_0\times2^0
]

例如：

```text
11111011
```

其数值是：

[
-128+64+32+16+8+0+2+1
]

[
=-5
]

因此最高位不是单纯的“正负符号”，它代表：

[
-2^7=-128
]

对于 (n) 位补码整数，范围是：

[
-2^{n-1}\sim2^{n-1}-1
]

例如：

|  位数 |        有符号整数范围 |
| ----: | --------------------: |
|  8 位 |             -128～127 |
| 16 位 |         -32768～32767 |
| 32 位 | (-2^{31})～(2^{31}-1) |
| 64 位 | (-2^{63})～(2^{63}-1) |

---

# 五、小数如何存储

小数主要有两种存储方式：

1. **定点数**
2. **浮点数**

普通计算机中，`float` 和 `double` 通常使用浮点数；金融、嵌入式和某些控制系统中，也常使用定点数。

---

# 六、先理解二进制小数

十进制小数点左边的位权是：

[
10^0,\ 10^1,\ 10^2
]

小数点右边是：

[
10^{-1},\ 10^{-2},\ 10^{-3}
]

例如：

[
123.45
]

表示：

[
1\times10^2+
2\times10^1+
3\times10^0+
4\times10^{-1}+
5\times10^{-2}
]

二进制也是一样，只是底数变成 2。

例如：

[
101.101_2
]

表示：

[
1\times2^2+
0\times2^1+
1\times2^0+
1\times2^{-1}+
0\times2^{-2}+
1\times2^{-3}
]

即：

[
4+1+0.5+0.125=5.625
]

因此：

[
101.101_2=5.625_{10}
]

二进制小数位权如下：

| 二进制位置 |    数值 |
| ---------- | ------: |
| (2^{-1})   |     0.5 |
| (2^{-2})   |    0.25 |
| (2^{-3})   |   0.125 |
| (2^{-4})   |  0.0625 |
| (2^{-5})   | 0.03125 |

---

# 七、定点数怎样存储小数

定点数就是事先约定：

> 某几位是整数部分，某几位是小数部分。

例如使用 8 位，规定：

```text
前4位：整数部分
后4位：小数部分
```

二进制：

```text
01011000
```

可以看成：

```text
0101.1000
```

其中：

[
0101_2=5
]

[
.1000_2=0.5
]

所以表示：

[
5.5
]

从另一个角度看，内存中实际保存整数：

```text
01011000 = 88
```

但程序规定真实值为：

[
88\div2^4=5.5
]

这就是定点数。

## 定点数例子

假设统一把金额乘 100 后存为整数：

[
12.34\text{ 元}
]

存储为：

[
1234
]

读取时再除以 100：

[
1234\div100=12.34
]

这也是一种十进制定点思想。

定点数的优点是：

* 运算简单；
* 可精确控制小数位数；
* 适合金融金额和嵌入式系统。

缺点是：

* 可表示范围固定；
* 小数位和整数位必须提前分配；
* 数值过大或精度要求过高时容易溢出。

---

# 八、浮点数怎样存储

浮点数类似十进制科学计数法。

例如：

[
12345.6
]

可以写成：

[
1.23456\times10^4
]

二进制浮点数则写成：

[
1.xxxxx\times2^n
]

计算机通常把浮点数拆成三部分：

```text
符号位 + 指数部分 + 尾数部分
```

以常见的 32 位 `float` 为例，IEEE 754 格式通常为：

```text
1 位符号位
8 位指数
23 位尾数
```

结构为：

```text
S | EEEEEEEE | FFFFFFFFFFFFFFFFFFFFFFF
```

其中：

* `S`：正负；
* `E`：指数；
* `F`：有效数字，也称尾数或小数部分。

---

# 九、一个浮点数例子：5.75

先把十进制 (5.75) 转换为二进制。

整数部分：

[
5=101_2
]

小数部分：

[
0.75=0.5+0.25
]

所以：

[
0.75=0.11_2
]

因此：

[
5.75=101.11_2
]

写成二进制科学计数法：

[
101.11_2=1.0111_2\times2^2
]

于是：

* 符号位：0，因为是正数；
* 指数：2；
* 有效数字：1.0111。

IEEE 754 不直接存指数 2，而是使用偏置值。

32 位浮点数的指数偏置通常是：

[
127
]

所以存储指数：

[
2+127=129
]

129 的二进制是：

```text
10000001
```

尾数部分通常省略最前面的固定 `1`，只存：

```text
01110000000000000000000
```

所以 (5.75) 的 32 位浮点表示可以拆成：

```text
0 | 10000001 | 01110000000000000000000
```

---

# 十、负小数如何存储

负小数的浮点数表示不是用整数补码整体处理，而是使用单独的符号位。

例如：

[
-5.75
]

与 (5.75) 的指数和尾数相同，只是符号位变为 1：

```text
1 | 10000001 | 01110000000000000000000
```

因此：

* 有符号整数通常使用补码；
* 浮点数的正负通常使用单独的符号位。

这是两种不同的编码规则。

---

# 十一、为什么很多十进制小数不能精确存储

有些十进制小数可以有限地表示成二进制，例如：

[
0.5=0.1_2
]

[
0.25=0.01_2
]

[
0.75=0.11_2
]

但有些十进制小数在二进制中是无限循环的。

例如：

[
0.1_{10}
]

转换为二进制约为：

[
0.000110011001100110011\ldots_2
]

其中 `0011` 不断循环。

由于浮点数位数有限，计算机只能保存一个近似值。

因此程序中：

```text
0.1 + 0.2
```

内部结果可能接近：

```text
0.30000000000000004
```

而不是绝对精确的 `0.3`。

这不是 CPU 计算错误，而是：

> 有限位数的二进制浮点数无法精确表示某些十进制小数。

---

# 十二、float 和 double 有什么区别

常见情况：

| 类型   | 总位数 | 符号位 | 指数位 | 尾数位 |
| ------ | -----: | -----: | -----: | -----: |
| float  |     32 |      1 |      8 |     23 |
| double |     64 |      1 |     11 |     52 |

`double` 使用更多位，因此：

* 可表示的数值范围更大；
* 有效数字更多；
* 精度更高；
* 占用内存更多。

大致有效十进制数字：

```text
float：约 6～7 位
double：约 15～16 位
```

例如 `float` 对下面数值可能只能保留约 7 位有效数字：

```text
1234567
```

而 `double` 可以保存更多有效数字。

---

# 十三、浮点数不是“小数点固定”

“浮点”中的“浮”表示小数点位置可以变化。

例如同一个尾数通过不同指数，可以表示：

[
1.01\times2^3
]

或：

[
1.01\times2^{-5}
]

所以浮点数能够同时表示：

* 很大的数；
* 很小的数。

但代价是精度并不均匀。

数值越大，相邻两个可表示浮点数之间的间隔通常也越大。

例如浮点数可能能精细地区分：

```text
1.000001 和 1.000002
```

但在非常大的数附近，可能无法区分相差 1 的两个数。

---

# 十四、内存中的同一组二进制可以有不同含义

假设内存中有：

```text
11111111
```

它可以被解释为：

* 无符号整数：255；
* 8 位补码有符号整数：-1；
* 字符编码的一部分；
* 指令的一部分；
* 浮点数的一部分。

内存本身并不知道它是负数还是小数。

真正决定含义的是：

* 指令如何读取它；
* 程序声明的数据类型；
* CPU 使用哪种运算指令。

例如 C 语言中：

```c
unsigned char a = 255;
signed char b = -1;
```

在某些系统中，`a` 和 `b` 的内存二进制都可能是：

```text
11111111
```

但程序解释方式不同。

---

# 十五、核心区别

## 有符号整数

通常使用补码：

```text
正数：普通二进制
负数：按位取反再加1
```

例如 8 位：

```text
5  = 00000101
-5 = 11111011
```

## 定点小数

规定固定的小数位数：

```text
存储整数 ÷ 固定缩放倍数 = 实际值
```

## 浮点小数

使用类似科学计数法的结构：

```text
符号位 + 指数 + 尾数
```

例如：

[
5.75=1.0111_2\times2^2
]

最重要的一点是：

> 计算机没有天然的“负数”和“小数”，只有二进制位。负数和小数都是程序和硬件按照既定编码规则对这些二进制位进行解释的结果。

### 9.5 文件后缀与解释器

```
1. 源代码文件（需要编译器或解释器）
   .py, .sh, .js, .ts, .java, .c, .cpp, .go, .rs
   ↓
   这些是人类可读的代码文本
   需要编译器或解释器翻译成机器码

#### 不同文件后缀是怎么执行的？

**问题**：.py、.sh、.txt这些文件后缀有什么区别？都需要解释器吗？

**回答**：

**文件分类**：

2. 可执行文件（直接由操作系统加载执行）
   .exe（Windows）、ELF（Linux）、Mach-O（macOS）
   ↓
   这些已经是机器码了
   操作系统直接加载到内存，CPU执行

3. 数据文件（需要应用程序打开）
   .txt, .md, .docx, .xlsx, .pdf, .json
   .jpg, .png, .mp4, .mp3
   ↓
   这些是数据，不是程序
   需要对应的应用程序来读取和解释
```

常见的寄存器种类：• 通用寄存器，用来存放需要进行运算的数据，比如需要进行加和运算的两个数据。
• 程序计数器，用来存储 CPU 要执行下一条指令「所在的内存地址」，注意不是存储了下一条要执行的指令，此时指令还在内存中，程序计数器只是存储了下一条指令「的地址」。
• 指令寄存器，用来存放当前正在执行的指令，也就是指令本身，指令被执行完成之前，指令都存储在这里。
