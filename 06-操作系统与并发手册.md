本手册包含 2 份源文件：E:\GithubProject\docs\general\计算机基础知识整合指南\04-操作系统并发与锁.md、E:\GithubProject\问题答案_整理版.md（中段：隔离/并发）

> 来源：由总文档拆分生成。原补充材料会按主题整理插入到对应位置。

# 操作系统、并发与锁

## 操作系统基础

### 3.1 操作系统本质

#### 什么是中断？

#### 为什么CPU+操作系统才是应用程序运行的必要组合？

#### 操作系统到底是什么？

```
"操作系统管理硬件资源"的底层原理：

```
┌─────────────────────────────────────────────────────────┐
│                        CPU                              │
│  ┌──────────────────┐  ┌──────────────────┐            │
│  │ 用户态（Ring 3）   │  │ 内核态（Ring 0）   │            │
│  │ 应用程序代码       │  │ 操作系统代码       │            │
│  │                   │  │                   │            │
│  │ 能做的事：         │  │ 能做的事：         │            │
│  │ - 计算            │  │ - 计算            │            │
│  │ - 读写自己的内存   │  │ - 读写任何内存     │            │
│  │ - 调用系统调用     │  │ - 操作硬件端口     │            │
│  │                   │  │ - 修改页表         │            │
│  │ 不能做的事：       │  │ - 切换进程         │            │
│  │ - 操作硬件端口     │  │ - 控制中断         │            │
│  │ - 修改页表        │  │ - 访问所有设备     │            │
│  │ - 访问其他进程内存  │  │                   │            │
│  └──────────────────┘  └──────────────────┘            │
└─────────────────────────────────────────────────────────┘

```
应用程序调用 read() 时：

```
以键盘按下为例：

```
中断向量表是一个数组，存储了各个中断对应的处理程序地址：

```
中断处理程序的执行流程（汇编级别）：

```
应用程序调用 open() 时的流程：

```
硬件中断（外部设备发出）：
  - 定时器中断：时间片用完，需要切换进程
  - 键盘中断：用户按了键盘
  - 网络中断：网卡收到了数据包

```
没有中断的世界：
  CPU只会傻傻地一条一条执行当前程序的指令。
  键盘按了？不知道。时间片到了？不知道。硬盘读完了？不知道。
  CPU只能等程序自己去"轮询"检查这些事件。

**问题**：操作系统的本质是什么？

**回答**：

**是什么**：
操作系统本质上就是一个**超级大程序**，它是开机后第一个被加载运行的软件。它的核心工作就两件事：

1. 管理硬件资源（CPU、内存、硬盘、网络...）
2. 给应用程序提供统一的接口（系统调用）

**底层原理**：

没有任何"管理"！操作系统只是代码+数据结构！

实际发生的是：
  1. 操作系统是一个程序（代码+数据）
  2. 它维护一些数据结构（页表、进程表、文件表）
  3. 当应用程序请求服务时，CPU执行操作系统代码
  4. 操作系统代码修改数据结构，然后返回结果

"管理"的真相：
  - "管理内存" = 维护页表、内存分配表等数据结构
  - "管理进程" = 维护进程控制块（PCB）、运行队列等
  - "管理文件" = 维护文件系统元数据（inode、目录项等）

本质：CPU执行操作系统代码，修改内存中的数据结构
```

#### 应用程序和操作系统都是代码，区别在哪？

**问题**：应用程序也是代码，操作系统也是代码，CPU就是不断执行代码，那它们的本质区别是什么？

**是什么**：
CPU不区分"应用程序"和"操作系统"——它只是不断从内存取指令、执行指令。但在硬件层面，CPU有两种运行模式：**用户态**和**内核态**。只有运行在内核态的代码（操作系统代码）才能执行特权指令，直接改变硬件状态。

应用程序想要操作硬件（比如读硬盘），必须通过系统调用，
让CPU切换到内核态，执行操作系统代码，由操作系统去操作硬件。
```

**问题**：为什么应用程序必须依赖特定的操作系统？为什么不能写一个程序在所有系统上直接运行？

**是什么**：

应用程序一定会执行系统调用（读文件、申请内存、网络通信等），而系统调用的本质是**软件中断**。不同操作系统的中断描述符（中断向量表）中，同一个中断号对应的处理程序地址完全不同——因为不同OS的内核代码加载在不同的内存位置。

1. 编译为 INT 0x80（Linux）或 syscall（不同OS不同）
2. CPU查中断向量表，找到对应地址
3. 跳转到该地址，执行操作系统的 read 实现

关键问题：
┌─────────────────────────────────────────────────────────┐
│  Linux 的中断向量表：                                    │
│  0x80 → 0xC0100000  （Linux内核的系统调用入口地址）        │
│                                                         │
│  Windows 的中断向量表：                                   │
│  0x2E → 0x80400000  （Windows内核的系统调用入口地址）       │
│                                                         │
│  macOS 的中断向量表：                                     │
│  0x80 → 0x00100000  （macOS内核的系统调用入口地址）         │
└─────────────────────────────────────────────────────────┘

同一个中断号，跳转到的地址完全不同！
这些地址上存放的是各自操作系统的代码。

所以：
- 编译好的程序 = 机器指令 + 系统调用约定（哪个中断号、哪些参数寄存器）
- 不同OS的系统调用约定不同 → 同一个程序无法在不同OS上运行
- 这就是为什么 Windows 的 .exe 不能直接在 Linux 上运行
```

**本质**：
CPU+操作系统构成了应用程序的**运行环境**。应用程序的机器码依赖于：

1. CPU指令集（x86、ARM等）→ 决定了机器码格式
2. 操作系统的中断描述符和系统调用约定 → 决定了如何请求OS服务

两者缺一不可，这就是为什么同一个Python程序在不同系统上虽然源码相同，但CPython解释器的二进制文件是不同的。

---

**问题**：中断是什么？为什么需要中断？

**回答**：

**是什么**：
中断 = 硬件或软件发出的"通知信号"，告诉CPU："有事情发生了，快来处理！"

**中断的硬件机制**

1. 物理层面：
   - 你按下键盘上的'A'键
   - 键盘控制器芯片检测到按键
   - 键盘控制器把按键编码存入寄存器
   - 键盘控制器拉高INT引脚电压（发出中断信号）

2. 电信号传递：
   ┌──────────┐      ┌──────────┐      ┌──────────┐
   │ 键盘控制器 │──────│ 主板电路  │──────│ CPU引脚  │
   │ INT引脚   │ 电压  │          │ 信号  │ INTR    │
   └──────────┘ 变化  └──────────┘ 传递  └──────────┘

3. CPU内部（每时钟周期都在检测）：
   // CPU硬件电路（简化）
   always @(posedge clock) begin
       if (INTR_pin == HIGH) begin
           // 检测到中断信号！
           interrupt_pending <= 1;
       end
   end

4. CPU响应中断（在当前指令执行完后）：
   - CPU完成当前正在执行的指令
   - CPU把当前PC（程序计数器）压入栈
   - CPU把状态寄存器压入栈
   - CPU跳转到中断向量表对应的位置
   - 开始执行中断处理程序
```

**中断向量表**

内存地址        内容
0x0000         0x8000  // 中断0的处理程序地址
0x0004         0x8100  // 中断1的处理程序地址
0x0008         0x8200  // 中断2的处理程序地址
...

当键盘中断（假设中断号是0x21）发生时：
1. CPU从地址 0x21 * 4 = 0x84 读取处理程序地址
2. 假设读到 0x9000
3. CPU跳转到 0x9000 执行
4. 0x9000 处存放的是操作系统键盘驱动代码
```

**CPU执行中断处理程序**

; CPU硬件自动执行：
PUSH CS              ; 保存代码段寄存器
PUSH IP              ; 保存指令指针（返回地址）
PUSHF                ; 保存标志寄存器
CLI                  ; 关闭中断（防止嵌套）
JMP [中断向量表地址]  ; 跳转到处理程序

; 操作系统中断处理程序：
keyboard_handler:
    PUSH AX          ; 保存寄存器
    PUSH BX
    ; ...
  
    ; 从键盘控制器读取按键
    IN AL, 0x60      ; 从端口0x60读取按键编码
  
    ; 把按键放入键盘缓冲区
    MOV [keyboard_buffer + tail], AL
    INC tail
  
    ; 通知键盘控制器已处理
    MOV AL, 0x20
    OUT 0x20, AL     ; 发送EOI（中断结束）信号
  
    ; 恢复寄存器
    POP BX
    POP AX
  
    IRET             ; 中断返回，恢复之前的状态

关键：
- IN/OUT 指令是CPU操作硬件端口的指令
- IRET 指令让CPU恢复之前被中断的代码执行
- 整个过程是CPU执行指令，没有"操作系统接管"
```

**软件中断（系统调用）**

1. 应用程序代码：
   int fd = open("file.txt", O_RDONLY);

2. 编译后的汇编：
   MOV EAX, 5        ; 系统调用号（open是5）
   MOV EBX, filename ; 文件名地址
   MOV ECX, O_RDONLY ; 标志
   INT 0x80          ; 触发软件中断

3. INT 0x80 指令执行时：
   - CPU保存当前状态（寄存器、返回地址）
   - CPU跳转到中断向量表 0x80 对应的地址
   - 那里存放的是系统调用入口代码

4. 系统调用入口（内核代码）：
   system_call_entry:
       ; 根据EAX的值，跳转到对应的系统调用处理函数
       CMP EAX, NR_SYSCALLS
       JA bad_sys_call
       CALL [sys_call_table + EAX * 4]
   
       ; 返回用户态
       IRET

本质：
- 软件中断是CPU指令（INT n）
- 执行这条指令后，CPU跳转到内核代码
- 这是用户态→内核态切换的机制
```

**常见中断类型**：

软件中断（程序主动触发）：
  - 系统调用：应用程序请求操作系统服务
  - 异常：程序出错（除零、访问非法内存等）
```

#### 中断的核心意义是什么？

**问题**：中断为什么是操作系统最关键的机制？

**回答**：

**是什么**：
中断的核心意义在于：它让CPU能够**按照硬件设计师预先设定好的响应流程**，自动跳转到指定的代码位置并执行。

有了中断的世界：
  硬件事件发生 → 中断信号 → CPU自动跳转到中断向量表指定的地址 → 执行那里的代码

关键点：
  "中断向量表"是CPU设计师在硬件层面就决定好的机制：
  - CPU检测到中断信号
  - CPU自动保存当前状态（PC、寄存器）
  - CPU自动查中断向量表，找到处理程序地址
  - CPU自动跳转到那个地址开始执行

  这整个流程是CPU硬件电路实现的，不是软件控制的。
  操作系统要做的，只是在启动时把各个中断处理程序的地址填入中断向量表。
```

这就是为什么中断是操作系统最核心的机制——它是CPU从"只执行当前程序"变成"能响应各种事件"的关键硬件能力。

---

### 3.2 进程与线程

#### 什么是进程？

总结：`fork()` 解决的是“如何从当前进程派生出一个新进程”的问题；`exec()` 解决的是“如何让当前进程变成另一个程序”的问题。Shell 使用 `fork + exec`，本质上是为了同时满足“保留 Shell 自身”和“执行外部命令”这两个目标。

#### 什么是线程？

- 每个进程都有自己独立的内存空间
- 进程之间互相不能直接访问
- 每个进程有唯一的PID（进程ID）

```
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  微信进程      │  │  QQ进程       │  │ Python进程    │
│  内存: 200MB  │  │  内存: 150MB  │  │  内存: 50MB   │
│  PID: 1234   │  │  PID: 1235   │  │  PID: 1236   │
└──────────────┘  └──────────────┘  └──────────────┘
```

```
操作系统用进程控制块（PCB）来描述进程：

```
一个进程的内存空间布局（Linux）：

```
执行 fork() 系统调用时：

```text
Shell 进程
  ↓ fork()
父进程 Shell 继续存在
子进程获得一份以 Shell 为模板创建的进程状态
  ↓
子进程 exec(ls)
  ↓
子进程的用户态代码和数据被替换成 ls
  ↓
父进程 Shell 等待子进程结束
  ↓
Shell 继续处理下一条命令
```

```text
语义层：
子进程像是复制了父进程，二者之后互不影响。

实现层：
现代 Linux 通常不会立即复制全部物理内存，而是先复用物理页，
通过页表和写时复制推迟真正的内存拷贝。
```

```text
父进程：fork() 返回子进程 PID
子进程：fork() 返回 0
失败时：fork() 返回 -1
```

```c
pid_t pid = fork();

```
每个进程有独立的页表，实现内存隔离：

- **进程是资源分配的单位，线程是CPU调度的单位**
- 一个进程可以有多个线程，这些线程**共享进程的内存空间**

```
Linux中线程和进程都用task_struct表示：

```
pthread_create() 的执行流程：

```
线程切换比进程切换快：

```
共享的资源：
- 代码段（程序代码）
- 数据段（全局变量）
- 堆（malloc分配的内存）
- 打开的文件描述符
- 信号处理函数

```
为什么需要同步：

```
┌──────────────────────────────────────┐
│            微信进程                    │
│                                      │
│  ┌─────────┐ ┌─────────┐ ┌────────┐ │
│  │ 线程1    │ │ 线程2    │ │ 线程3   │ │
│  │ 接收消息  │ │ 渲染界面  │ │ 发文件  │ │
│  └─────────┘ └─────────┘ └────────┘ │
│                                      │
│        共享内存（聊天记录等）           │
└──────────────────────────────────────┘
```

| 动作       | 作用                                             |
| ---------- | ------------------------------------------------ |
| `fork()` | 创建一个新的子进程                               |
| `exec()` | 把当前进程的用户态程序内容替换成另一个可执行文件 |

| 方式         | 原理                           | 比喻                     |
| ------------ | ------------------------------ | ------------------------ |
| 管道（Pipe） | 一个写，一个读，单向流         | 水管，水从一头流到另一头 |
| 消息队列     | 通过内核维护的队列传消息       | 邮局，投信取信           |
| 共享内存     | 两个进程映射同一块内存         | 两个人看同一块黑板       |
| Socket       | 通过网络协议通信（即使在本机） | 打电话                   |

**是什么**：
进程 = 程序代码 + 运行时需要的资源（内存、文件句柄等）

**特点**：

**进程的数据结构**

struct task_struct {
    int pid;                    // 进程ID
    int state;                  // 状态：运行、就绪、阻塞
    void *stack;                // 内核栈地址
    struct mm_struct *mm;       // 内存管理信息
    struct files_struct *files; // 打开的文件列表
    struct list_head children;  // 子进程列表
    // ...还有很多字段
};

每个进程都有一个 task_struct 结构体，存储在内核内存中。
```

**进程的内存布局**

高地址
┌────────────────────┐
│ 内核空间            │ ← 用户程序不能直接访问
├────────────────────┤
│ 栈（向下增长）       │ ← 局部变量、函数调用
│         ↓          │
│                    │
│         ↑          │
│ 堆（向上增长）       │ ← malloc/new 分配
├────────────────────┤
│ BSS段              │ ← 未初始化的全局变量
├────────────────────┤
│ 数据段              │ ← 已初始化的全局变量
├────────────────────┤
│ 代码段              │ ← 程序指令（只读）
└────────────────────┘
低地址

关键：每个进程都有自己独立的这个布局
```

**创建进程时CPU做了什么**

1. 应用程序调用 fork()：
   pid_t pid = fork();

2. 编译后的汇编：
   MOV EAX, 57       ; fork的系统调用号
   INT 0x80          ; 触发系统调用

3. 内核的 fork 实现：
   int sys_fork() {
       // 1. 分配新的 task_struct
       struct task_struct *child = alloc_task_struct();
   
       // 2. 复制父进程的 task_struct
       memcpy(child, current, sizeof(struct task_struct));
       child->pid = get_new_pid();
   
       // 3. 复制内存空间（写时复制）
       child->mm = copy_mm(current->mm);
   
       // 4. 复制文件描述符表
       child->files = copy_files(current->files);
   
       // 5. 把子进程加入运行队列
       list_add(&child->run_list, &runqueue);
   
       // 6. 返回子进程PID给父进程
       return child->pid;
   }

4. fork() 的特殊之处：
   - 返回两次：父进程返回子进程PID，子进程返回0
   - 这是通过设置不同的返回值实现的：
     child->regs.eax = 0;           // 子进程返回0
      current->regs.eax = child->pid; // 父进程返回PID
```

#### fork 的本质是什么？Shell 为什么用 fork + exec 执行外部命令？

**问题**：`fork()` 在进程模型里表示什么？Shell 执行外部命令时，为什么通常是 `fork()` 之后再 `exec()`？

**回答**：

`fork()` 的核心语义是：**以当前进程为模板，由内核创建一个新的子进程**。

这里要区分两个动作：

Shell 执行外部命令时，不能只使用 `exec()`。原因是 `exec()` 会替换当前进程的程序映像。如果 Shell 进程自己直接调用 `exec("ls")`，那么当前 Shell 进程会变成 `ls` 进程；`ls` 结束后，原来的 Shell 不会自动恢复。

因此 Shell 需要先调用 `fork()` 创建子进程，再让子进程调用 `exec()`。这样父进程 Shell 保留下来，子进程被替换成目标命令。

从内核角度看，`fork()` 创建的不是一份源代码副本，而是一个新的进程实体。内核会为子进程建立对应的进程控制信息，包括 PID、父子关系、调度状态、寄存器上下文、虚拟地址空间描述、文件描述符表等。

内存复制需要分语义层和实现层理解：

刚 `fork()` 完成时，父子进程的虚拟地址空间看起来相同，但它们的页表会指向同一批物理页，并把相关页标记为只读。当其中一个进程尝试写入某一页时，CPU 的内存管理单元触发异常，内核再复制这一页，并修改页表，使写入方拥有自己的物理页。

“调用一次，返回两次”也是这个机制的结果。`fork()` 调用完成后，系统中已经存在父、子两个进程，它们都会从 `fork()` 返回后的下一条指令继续执行。为了让同一份代码区分自己处于父进程还是子进程，内核给它们设置不同返回值：

典型代码因此会写成：

if (pid == 0) {
    execve(path, argv, environ);    // 子进程执行目标程序
} else if (pid > 0) {
    waitpid(pid, &status, 0);       // 父进程等待子进程
}
```

**进程隔离的实现**

进程A的页表：
  虚拟地址 0x1000 → 物理地址 0x5000
  虚拟地址 0x2000 → 物理地址 0x6000

进程B的页表：
  虚拟地址 0x1000 → 物理地址 0x7000  ← 不同的物理页
  虚拟地址 0x2000 → 物理地址 0x8000

当CPU切换进程时：
1. 内核把新进程的页表地址写入 CR3 寄存器
2. CPU的MMU开始使用新的页表
3. 进程A的虚拟地址 0x1000 和进程B的虚拟地址 0x1000 指向不同的物理内存

这就是"进程有独立内存空间"的底层实现。
```

---

**问题**：线程是什么？线程和进程有什么区别？

**回答**：

**是什么**：

**线程的数据结构**

struct task_struct {
    int pid;                    // 线程ID
    int tgid;                   // 线程组ID（进程ID）
    struct task_struct *group_leader;  // 指向主线程
    struct mm_struct *mm;       // 共享的内存空间
    void *stack;                // 线程自己的栈
    // ...
};

同一个进程的线程：
- 有不同的pid
- 有相同的tgid
- 共享同一个mm（内存空间）
- 各自有独立的stack（栈）
```

**创建线程时CPU做了什么**

1. 应用程序调用：
   pthread_create(&tid, NULL, func, arg);

2. glibc的pthread_create：
   int pthread_create(pthread_t *tid, void *(*func)(void*), void *arg) {
       // 分配线程栈
       void *stack = mmap(NULL, STACK_SIZE, PROT_READ|PROT_WRITE,
                          MAP_PRIVATE|MAP_ANONYMOUS, -1, 0);
   
       // 调用clone系统调用
       int flags = CLONE_VM | CLONE_FS | CLONE_FILES | CLONE_SIGHAND;
       tid = clone(func, stack, flags, arg);
   
       return 0;
   }

3. clone系统调用（内核）：
   int sys_clone(unsigned long flags, void *stack) {
       struct task_struct *new = copy_process(current, flags);
   
       // CLONE_VM：共享内存空间
       if (flags & CLONE_VM) {
           new->mm = current->mm;  // 共享
           atomic_inc(&new->mm->mm_users);
       } else {
           new->mm = copy_mm(current->mm);  // 复制
       }
   
       // 设置线程栈
       new->stack = stack;
   
       // 加入运行队列
       wake_up_process(new);
   
       return new->pid;
   }

关键：线程共享内存空间，但有自己的栈
```

**线程切换**

进程切换：
1. 保存寄存器
2. 切换页表（CR3寄存器）  ← 开销大
3. 刷新TLB
4. 恢复寄存器

线程切换（同进程内）：
1. 保存寄存器
2. 切换栈指针  ← 只需要这个
3. 恢复寄存器

因为线程共享地址空间，不需要切换页表
```

**线程共享与独立**

独立的资源：
- 栈（局部变量、函数调用）
- 寄存器状态
- 线程ID
- 信号掩码
- errno变量

示例：
int global_var = 0;  // 所有线程共享

void *thread_func(void *arg) {
    int local_var = 0;  // 每个线程独立
    local_var++;
    global_var++;  // 可能产生竞争条件！
}
```

**线程同步**

线程A                          线程B
global_var = 0
读取 global_var → 0
                               读取 global_var → 0
计算 0 + 1 = 1
写入 global_var = 1
                               计算 0 + 1 = 1
                               写入 global_var = 1
结果：global_var = 1（应该是2）

互斥锁的实现：
pthread_mutex_lock:
    ; 原子操作
    LOCK CMPXCHG [mutex], 1
    JZ acquired
    ; 未获取锁，阻塞等待
    CALL __pthread_lock_wait
acquired:

pthread_mutex_unlock:
    MOV [mutex], 0
    ; 唤醒等待的线程
    CALL __pthread_unlock_wake

LOCK前缀保证CMPXCHG是原子操作
```

**为什么要有线程**：
如果微信只用一个线程：你发文件的时候，界面就卡住了。有了多线程，一个线程发文件，另一个线程同时画界面，互不影响。

#### 进程之间怎么通信？

**问题**：进程之间怎么通信？

**回答**：

进程的内存是隔离的，操作系统提供了以下通信方式：

---

### 3.3 内存管理

#### 什么是虚拟内存？

```
CPU中的内存管理单元（MMU）负责地址转换：

```
页表是操作系统维护的数据结构，存储在内存中：

```
当访问的页不在物理内存中时：

```
每个进程有独立的页表：

```
进程A看到的：                    物理内存（真实的）：
┌──────────────┐               ┌──────────────┐
│ 地址 0x0000  │ ──映射──→     │ 物理地址 0x5000│
│ 地址 0x1000  │ ──映射──→     │ 物理地址 0x8000│
└──────────────┘               └──────────────┘

**问题**：虚拟内存是什么？为什么需要虚拟内存？

**回答**：

**是什么**：
操作系统给每个进程一个**假的（虚拟的）内存空间**，让它以为自己独占了一大块连续内存。实际上，操作系统在背后把这些虚拟地址**映射**到真实的物理内存地址。

**虚拟内存的硬件支持**

┌─────────────────────────────────────────────────────────┐
│ CPU                                                      │
│ ┌─────────────┐    ┌─────────────┐    ┌─────────────┐  │
│ │ 执行指令     │───→│ 生成虚拟地址 │───→│ MMU转换     │  │
│ │ MOV EAX, [addr]│  │ 0x1000      │    │ 0x1000→0x5000│ │
│ └─────────────┘    └─────────────┘    └──────┬──────┘  │
│                                               │         │
│                                               ▼         │
│                                        ┌─────────────┐  │
│                                        │ 物理地址     │  │
│                                        │ 0x5000      │  │
│                                        └─────────────┘  │
└─────────────────────────────────────────────────────────┘

MMU查页表完成转换：
虚拟地址 0x1000 → 查页表 → 物理地址 0x5000
```

**页表的结构**

每个进程有自己的页表：

进程A的页表：
┌──────────┬──────────┬──────────┐
│ 虚拟页号  │ 物理页号  │ 权限位    │
├──────────┼──────────┼──────────┤
│ 0        │ 5        │ R/W      │
│ 1        │ 8        │ R/W      │
│ 2        │ 12       │ R        │
│ ...      │ ...      │ ...      │
└──────────┴──────────┴──────────┘

进程B的页表：
┌──────────┬──────────┬──────────┐
│ 虚拟页号  │ 物理页号  │ 权限位    │
├──────────┼──────────┼──────────┤
│ 0        │ 20       │ R/W      │  ← 不同的物理页
│ 1        │ 25       │ R/W      │
│ 2        │ 30       │ R        │
│ ...      │ ...      │ ...      │
└──────────┴──────────┴──────────┘

关键：进程A的虚拟页0和进程B的虚拟页0映射到不同的物理页
```

**缺页异常**

1. MMU发现页表项的present位为0
2. 触发缺页异常（异常号14）
3. CPU跳转到缺页处理程序

缺页处理程序：
void page_fault_handler() {
    // 获取导致缺页的虚拟地址
    void *fault_addr = CR2_register;
  
    // 在磁盘上找到对应的页
    void *disk_addr = find_in_swap(fault_addr);
  
    // 分配一个空闲物理页
    void *physical_page = alloc_physical_page();
  
    // 从磁盘读取数据到物理页
    read_from_disk(disk_addr, physical_page, PAGE_SIZE);
  
    // 更新页表
    pte->physical_page = physical_page;
    pte->present = 1;
  
    // 返回，重新执行导致缺页的指令
}

这样，进程可以使用比物理内存更大的内存空间
```

**进程隔离的实现**

进程切换时：
void switch_process(struct task_struct *next) {
    // 切换页表
    // 把新进程的页表地址写入CR3寄存器
    write_cr3(next->mm->pgd);
  
    // 这会使TLB失效
    // 新进程的地址空间生效
}

效果：
- 进程A访问地址0x1000 → 物理地址0x5000
- 进程B访问地址0x1000 → 物理地址0x8000
- 它们互不影响

进程A无法访问进程B的内存：
- 进程A的页表中没有进程B的物理页映射
- 如果尝试访问，MMU发现页表项无效
- 触发段错误（Segmentation Fault）
```

**为什么**：

进程A和进程B都觉得自己的内存从0x0000开始
但实际上它们被映射到了完全不同的物理位置
这样它们就不可能互相访问对方的内存了
```

### 3.5 Shell与终端

```
系统启动时：

```
Shell主循环：

```
执行 ls -la /home：

```
执行 ls | grep txt：

```
执行 ls > output.txt：

#### Shell是什么？

**问题**：Shell是什么？Shell和终端有什么区别？

**回答**：

**是什么**：
Shell是一个**命令行解释器程序**。你在终端输入命令，Shell负责理解你的命令，然后告诉操作系统去执行。

**Shell进程的启动**

1. 内核启动init进程（PID 1）
2. init进程启动getty程序：
   // getty等待用户登录
   int main() {
       while (1) {
           // 打开终端设备
           int fd = open("/dev/tty1", O_RDWR);
   
           // 显示登录提示
           write(fd, "login: ", 7);
   
           // 读取用户名
           read(fd, username, 32);
   
           // 启动login程序验证密码
           execl("/bin/login", "login", username, NULL);
       }
   }

3. login验证成功后启动Shell：
   int main(int argc, char *argv[]) {
       char *shell = getpwnam(username)->pw_shell;
   
       // 设置环境变量
       setenv("HOME", home_dir, 1);
       setenv("USER", username, 1);
   
       // 启动Shell
       execl(shell, shell, NULL);
   }

4. Shell进程开始运行，等待用户输入
```

**Shell读取命令**

int main() {
    char line[4096];
  
    while (1) {
        // 显示提示符
        write(1, "$ ", 2);
  
        // 读取用户输入
        int n = read(0, line, sizeof(line));
        line[n-1] = '\0';  // 去掉换行符
  
        // 解析命令
        char **argv = parse_line(line);
  
        // 执行命令
        execute_command(argv);
    }
}

解析命令：
char **parse_line(char *line) {
    char **argv = malloc(sizeof(char*) * 64);
    int argc = 0;
    char *token = strtok(line, " ");
  
    while (token != NULL) {
        argv[argc++] = strdup(token);
        token = strtok(NULL, " ");
    }
    argv[argc] = NULL;
  
    return argv;
}
```

**Shell执行外部命令**

void execute_command(char **argv) {
    // 1. 检查是否是内置命令
    if (strcmp(argv[0], "cd") == 0) {
        chdir(argv[1]);
        return;
    }
  
    // 2. 查找命令位置
    char *path = find_command(argv[0]);
    if (path == NULL) {
        printf("command not found: %s\n", argv[0]);
        return;
    }
  
    // 3. 创建子进程
    pid_t pid = fork();
  
    if (pid == 0) {
        // 子进程
        execve(path, argv, environ);
        // 如果execve返回，说明执行失败
        exit(1);
    } else {
        // 父进程（Shell）
        int status;
        waitpid(pid, &status, 0);  // 等待子进程结束
    }
}

查找命令：
char *find_command(char *cmd) {
    char *path_env = getenv("PATH");
    char *path = strtok(path_env, ":");
  
    while (path != NULL) {
        char full_path[256];
        sprintf(full_path, "%s/%s", path, cmd);
  
        // 检查文件是否存在且可执行
        if (access(full_path, X_OK) == 0) {
            return strdup(full_path);
        }
  
        path = strtok(NULL, ":");
    }
  
    return NULL;
}
```

**管道的实现**

void execute_pipeline(char **cmd1, char **cmd2) {
    int pipefd[2];
    pipe(pipefd);  // 创建管道
  
    pid_t pid1 = fork();
    if (pid1 == 0) {
        // 子进程1：执行ls
        close(pipefd[0]);  // 关闭读端
        dup2(pipefd[1], 1);  // 标准输出重定向到管道写端
        close(pipefd[1]);
        execve("/bin/ls", cmd1, environ);
    }
  
    pid_t pid2 = fork();
    if (pid2 == 0) {
        // 子进程2：执行grep
        close(pipefd[1]);  // 关闭写端
        dup2(pipefd[0], 0);  // 标准输入重定向到管道读端
        close(pipefd[0]);
        execve("/bin/grep", cmd2, environ);
    }
  
    // 父进程
    close(pipefd[0]);
    close(pipefd[1]);
    waitpid(pid1, NULL, 0);
    waitpid(pid2, NULL, 0);
}

管道的本质：
- pipe() 系统调用创建一个内核缓冲区
- 写端写入的数据存入缓冲区
- 读端从缓冲区读取数据
- 内核负责数据传输
```

**重定向的实现**

void execute_with_redirect(char **argv, char *filename) {
    pid_t pid = fork();
  
    if (pid == 0) {
        // 打开文件
        int fd = open(filename, O_WRONLY | O_CREAT | O_TRUNC, 0644);
  
        // 重定向标准输出
        dup2(fd, 1);  // 把文件描述符复制到标准输出
        close(fd);
  
        // 执行命令
        execve(find_command(argv[0]), argv, environ);
    }
  
    waitpid(pid, NULL, 0);
}

dup2的作用：
- 复制文件描述符
- dup2(fd, 1) 把fd复制到1（标准输出）
- 之后所有写到标准输出的内容都会写入文件
```

---

## 锁与并发控制

### 9. 线程锁、异步锁内部是不是一个 0/1 状态？

核心一句话：

```text
0 = 没人持有
1 = 已经被持有
```

```text
如果状态是 0，就改成 1，拿到锁
如果状态是 1，就阻塞等待
```

```text
先检查是不是 0
再改成 1
```

```text
_locked = False / True
_waiters = 等待协程队列
```

```text
普通锁可以抽象成共享状态，但真正可靠的锁必须同时具备原子修改和等待/唤醒机制。
```

| 组成          | 作用                                          |
| ------------- | --------------------------------------------- |
| 共享状态      | 记录锁是否被持有、持有者是谁、是否有等待者    |
| 原子操作      | 保证“检查状态并修改状态”不可被其他 CPU 插入 |
| 等待/唤醒机制 | 锁被占用时让线程阻塞，锁释放时唤醒等待线程    |

**合并后的问题：**
线程锁、异步锁的 acquire 底层是不是类似把某个数改成 1？如果已经是 1，别人就拿不到锁？

**答案：**
锁的核心不是代码本身，而是一份所有竞争者都能看见的共享状态，以及围绕这份状态建立的原子操作和等待/唤醒机制。

普通互斥锁内部可以抽象成一个状态位：

当线程调用 `acquire()` 时：

但关键点是：
这个“检查并修改”必须是**原子操作**，不能分成两步。

如果分成：

两个线程可能同时看到 0，然后都拿到锁，锁就失效了。

所以线程锁底层通常依赖操作系统和 CPU 原子指令，比如 CAS。

更完整地说，一个实际可用的互斥锁通常包含三部分：

如果只有一个普通整数，没有原子操作，两个 CPU 核可能同时读到 `0` 并同时写成 `1`，互斥性会失效。如果只有原子操作但没有等待队列，线程可能只能反复自旋，占用 CPU。操作系统互斥锁通常会把原子状态和内核等待队列结合起来：短时间竞争可能自旋，长时间拿不到锁就睡眠，释放锁时再唤醒等待者。

异步锁不一样。
`asyncio.Lock` 是单线程协程环境里的锁，内部通常就是类似：

因为协程在同一个线程事件循环里运行，所以不需要操作系统级互斥量。

---

### 10. 普通锁、可重入锁、信号量有什么区别？

总结：

核心一句话：

```text
只有 0/1 状态
同一时间只允许一个线程进入
```

```text
记录当前持有锁的线程 ID
还记录重入次数
```

```text
acquire 一次，计数 +1
release 一次，计数 -1
计数归 0 才真正释放
```

```text
内部是计数器
允许多个线程/协程同时进入
```

```python
Semaphore(3)
```

```text
普通锁是互斥，RLock 是可重复进入，Semaphore 是限制并发数量。
```

| 类型      | 内部核心          | 适用场景             |
| --------- | ----------------- | -------------------- |
| Lock      | 0/1 状态          | 同一时间只能一个人进 |
| RLock     | 持有者 + 重入计数 | 同一线程可能重复加锁 |
| Semaphore | 计数器            | 控制最大并发数       |

**合并后的问题：**
是不是所有锁都是加 1 减 1？普通锁、RLock、Semaphore 有什么区别？

**答案：**

普通互斥锁：

可重入锁 RLock：

同一个线程可以多次获取同一把锁：

信号量 Semaphore：

比如：

表示最多允许 3 个任务同时执行。

### 3. 乐观锁是什么？为什么需要它？

核心一句话：

```text
冲突不会经常发生，所以不提前加锁。
```

```text
这条数据有没有被别人改过？
```

```text
库存 = 10
version = 1
```

```text
库存 10，version 1
```

```sql
UPDATE goods
SET stock = 9, version = 2
WHERE id = 1 AND version = 1;
```

```text
库存 9，version 2
```

```sql
WHERE version = 1
```

```text
超卖
余额算错
积分覆盖
多人同时修改同一条数据导致结果错误
```

```text
乐观锁不是提前拦人，而是在更新时检查数据版本，防止旧数据覆盖新数据。
```

**合并后的问题：**
乐观锁是什么？多个用户同时查同一条数据会有什么问题？为什么需要乐观锁？

**答案：**
乐观锁是一种并发控制方案。

它默认认为：

但在真正更新数据时，会检查：

最常见实现方式是加一个 `version` 字段。

比如商品库存：

A 和 B 同时查到：

A 更新：

A 成功后，数据库变成：

B 再更新时，条件还是：

但数据库已经是 version 2，所以 B 更新失败。

这样可以防止：


---

# 附录：事务隔离与并发问题（来自 问题答案_整理版.md）

## 为什么要进行隔离？

隔离的核心目的，就是为了防止我在一次完整操作过程中，**明明自己没有修改数据，但是因为别人同时修改了数据，导致我前后读到的数据不一样**。

比如我第一次查询某个数据是 A，程序还没有处理完，第二次再查的时候却变成了 B。

这时候程序就可能不知道到底应该相信哪个数据，后续的判断、计算、修改都会出问题。

在数据库里，这种现象常见于：

**不可重复读：同一个事务中，多次读取同一条数据，结果却不一样。**

也就是说，不是我自己改了数据，而是其他事务在中间把数据修改并提交了，所以我前后看到的数据不一致。

---

## 读到了不一样的数据，会产生什么问题？

### 1. 会导致判断错误

程序很多时候是根据查询结果做判断的。

比如第一次查询库存是 10，程序判断可以下单，于是继续执行。

但是后面库存被别的请求改成了 0，程序如果还按照之前的库存结果继续处理，就可能导致超卖。

这种问题的本质是：

**程序基于旧数据做出了错误判断。**

---

### 2. 会导致计算错误

很多业务逻辑需要先读取数据，再根据读取结果进行计算。

比如账户余额第一次查询是 1000，程序准备扣款 300。

但是在扣款前，余额已经被别的事务改成了 500。

如果程序还按照之前的 1000 去计算，就可能导致扣款结果错误，甚至出现余额异常。

这种问题的本质是：

**前面读到的数据已经失效，但程序还在用它继续计算。**

---

### 3. 会导致业务流程混乱

业务流程通常依赖数据状态。

比如订单第一次查询是“未支付”，程序准备发起支付流程。

但是第二次查询时，订单已经变成“已支付”。

如果没有隔离控制，程序可能会重复支付、重复发货，或者进入错误的业务分支。

这种问题的本质是：

**同一个操作过程中，数据状态发生变化，导致程序流程前后矛盾。**

---

### 4. 会导致统计结果不准确

统计、报表、分页查询这类场景，对数据一致性要求很高。

比如程序正在统计订单总金额，前面读取的是修改前的数据，后面读取的是修改后的数据。

最终统计结果可能既不是修改前的正确结果，也不是修改后的正确结果，而是一个混合状态。

这种问题的本质是：

**一次统计过程中读到的数据不是同一个时间点的数据快照。**

---

### 5. 会导致数据修改错误

很多修改操作都是“先读，再改”。

比如库存是 10，两个请求同时读取到 10。

请求 A 准备减 1，请求 B 也准备减 1。

如果没有隔离或加锁，两个请求都可能基于 10 进行修改，最后库存变成 9，而不是正确的 8。

这种问题的本质是：

**多个操作基于同一个旧数据进行修改，导致修改结果丢失。**

这也叫**丢失更新**。

---

## 常见并发问题可以这样理解

**脏读**：读到了别人还没有提交的数据。

如果别人后面回滚了，那我刚才读到的数据就是假的。

**不可重复读**：我在同一个操作过程中，多次读取同一条数据，结果不一样。

这就是你说的“我自己什么都没做，但前后读到的数据变了”。

**幻读**：我按同一个条件查询，第一次查到 10 条，第二次查到 11 条。

不是原来的数据变了，而是别人插入或删除了符合条件的数据。

**丢失更新**：多个线程或事务同时修改同一份数据，后面的修改把前面的修改覆盖了。

---

## 最终总结

隔离的目的，不只是单纯防止别人改数据，而是为了保证一次完整操作过程中，程序读取到的数据是**稳定的、可靠的、前后一致的**。

如果前后读到的数据不一样，就可能导致：

判断错误、计算错误、流程混乱、统计不准、重复处理、超卖、余额异常、订单状态错误、数据覆盖等问题。

所以，隔离和加锁的本质，就是在并发环境下控制多个执行单元对共享数据的读写顺序，避免因为数据前后不一致而导致程序逻辑出错。

你这个问题可以分成 **3 种情况** 来看。你主要问的是：**函数里的变量到底是全局变量还是局部变量？函数结束后会不会销毁？**

---
