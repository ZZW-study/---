# 前后端全栈开发：从浏览器运行原理到完整请求链路

> 本文不是把若干概念简单堆在一起，而是围绕三个问题展开：
>
> 1. **前端代码究竟在哪里运行，浏览器到底做了什么？**
> 2. **前端和后端之间的一次请求，底层究竟经历了什么？**
> 3. **如何把这些原理落实为一个能运行、能调试、能继续扩展的项目？**

---

## 总览：先建立一张完整地图

```text
用户输入网址或操作页面
        ↓
浏览器向静态资源服务器请求 HTML / CSS / JavaScript
        ↓
浏览器解析 HTML，构建 DOM
浏览器解析 CSS，构建样式规则
浏览器执行 JavaScript
        ↓
JavaScript 通过浏览器提供的 Web API：
  ├─ 监听点击、输入、粘贴等事件
  ├─ 读取和修改 DOM
  ├─ 使用 fetch 发起 HTTP 请求
  └─ 使用 File / Blob / FormData 处理文件
        ↓
请求可能：
  ├─ 直接发送到 FastAPI 后端
  └─ 先到 Vite / Nginx，再由代理转发到后端
        ↓
FastAPI：
  ├─ 匹配路由
  ├─ 解析请求
  ├─ 校验数据
  ├─ 执行业务逻辑
  ├─ 访问数据库或其他服务
  └─ 返回状态码、响应头和响应体
        ↓
浏览器收到响应
        ↓
JavaScript 解析响应并更新页面状态
        ↓
DOM / 样式发生变化
        ↓
浏览器完成样式计算、布局、绘制与合成
        ↓
用户看到最终结果
```

这张图中最重要的边界是：

| 层次                    | 核心职责                                     |
| ----------------------- | -------------------------------------------- |
| HTML                    | 描述页面的初始结构和语义                     |
| CSS                     | 描述页面的样式规则                           |
| JavaScript / TypeScript | 描述交互逻辑和程序行为                       |
| DOM                     | 浏览器在内存中维护的文档对象模型             |
| 浏览器                  | 执行前端代码、提供 Web API、发请求并渲染页面 |
| HTTP / API              | 规定前端和后端如何交换数据                   |
| FastAPI 后端            | 校验请求、执行业务、访问数据并返回结果       |
| Vite                    | 改善前端开发和构建体验，不承担后端业务       |
| 数据库                  | 持久化业务数据，不直接与普通网页脚本通信     |

---

# 第一部分　浏览器与前端运行的本质

## 1. 浏览器不是 JavaScript，JavaScript 也不是浏览器

### 1.1 浏览器是一个完整的软件运行环境

浏览器不是一个单纯的“JavaScript 解释器”。它至少承担以下几类工作：

```text
浏览器
├─ HTML 解析：把 HTML 文本解析成 DOM
├─ CSS 解析：读取样式规则并参与样式计算
├─ JavaScript 引擎：执行 JavaScript
├─ 网络能力：加载资源、发送 HTTP 请求、处理缓存和连接
├─ 渲染能力：布局、绘制、图层合成
├─ 事件系统：处理点击、键盘、输入、拖拽、粘贴等事件
├─ Web API：DOM、fetch、File、Blob、定时器、存储等
└─ 安全机制：同源策略、CORS、权限、沙箱、内容安全策略等
```

以 Chromium 系浏览器为例，V8 主要负责执行 JavaScript；Blink 负责 HTML、CSS、DOM 和页面渲染相关工作；网络请求还需要浏览器网络模块和操作系统网络栈共同完成。

因此，下面两句话都不准确：

```text
浏览器就是 JavaScript。
JavaScript 自己可以直接访问网络、文件和页面。
```

更准确的说法是：

> **JavaScript 是语言；浏览器是运行环境。浏览器把 DOM、fetch、File、定时器等能力以 Web API 的形式提供给 JavaScript。**

---

### 1.2 JavaScript 核心语言与浏览器 Web API 要区分

下面这些属于 JavaScript 语言本身：

```js
const numbers = [1, 2, 3];
const doubled = numbers.map((value) => value * 2);

function add(a, b) {
  return a + b;
}
```

下面这些并不是 JavaScript 语言核心的一部分，而是浏览器提供的 Web API：

```js
document.querySelector("#submit-button");
window.addEventListener("resize", handler);
fetch("/api/greet");
setTimeout(callback, 1000);
new FormData();
URL.createObjectURL(file);
```

这也解释了为什么同一门 JavaScript 在不同**运行环境**中拥有不同能力：

| 运行环境   | 典型能力                                       |
| ---------- | ---------------------------------------------- |
| 浏览器     | DOM、窗口、页面事件、fetch、文件选择、剪贴板等 |
| Node.js    | 文件系统、进程、服务器套接字、包管理生态等     |
| Web Worker | 可运行 JavaScript，但不能直接操作页面 DOM      |
| 浏览器扩展 | 在额外授权下拥有扩展 API                       |

不能因为代码使用 JavaScript，就认为它一定能操作网页；只有处于拥有 DOM 的浏览器上下文中，代码才能使用 `document` 等接口。

---

### 1.3 TypeScript 最终也要变成 JavaScript

浏览器通常不直接执行项目中的 TypeScript 源文件。常见流程是：

```text
TypeScript 源码
    ↓
TypeScript 编译器或构建工具进行转换
    ↓
JavaScript
    ↓
浏览器执行
```

TypeScript 的类型主要服务于开发和构建阶段。例如：

```ts
interface GreetResponse {
  message: string;
}
```

这段接口声明不会作为运行时校验逻辑自动保留在浏览器中。构建后，类型信息通常被移除。

---

## 2. 用户输入网址后，浏览器究竟做了什么

为了突出核心逻辑，先忽略 DNS、TLS、HTTP/2、HTTP/3 等细节，只看页面层面的主流程。

### 2.1 第一次加载页面

```text
用户输入网址
    ↓
浏览器请求 HTML
    ↓
服务器返回已部署的 HTML
    ↓
浏览器从 HTML 中发现 CSS、JavaScript、图片、字体等资源
    ↓
浏览器继续请求这些资源
    ↓
解析 HTML，构建 DOM
解析 CSS，参与样式计算
执行 JavaScript
    ↓
布局、绘制、合成
    ↓
显示页面
```

需要注意：

1. 浏览器收到的是服务器对外发布的资源；
2. 收到的 JavaScript 可能已被打包、压缩、分块和改名；
3. 原始 TypeScript、开发目录、注释和源映射不一定会被发布；
4. 但凡真正发送到浏览器执行的客户端逻辑，原则上都可以被用户观察和分析。

所以，“浏览器能看到前端代码”的准确含义是：

> **客户端必须获得可执行的前端资源，因此部署到客户端的代码不能被当作秘密。它不一定等于开发者电脑中的原始源码，但其行为无法依靠隐藏来获得安全性。**

---

### 2.2 初始 HTML 不是当前页面状态的全部

假设服务器返回：

```html
<p id="message">尚未请求</p>
<button id="load-button">加载</button>
```

JavaScript 运行后可能把它改成：

```html
<p id="message">加载成功</p>
<button id="load-button" disabled>已完成</button>
```

此时需要区分三个概念：

| 概念        | 含义                                           |
| ----------- | ---------------------------------------------- |
| HTML 源响应 | 服务器最初返回的 HTML 文本                     |
| DOM         | 浏览器当前在内存中维护的文档对象结构           |
| 屏幕像素    | 浏览器根据 DOM、样式、布局和图层最终绘制的结果 |

初始 HTML 是构建 DOM 的输入之一，但 DOM 会在 JavaScript、浏览器修正、用户交互和框架更新下持续变化。

---

## 3. DOM 到底是什么

### 3.1 DOM 的本质：文档的编程接口和内存模型

DOM 是 **Document Object Model，文档对象模型**。

更准确地说：

> **DOM 是一组标准化接口。浏览器使用这些接口，把 HTML 或 XML 文档表示为内存中的对象树，使程序能够读取和修改文档的结构、内容、属性和事件。**

DOM 不是：

- 一种编程语言；
- HTML 文件本身；
- 页面截图；
- 浏览器开发者工具；
- React 的专属概念。

DOM 是浏览器将“文档”暴露给程序的一套对象模型。

---

### 3.2 HTML 如何变成 DOM 树

给定 HTML：

```html
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <title>DOM 示例</title>
  </head>
  <body>
    <p id="message">初始文字</p>
    <button id="change-button">修改文字</button>
  </body>
</html>
```

可以用简化结构表示为：

```text
Document
└─ html
   ├─ head
   │  ├─ meta
   │  └─ title
   │     └─ "DOM 示例"
   └─ body
      ├─ p#message
      │  └─ "初始文字"
      └─ button#change-button
         └─ "修改文字"
```

这里有不同类型的节点：

- `Document`：整个文档的入口；
- `Element`：`html`、`body`、`p`、`button` 等元素对象；
- `Text`：元素内部的文字节点；
- `Comment`：HTML 注释对应的节点；
- 属性通常通过元素对象的属性接口访问，不应简单理解为主树上与元素并列的普通子节点。

---

### 3.3 “普通 JavaScript”与“DOM 操作”的关系

下面的代码：

```js
const button = document.getElementById("change-button");
const message = document.getElementById("message");

button?.addEventListener("click", () => {
  if (message) {
    message.textContent = "文字已修改";
    message.style.color = "blue";
  }
});
```

其中：

| 代码                   | 实际含义                    |
| ---------------------- | --------------------------- |
| `document`           | 当前文档的`Document` 对象 |
| `getElementById()`   | 在 DOM 中查找元素           |
| `addEventListener()` | 给事件目标注册监听器        |
| `textContent`        | 读取或修改节点的文本内容    |
| `style.color`        | 修改元素的内联样式          |

这些语句的语法是 JavaScript，但它们调用的是浏览器提供的 DOM 接口。

可以这样理解：

```text
JavaScript 语法
    +
DOM API
    =
用程序操作当前页面
```

---

### 3.4 为什么不说“JavaScript 直接修改 HTML 文件”

把 HTML 源文件和 DOM 区分开，是理解前端的关键。

```text
HTML 文本
    ↓ 解析
DOM 对象树
    ↓ JavaScript 修改
新的 DOM 状态
    ↓ 浏览器渲染
新的屏幕结果
```

JavaScript 通常修改的是当前页面内存中的 DOM，而不是服务器磁盘上的 `.html` 文件。

即使执行：

```js
document.body.innerHTML = "<h1>新页面</h1>";
```

它也不是在修改服务器上的 HTML 文件，而是在让浏览器解析一段新字符串并替换当前 DOM 的部分内容。

---

### 3.5 DOM 变化后，页面为什么会变化

当 DOM 或样式发生改变时，浏览器可能进行以下工作：

```text
DOM / 样式变化
    ↓
重新计算受影响元素的样式
    ↓
必要时重新布局（Layout）
    ↓
必要时重新绘制（Paint）
    ↓
图层合成（Composite）
    ↓
显示新画面
```

不能简单地说“改一个按钮时浏览器一定只重绘这个按钮”。实际影响范围取决于修改内容：

- 改颜色通常不需要重新布局，但可能需要重绘；
- 改宽高可能影响自身和周围元素的布局；
- 插入一个元素可能导致较大范围重新布局；
- 某些动画可以主要由合成阶段处理；
- 浏览器会尽量缩小无效区域，但不是所有变化都只影响一个节点。

准确说法是：

> **DOM 操作允许程序进行局部状态更新；浏览器根据依赖关系决定需要重新计算和绘制的实际范围。**

---

### 3.6 DOM 树、CSSOM 和渲染结构不是同一个东西

可以用简化模型理解：

```text
HTML ──解析──> DOM
CSS  ──解析──> CSSOM / 样式规则
                 ↓
          样式计算与布局信息
                 ↓
             绘制与合成
```

常见区别：

| 对象             | 关注点                           |
| ---------------- | -------------------------------- |
| DOM              | 文档结构、元素、文本、属性、事件 |
| CSSOM / 样式规则 | 选择器、声明和级联规则           |
| 布局结果         | 元素尺寸与位置                   |
| 绘制记录         | 背景、边框、文本等如何绘制       |
| 合成图层         | 哪些图层如何组合到最终画面       |

“渲染树”是常用教学模型，但现代浏览器内部实现比一棵简单的树复杂得多。学习阶段应掌握概念边界，不必把某个简化流程误认为浏览器内部唯一的数据结构。

---

## 4. 事件、异步任务与页面状态

### 4.1 事件不是 JavaScript 主动轮询出来的

当用户点击按钮时，大致过程是：

```text
用户点击
    ↓
操作系统接收输入
    ↓
浏览器识别点击目标
    ↓
浏览器创建并分派事件
    ↓
已注册的 JavaScript 回调进入执行流程
    ↓
回调修改状态或 DOM
```

代码：

```js
button.addEventListener("click", handleClick);
```

不是让 JavaScript 一直检查“按钮是否被点”，而是向浏览器注册一个回调：事件发生时再调用它。

---

### 4.2 `fetch` 为什么使用 Promise

发网络请求需要时间。如果 JavaScript 阻塞等待，页面会失去响应。

```ts
const response = await fetch("/api/greet");
```

`await` 的直观含义不是“让整个浏览器停住”，而是：

1. 当前异步函数暂停后续步骤；
2. 浏览器继续处理渲染和其他事件；
3. 网络模块在后台完成请求；
4. Promise 完成后，异步函数继续执行。

因此，常见页面状态不是只有“成功”，而是：

```text
idle
  ↓ 用户提交
loading
  ├─ 请求成功 → success
  └─ 请求失败 → error
```

一个可靠页面要明确处理这四种状态，避免重复提交、无限加载和错误无提示。

---

## 5. 前端代码公开到什么程度，真正的安全边界在哪里

### 5.1 客户端代码不能保存秘密

不能把下列内容写进会发送到浏览器的代码：

- 数据库密码；
- 服务端私钥；
- JWT 签名密钥；
- 第三方服务的高权限 API Key；
- 绕过授权的内部凭证；
- 只允许服务端知道的业务规则参数。

即使代码被压缩或混淆，也不能把秘密变安全。

压缩和混淆主要用于：

- 减小文件体积；
- 提高加载效率；
- 增加阅读成本；
- 避免直接暴露有意义的变量名。

它们不是可靠的访问控制或加密手段。

---

### 5.2 后端代码通常不会作为页面资源发送给浏览器

普通网页请求会得到后端的输出，而不是后端程序文件本身：

```text
浏览器发送：
POST /api/login

后端内部执行：
读取数据库
校验密码哈希
检查账号状态
生成会话

浏览器收到：
200 + 登录结果
或
401 + 认证失败
```

后端实现没有被正常响应直接发送给浏览器，但这不意味着可以依赖“别人看不到代码”来保证安全。

安全仍然必须依赖：

- 正确认证；
- 正确授权；
- 输入校验；
- 最小权限；
- 密钥管理；
- 数据加密；
- 速率限制；
- 日志与审计；
- 安全更新；
- 防止注入、越权和信息泄露。

> **攻击者不需要拿到后端源码，也可能通过接口行为、错误信息和漏洞攻击系统。安全不能只依赖代码保密。**

---

### 5.3 用户能从前端观察到什么

通常可以观察到：

- 页面结构和样式；
- 已发送到浏览器的 JavaScript；
- 网络请求地址、方法、请求头和请求体；
- 后端返回的状态码、响应头和响应体；
- 前端的交互流程；
- 浏览器存储中的可访问数据。

通常不能通过普通页面加载直接获得：

- 服务器文件系统；
- 数据库连接密码；
- 服务端环境变量；
- 后端源文件；
- 其他用户的受保护数据；
- 服务端签名密钥。

但“不能直接获得”成立的前提是系统本身没有错误配置、越权漏洞或信息泄露。

---

### 8.2 GET、POST、PUT、PATCH、DELETE

| 方法   | 常见语义                         | 协议层是否幂等 | 常见目标               |
| ------ | -------------------------------- | -------------- | ---------------------- |
| GET    | 读取表示                         | 是             | 集合或单个资源         |
| POST   | 让目标资源按自身语义处理请求体   | 通常不是       | 集合、命令入口、处理器 |
| PUT    | 用请求表示创建或替换目标资源状态 | 是             | 明确资源 URI           |
| PATCH  | 对资源应用部分修改               | 取决于补丁语义 | 明确资源 URI           |
| DELETE | 删除目标资源                     | 是             | 明确资源 URI           |

“幂等”关注的是多次执行同一请求的预期效果，而不是每次响应的时间戳、日志编号都必须完全一样。

---

## 9. 幂等性：为什么网络重试会制造重复业务

### 9.1 幂等不是“请求只能发一次”

幂等的核心是：

> **同一个操作执行一次或重复执行，目标资源的预期最终状态相同。**

例如：

```http
PUT /users/42

{"name":"李四"}
```

重复发送，用户 42 仍然是“李四”。

而：

```http
POST /orders

{"product_id":1001,"quantity":1}
```

如果每次都创建新订单，重复发送会产生多个订单。

---

### 9.2 为什么客户端会重试

```text
客户端发送下单请求
    ↓
服务器已经成功创建订单
    ↓
响应在返回途中丢失
    ↓
客户端只知道“超时”，不知道服务器是否成功
    ↓
客户端或用户再次提交
```

这不是罕见异常，而是分布式系统必须面对的不确定性。

---

### 9.3 幂等键的基本方案

客户端为一次业务意图生成唯一键：

```http
POST /orders
Idempotency-Key: 89f4a68d-2f31-44d2-b9a0-b6d640bb61af
Content-Type: application/json

{"product_id":1001,"quantity":1}
```

服务端逻辑：

```text
接收幂等键
    ↓
在事务或原子操作中检查记录
    ├─ 第一次出现：
    │    执行业务
    │    保存业务结果
    │    保存幂等键与结果
    │    返回结果
    │
    └─ 已经出现：
         不重复执行业务
         返回第一次结果
```

只写一个“先查询、再插入”的普通逻辑仍可能发生并发穿透。可靠实现通常还需要：

- 数据库唯一约束；
- 事务；
- 原子写入；
- 必要时使用锁；
- 明确幂等键作用域和过期时间；
- 校验同一幂等键是否对应同一请求参数。

前端禁用按钮只能减少重复点击，不能替代后端幂等保证。

---

## 10. 常用状态码应该如何理解

| 状态码                         | 常见含义                             |
| ------------------------------ | ------------------------------------ |
| `200 OK`                     | 请求成功，有正常响应体               |
| `201 Created`                | 新资源创建成功                       |
| `204 No Content`             | 成功，但没有响应体                   |
| `400 Bad Request`            | 请求格式或语义不符合接口要求         |
| `401 Unauthorized`           | 未认证或认证失效；名称历史上容易误导 |
| `403 Forbidden`              | 已识别请求方，但不允许执行           |
| `404 Not Found`              | 路由或资源不存在                     |
| `405 Method Not Allowed`     | URL 存在，但该方法不允许             |
| `409 Conflict`               | 与当前资源状态发生冲突               |
| `413 Content Too Large`      | 请求内容超过服务端限制               |
| `415 Unsupported Media Type` | 请求体媒体类型不支持                 |
| `422 Unprocessable Content`  | 请求可解析，但字段或业务校验失败     |
| `429 Too Many Requests`      | 请求过于频繁                         |
| `500 Internal Server Error`  | 服务端未处理的内部错误               |
| `502 Bad Gateway`            | 网关从上游得到无效响应               |
| `503 Service Unavailable`    | 服务暂时不可用                       |
| `504 Gateway Timeout`        | 网关等待上游超时                     |

状态码只表达一层结果。稳定 API 还应提供统一错误体，例如：

```json
{
  "code": "INVALID_NAME",
  "message": "姓名不能为空",
  "details": {
    "field": "name"
  }
}
```

前端不应只靠解析人类可读字符串判断错误类型。

---

## 11. 一次 `fetch` 请求的完整链路

以用户输入姓名并点击提交为例。

### 11.1 页面加载阶段

```text
浏览器加载 index.html
    ↓
发现并加载 JavaScript
    ↓
JavaScript查找表单、输入框、按钮和结果容器
    ↓
注册 submit 事件
    ↓
渲染 idle 状态
```

---

### 11.2 提交阶段

```ts
form.addEventListener("submit", async (event) => {
  event.preventDefault();
  // ...
});
```

`event.preventDefault()` 阻止浏览器按传统表单行为重新导航或刷新页面，让 JavaScript 接管提交过程。

---

### 11.3 进入 loading 状态

```ts
submitButton.disabled = true;
renderResult("loading", "正在请求...");
```

目的包括：

- 给用户反馈；
- 减少重复点击；
- 表示当前操作未完成；
- 为成功和失败分支建立清晰状态。

---

### 11.4 构造 HTTP 请求

```ts
const response = await fetch("/api/greet", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
  },
  body: JSON.stringify({ name }),
});
```

此时前端只是声明请求。真正的编码、连接和网络发送由浏览器网络能力完成。

---

### 11.5 FastAPI 处理

```text
请求到达服务端
    ↓
Web 服务器接收连接和请求
    ↓
FastAPI 匹配 POST /api/greet
    ↓
读取 Content-Type
    ↓
解析 JSON
    ↓
Pydantic 校验字段
    ↓
调用路由函数
    ↓
构造响应模型
    ↓
序列化为 JSON
    ↓
返回状态码、响应头和响应体
```

---

### 11.6 前端读取响应

`fetch()` 在收到 404、422、500 时通常不会自动抛出网络异常。它会正常返回 `Response`，因此需要检查：

```ts
if (!response.ok) {
  throw new Error(`请求失败：${response.status}`);
}
```

然后解析：

```ts
const data: unknown = await response.json();
```

使用 `unknown` 是为了提醒：网络数据尚未证明满足 TypeScript 接口。

---

### 11.7 运行时校验

```ts
interface GreetResponse {
  message: string;
}

function isGreetResponse(value: unknown): value is GreetResponse {
  return (
    typeof value === "object" &&
    value !== null &&
    "message" in value &&
    typeof value.message === "string"
  );
}
```

小项目可以使用类型守卫；复杂项目可使用 Zod、Valibot、JSON Schema 或 OpenAPI 生成方案。

---

### 11.8 更新 DOM

```ts
renderResult("success", data.message);
```

最终链路：

```text
后端 JSON
    ↓
浏览器接收字节
    ↓
response.json() 解析
    ↓
JavaScript 得到对象
    ↓
渲染函数创建或修改 DOM
    ↓
浏览器更新画面
```

---

## 12. 同源策略、CORS 和代理

### 12.1 什么是源

对普通 HTTP(S) 页面，源通常由以下三项共同决定：

```text
scheme + host + port
```

对比：

| 地址                           | 是否与`http://localhost:5173` 同源 |
| ------------------------------ | ------------------------------------ |
| `http://localhost:5173/page` | 是                                   |
| `https://localhost:5173`     | 否，协议不同                         |
| `http://127.0.0.1:5173`      | 否，主机不同                         |
| `http://localhost:8000`      | 否，端口不同                         |

---

### 12.2 同源策略的目的

同源策略限制一个来源中的脚本随意读取另一个来源的敏感数据。

否则，用户登录银行网站后再打开恶意网站，恶意页面可能借助用户的浏览器身份直接读取银行响应。

同源策略不是简单的“跨域请求永远发不出去”。不同资源类型和请求类型行为不同。对 `fetch` 来说，核心应区分：

```text
请求是否被浏览器发送
响应是否允许当前 JavaScript 读取
```

某些请求可能已经到达服务器，但响应因为缺少正确 CORS 头而不能被前端脚本读取。

---

### 12.3 CORS 是服务器授予浏览器脚本读取权限

后端配置：

```python
from fastapi.middleware.cors import CORSMiddleware

app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "http://localhost:5173",
        "http://127.0.0.1:5173",
    ],
    allow_credentials=False,
    allow_methods=["GET", "POST"],
    allow_headers=["Content-Type"],
)
```

这表示后端在响应中提供合适的 CORS 头，浏览器据此决定是否把响应暴露给调用脚本。

CORS 不是：

- 用户认证；
- 角色授权；
- CSRF 的完整防护；
- 防火墙；
- 阻止非浏览器客户端调用接口的机制。

curl、服务端程序或攻击者自己的 HTTP 客户端不受浏览器同源策略约束。

---

### 12.4 为什么有时会出现 OPTIONS 预检

跨源请求如果不属于 CORS 简单请求，浏览器通常先发送预检：

```http
OPTIONS /api/greet
Origin: http://localhost:5173
Access-Control-Request-Method: POST
Access-Control-Request-Headers: content-type
```

例如跨源发送：

```http
Content-Type: application/json
```

通常会触发预检，因为 `application/json` 不属于 CORS 安全列出的简单内容类型。

预检通过后，浏览器才发送真正请求。

---

### 12.5 Vite 代理为什么能避免开发阶段 CORS

浏览器请求：

```text
http://localhost:5173/api/greet
```

Vite 开发服务器再转发：

```text
http://127.0.0.1:8000/api/greet
```

从浏览器视角看，请求仍然发往页面所在的 `localhost:5173`，因此属于同源请求。

```text
浏览器
   │ 请求 /api/greet
   ▼
Vite 开发服务器
   │ 服务器到服务器转发
   ▼
FastAPI
```

注意：

- `server.proxy` 主要服务于开发环境；
- 生产环境通常由 Nginx、网关、负载均衡器或平台路由实现；
- 代理不是把 Vite 放进浏览器内部，Vite 是另一个本地服务器进程。

---

# 第三部分　工程化扩展

## 13. 为什么先做“不使用 Vite”的最小项目

直接使用 React、Next.js 或大型脚手架，容易把底层链路隐藏起来。

先完成一个纯 TypeScript + FastAPI 项目，可以明确看到：

```text
.ts 源码
    ↓ tsc
.js 文件
    ↓ 静态服务器提供
浏览器加载
    ↓
事件触发
    ↓
fetch 请求
    ↓
FastAPI 响应
    ↓
JavaScript 修改 DOM
```

理解这条链路后，再使用 Vite，会知道它替你自动完成了哪些事情。

---

## 15. 使用 Vite：它解决了什么，没有解决什么

### 15.1 不使用 Vite 的痛点

前面的项目需要：

```text
修改 TypeScript
    ↓
手动或 watch 编译
    ↓
静态服务器读取 dist
    ↓
浏览器刷新
```

随着项目增大，还会遇到：

- 模块和依赖处理复杂；
- JSX/TSX、CSS 模块和资源导入需要额外配置；
- 开发启动和更新体验差；
- 生产构建优化工作增多。

---

### 15.2 Vite 的核心职责

Vite 提供两类主要能力：

```text
开发阶段：
开发服务器 + 按需模块转换 + HMR + 开发代理

生产阶段：
构建、打包、代码分割、资源处理和优化输出
```

常见命令：

```powershell
npm run dev
npm run build
npm run preview
```

Vite 不负责：

- 数据库；
- 用户权限；
- 后端业务；
- 支付；
- 持久化；
- 替代 FastAPI；
- 自动保证接口正确。

## 16. React、Vite 和 Next.js 到底是什么关系

### 16.1 三者不在同一层

| 工具    | 本质               | 主要解决的问题                                 |
| ------- | ------------------ | ---------------------------------------------- |
| React   | UI 库              | 用组件和状态描述界面                           |
| Vite    | 前端开发与构建工具 | 开发服务器、模块转换、HMR、生产构建            |
| Next.js | React 框架         | 路由、服务端与客户端渲染、数据获取、构建部署等 |

可以组合：

```text
React + Vite
```

也可以使用：

```text
React + Next.js
```

Next.js 自己提供完整工具链，不是“在 Vite 项目里再加一个普通库”这么简单。

### 16.4 Next.js App Router 的文件路由

```text
app/page.tsx           → /
app/about/page.tsx     → /about
app/users/[id]/page.tsx → /users/:id
```

页面和布局默认是服务器组件。需要状态、事件处理或浏览器 API 时，在客户端边界文件顶部声明：

```tsx
"use client";

import { useState } from "react";


export default function Counter() {
  const [count, setCount] = useState(0);

  return (
    <button onClick={() => setCount(count + 1)}>
      点击了 {count} 次
    </button>
  );
}
```

`"use client"` 表示模块边界进入客户端代码图，不等于该组件在首次页面生成时绝不经过服务器预渲染。

---

## 17. 文件与图片上传的本质

### 17.1 文件不是“神秘对象”，但也不只是裸字节

在磁盘和网络层面，文件最终由字节组成。但程序理解文件还依赖：

- 文件格式；
- MIME 类型；
- 文件名；
- 元数据；
- 文件内部结构；
- 解码器或解析器。

例如：

```text
PNG 字节
    ↓
PNG 解码器验证并解析结构
    ↓
得到像素数据
    ↓
浏览器绘制图像
```

扩展名 `.png` 只是名称的一部分，不能证明内容一定是有效 PNG。

---

### 17.2 Blob、File 和 FormData

| 对象         | 本质                                | 常见用途                       |
| ------------ | ----------------------------------- | ------------------------------ |
| `Blob`     | 一段二进制数据及媒体类型            | 生成文件、响应内容、音视频片段 |
| `File`     | 带文件名、修改时间等信息的 Blob     | 选择、拖拽或粘贴得到的文件     |
| `FormData` | 多个文本字段和 Blob/File 字段的集合 | multipart 表单上传             |

```ts
const blob = new Blob(
  ["hello"],
  { type: "text/plain" },
);

const file = new File(
  [blob],
  "hello.txt",
  { type: "text/plain" },
);

const formData = new FormData();
formData.append("message", "请查看附件");
formData.append("file", file);
```

---

### 17.3 选择、拖拽和粘贴最终都可能得到 File

```text
<input type="file"> ─────────────┐
拖拽 dataTransfer.files ────────┼→ File
粘贴 clipboardData.files/items ─┘
```

浏览器不会因为前端获得 `File`，就允许网页读取用户任意本地路径。文件必须来自用户明确选择、拖拽、粘贴或授权的过程。

---

## 18. 图片粘贴上传的完整链路

### 18.1 粘贴不等于上传

```text
本地图片或截图
    ↓
操作系统剪贴板保存数据
    ↓
用户在网页按 Ctrl+V
    ↓
浏览器分派 paste 事件
    ↓
前端获得 File / Blob
    ↓
前端可创建本地预览
    ↓
用户点击发送
    ↓
前端构造 FormData
    ↓
浏览器编码 multipart 请求
    ↓
后端接收、校验和保存
```

只有在请求成功并得到后端确认后，才能认为上传完成。

---

### 18.2 四个责任方

| 责任方   | 负责                             | 不负责                   |
| -------- | -------------------------------- | ------------------------ |
| 操作系统 | 文件系统、剪贴板、输入设备       | 不理解网页业务           |
| 浏览器   | 事件、Web API、网络和安全限制    | 不自动决定何时上传       |
| 前端代码 | 接收、预览、删除、发送和状态展示 | 不能绕过授权读取任意文件 |
| 后端     | 解析、校验、存储、权限和返回结果 | 不会自动读取用户剪贴板   |

---

### 18.3 监听粘贴和预览

```ts
const input =
  document.querySelector<HTMLTextAreaElement>(
    "#message-input",
  );

const preview =
  document.querySelector<HTMLDivElement>(
    "#attachment-preview",
  );

let pendingImage: File | null = null;
let previewUrl: string | null = null;


input?.addEventListener(
  "paste",
  (event: ClipboardEvent) => {
    const files = Array.from(
      event.clipboardData?.files ?? [],
    );

    const image = files.find(
      (file) => file.type.startsWith("image/"),
    );

    if (!image) {
      return;
    }

    event.preventDefault();
    pendingImage = image;

    if (previewUrl) {
      URL.revokeObjectURL(previewUrl);
    }

    previewUrl = URL.createObjectURL(image);

    const imageElement =
      document.createElement("img");

    imageElement.src = previewUrl;
    imageElement.width = 200;
    imageElement.alt = "待发送图片";

    preview?.replaceChildren(imageElement);
  },
);
```

`blob:` URL 是浏览器为当前内存对象创建的临时引用：

```text
blob:http://localhost:5173/...
```

它不是：

- 服务器文件路径；
- 公网图片 URL；
- 上传成功证明；
- 永久地址。

不再使用时应调用：

```ts
URL.revokeObjectURL(previewUrl);
```

---

### 18.4 用 FormData 发送

```ts
const formData = new FormData();

formData.append(
  "message",
  input?.value.trim() ?? "",
);

if (pendingImage) {
  formData.append(
    "image",
    pendingImage,
    pendingImage.name || "pasted-image.png",
  );
}

const response = await fetch("/api/messages", {
  method: "POST",
  body: formData,
});
```

不要手动设置：

```ts
headers: {
  "Content-Type": "multipart/form-data",
}
```

浏览器需要自动生成带 boundary 的完整值：

```http
Content-Type: multipart/form-data; boundary=----BrowserBoundary...
```

如果只写媒体类型而没有匹配请求体的 boundary，后端无法正确分隔各字段。

---

## 19. multipart、原始二进制和 Base64

### 19.1 三种常见方式

| 方式                | 特点                         | 适用场景               |
| ------------------- | ---------------------------- | ---------------------- |
| multipart/form-data | 文件和多个文本字段可一起发送 | 表单、聊天附件、多文件 |
| 原始二进制 body     | 整个请求体就是一个文件       | 单文件专用接口         |
| JSON + Base64       | 文件变成字符串嵌入 JSON      | 协议明确要求、小文件   |

---

### 19.2 multipart/form-data

```ts
const formData = new FormData();
formData.append("message", "看看这张图");
formData.append("image", file);

await fetch("/api/messages", {
  method: "POST",
  body: formData,
});
```

请求体的概念结构：

```text
------boundary
Content-Disposition: form-data; name="message"

看看这张图
------boundary
Content-Disposition: form-data; name="image"; filename="cat.png"
Content-Type: image/png

<图片字节>
------boundary--
```

---

### 19.3 原始二进制

```ts
await fetch("/api/image", {
  method: "POST",
  headers: {
    "Content-Type": file.type,
  },
  body: file,
});
```

优点是简单，缺点是不方便在同一个请求体中同时表达多个独立字段。

额外元数据可放在 URL、请求头中，或改用 multipart。

---

### 19.4 JSON + Base64

Base64 把二进制转换为文本字符：

```ts
const reader = new FileReader();

reader.onload = async () => {
  const dataUrl = String(reader.result);
  const base64 = dataUrl.split(",", 2)[1];

  if (!base64) {
    throw new Error("无法读取图片。");
  }

  await fetch("/api/chat", {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
    },
    body: JSON.stringify({
      message: "分析这张图片",
      image_base64: base64,
    }),
  });
};

reader.readAsDataURL(file);
```

代价：

- 体积通常增加约三分之一；
- 编解码增加 CPU 和内存开销；
- 整个字符串可能在内存中复制多次；
- 不适合大文件和高并发上传。

不要默认所有视觉模型 API 都要求 Base64。应按目标 API 选择文件上传、受控 URL、Data URL 或其他输入形式。

---

### 19.5 multipart 中携带 JSON

一个请求的整体 `Content-Type` 是 multipart，但其中某个字段可以放 JSON 文本：

```ts
const metadata = {
  conversationId: "chat_001",
  replyToId: "msg_123",
};

const formData = new FormData();

formData.append(
  "metadata",
  JSON.stringify(metadata),
);

formData.append("image", file);
```

后端取得 `metadata` 字符串后，再执行 JSON 解析和模型校验。

---

## 20. FastAPI 接收上传文件

安装：

```powershell
python -m pip install python-multipart
```

基础接口：

```python
from pathlib import Path
from uuid import uuid4

from fastapi import (
    FastAPI,
    File,
    Form,
    HTTPException,
    UploadFile,
)


app = FastAPI()

UPLOAD_DIR = Path("uploads")
UPLOAD_DIR.mkdir(parents=True, exist_ok=True)

ALLOWED_TYPES = {
    "image/png": ".png",
    "image/jpeg": ".jpg",
    "image/webp": ".webp",
}

MAX_SIZE = 10 * 1024 * 1024


@app.post("/api/messages")
async def create_message(
    message: str = Form(""),
    image: UploadFile | None = File(None),
) -> dict[str, str | None]:
    saved_filename: str | None = None

    if image is not None:
        suffix = ALLOWED_TYPES.get(
            image.content_type or "",
        )

        if suffix is None:
            raise HTTPException(
                status_code=415,
                detail="仅支持 PNG、JPEG 和 WEBP。",
            )

        saved_filename = f"{uuid4().hex}{suffix}"
        save_path = UPLOAD_DIR / saved_filename

        total_size = 0

        try:
            with save_path.open("wb") as output:
                while chunk := await image.read(
                    1024 * 1024,
                ):
                    total_size += len(chunk)

                    if total_size > MAX_SIZE:
                        raise HTTPException(
                            status_code=413,
                            detail="文件不能超过 10 MB。",
                        )

                    output.write(chunk)
        except Exception:
            save_path.unlink(missing_ok=True)
            raise
        finally:
            await image.close()

    return {
        "message": message,
        "saved_filename": saved_filename,
    }
```

这只是基础示例。它限制了声明的媒体类型和大小，但还不能证明文件真实、安全。

---

常见文件头只能辅助识别：

| 格式 | 常见开头                    |
| ---- | --------------------------- |
| PNG  | `89 50 4E 47 0D 0A 1A 0A` |
| JPEG | `FF D8 FF`                |
| PDF  | `%PDF`                    |
| ZIP  | `50 4B 03 04`             |

文件头匹配不代表整个文件有效，更不代表文件安全。
